# 全国计算机等级考试Linux应用与开发技术试题（一）

## 一、单项选择题（共 40 题）

**1. 操作系统负责管理计算机系统的(  )，其中包括处理机、存储器、设备和文件。**
<table>
  <tr><td width="50%">A. 程序</td><td>B. 文件</td></tr>
  <tr><td>C. 资源</td><td>D. 进程</td></tr>
</table>


> **答案：C**
>
> **解析：**操作系统是管理计算机硬件与软件资源的系统软件，其管理对象统称为“资源”，包括处理机、存储器、设备和文件等。A“程序”、B“文件”、D“进程”都只是资源的一部分，不能涵盖全部管理对象。
>
> **类似题：**操作系统的基本功能不包括（　）。A. 处理器管理　B. 存储管理　C. 文字处理　D. 设备管理 —— **答案：C**。文字处理属于应用软件（如 Word）的功能。
>
> **知识点：**
> - 操作系统的五大管理功能：处理器管理、存储器管理、设备管理、文件管理、作业管理。
> - 操作系统是系统软件，是用户与计算机硬件之间的接口。

**2. 下列哪一个操作系统不属于Linux家族？(  )**
<table>
  <tr><td width="50%">A. Ubuntu</td><td>B. CentOS</td></tr>
  <tr><td>C. Solaris</td><td>D. RedHat</td></tr>
</table>


> **答案：C**
>
> **解析：**Ubuntu（基于 Debian）、CentOS（基于 Red Hat Enterprise Linux）、RedHat 都是常见的 Linux 发行版；Solaris 是 Sun 公司的 UNIX 操作系统，不属于 Linux 家族。
>
> **类似题：**下列不属于 Linux 发行版的是（　）。A. Debian　B. Fedora　C. AIX　D. openSUSE —— **答案：C**。AIX 是 IBM 的 UNIX 系统。
>
> **知识点：**
> - 常见 Linux 发行版：RedHat 系（RHEL、CentOS、Fedora）、Debian 系（Debian、Ubuntu）、SUSE 系、Gentoo、Arch 等。
> - UNIX 家族：Solaris、AIX、HP-UX、macOS（基于 BSD）等。

**3. 下列哪一个指令可以设定使用者的密码？(  )**
<table>
  <tr><td width="50%">A. pwd</td><td>B. newpwd</td></tr>
  <tr><td>C. passwd</td><td>D. password</td></tr>
</table>


> **答案：C**
>
> **解析：**`passwd` 用于设置或修改用户密码；`pwd` 是显示当前工作目录；Linux 中不存在 `newpwd`、`password` 这两个命令。
>
> **类似题：**普通用户修改自己的密码应使用命令（　）。A. pwd　B. passwd　C. shadow　D. chmod —— **答案：B**。普通用户直接执行 `passwd` 即可修改自己的密码，修改他人密码需要 root 权限。
>
> **知识点：**
> - `passwd` 修改的是 `/etc/shadow` 中的密码散列值，普通用户只能改自己的密码。
> - root 可用 `passwd 用户名` 修改任意用户的密码，用 `passwd -l 用户名` 锁定账号。

**4. 切换工作目录的命令是(  )**
<table>
  <tr><td width="50%">A. pwd</td><td>B. cd</td></tr>
  <tr><td>C. who</td><td>D. ls</td></tr>
</table>


> **答案：B**
>
> **解析：**`cd`（change directory）用于切换工作目录；`pwd` 显示当前目录；`who` 显示登录用户；`ls` 列出目录内容。
>
> **类似题：**显示当前工作目录绝对路径的命令是（　）。A. cd　B. ls　C. pwd　D. who —— **答案：C**。
>
> **知识点：**
> - `cd /` 到根目录，`cd ~` 或 `cd` 到用户主目录，`cd -` 回到上一次所在目录，`cd ..` 到上一级目录。
> - `pwd` 输出的是绝对路径，是“print working directory”的缩写。

**5. 已知ls命令的结果如下：chapter chapter1 chapter123 oschapter1，执行 `ls chapter[0-9]*` 的结果是(  )**
<table>
  <tr><td width="50%">A. chapter1 chapter123</td><td>B. chapter chapter1 chapter123</td></tr>
  <tr><td>C. chapter1 chapter123 oschapter1</td><td>D. chapter chapter1 chapter123 oschapter1</td></tr>
</table>


> **答案：A**
>
> **解析：**通配符 `[0-9]` 匹配任意一个数字字符，`*` 匹配任意长度（含 0 个）的字符。因此 `chapter[0-9]*` 要求文件名以 chapter 开头、紧跟一个数字，符合条件的是 `chapter1` 和 `chapter123`；`chapter`（无数字）和 `oschapter1`（开头不是 chapter）都不匹配。
>
> **类似题：**当前目录下有 a1、a2、ab、ba 四个文件，执行 `ls a[0-9]` 的结果是（　）。A. a1 a2　B. a1 a2 ab　C. a1 a2 ab ba　D. 无匹配 —— **答案：A**。`[0-9]` 只匹配单个数字。
>
> **知识点：**
> - 常用通配符：`*` 任意多个字符，`?` 任意单个字符，`[abc]` 匹配括号内任一字符，`[!abc]` 或 `[^abc]` 匹配不在括号内的字符。
> - 通配符由 Shell 展开，与正则表达式含义不同（`*` 在正则中表示前一个字符重复 0 次或多次）。

**6. 在bash中，在一条命令后输入 `"2>&1"`，表示(  )**
<table>
  <tr><td width="50%">A. 标准错误输出重定向到标准输入</td><td>B. 标准输入重定向到标准错误输出</td></tr>
  <tr><td>C. 标准错误输出重定向到标准输出</td><td>D. 标准输出重定向到标准输入</td></tr>
</table>


> **答案：C**
>
> **解析：**文件描述符 2 代表标准错误（stderr），1 代表标准输出（stdout）。`2>&1` 的含义是“把标准错误重定向到标准输出当前指向的位置”，即标准错误输出重定向到标准输出。
>
> **类似题：**将命令 cmd 的标准输出和标准错误都保存到文件 out.log 中，正确的写法是（　）。A. `cmd > out.log`　B. `cmd 2> out.log`　C. `cmd > out.log 2>&1`　D. `cmd 2>&1 > out.log` —— **答案：C**。
>
> **知识点：**
> - 三个标准文件描述符：0 标准输入（stdin，默认键盘）、1 标准输出（stdout，默认终端）、2 标准错误（stderr，默认终端）。
> - `>` 覆盖重定向、`>>` 追加重定向、`<` 输入重定向、`2>` 错误重定向；`&>` 也可将 stdout 和 stderr 一起重定向。

**7. 默认情况下管理员创建了一个用户，就会在(  )目录下创建一个用户主目录。**
<table>
  <tr><td width="50%">A. /usr</td><td>B. /home</td></tr>
  <tr><td>C. /root</td><td>D. /etc</td></tr>
</table>


> **答案：B**
>
> **解析：**默认情况下，使用 `useradd` 创建普通用户时，系统会在 `/home` 目录下创建与用户名同名的主目录（如 `/home/user1`），并把 `/etc/skel` 中的初始配置文件复制进去。
>
> **类似题：**超级用户 root 的主目录是（　）。A. /usr　B. /home　C. /root　D. /etc —— **答案：C**。
>
> **知识点：**
> - 普通用户主目录默认 `/home/用户名`，root 的主目录是 `/root`。
> - 主目录位置可通过 `useradd -d 路径` 指定，`/etc/default/useradd` 中的 HOME 变量决定默认位置。

**8. 下列哪个参数可以删除一个用户并同时删除用户的home目录？(  )**
<table>
  <tr><td width="50%">A. rmuser -r</td><td>B. deluser -r</td></tr>
  <tr><td>C. userdel -r</td><td>D. usermgr -r</td></tr>
</table>


> **答案：C**
>
> **解析：**`userdel -r 用户名` 在删除用户的同时删除其主目录和邮件池；只执行 `userdel` 会保留主目录。`rmuser`、`deluser`、`usermgr` 不是标准命令（deluser 是 Debian 系工具，但考试以 Red Hat 系命令为准）。
>
> **类似题：**添加用户并同时指定主目录为 /opt/tom，应使用命令（　）。A. useradd -r tom　B. useradd -d /opt/tom tom　C. userdel -d /opt/tom tom　D. usermod -d tom —— **答案：B**。
>
> **知识点：**
> - `useradd` 添加用户，`usermod` 修改用户，`userdel` 删除用户。
> - 常用选项：`-d` 指定主目录、`-g` 指定主组、`-G` 指定附加组、`-u` 指定 UID、`-s` 指定登录 Shell。

**9. 使用 `ls -l` 命令列出下面的文件列表，(  )是软链接文件。**
<table>
  <tr><td width="50%">A. -rw-rw-rw- 2 stu users 56 Sep 09 11:05 file1</td><td>B. -rwxrwxrwx 2 stu users 56 Sep 09 11:05 ex</td></tr>
  <tr><td>C. drwxr-xr-x 1 users 1024 Sep 10 08:10 zhang</td><td>D. lrwxr--r-- 1 users 2024 Sep 12 08:12 cheng</td></tr>
</table>


> **答案：D**
>
> **解析：**`ls -l` 输出行首第一个字符表示文件类型：`-` 普通文件、`d` 目录、`l` 符号链接（软链接）、`b` 块设备、`c` 字符设备、`p` 管道、`s` 套接字。选项 D 以 `l` 开头，是软链接文件。
>
> **类似题：**`ls -l` 某行显示为 `drwxr-xr-x 2 root root 4096 ... dir1`，则 dir1 是（　）。A. 普通文件　B. 目录　C. 软链接　D. 块设备 —— **答案：B**。
>
> **知识点：**
> - `ls -l` 长格式各字段：文件类型与权限、硬链接数、所有者、所属组、大小、修改时间、文件名。
> - 软链接行末尾通常显示 `链接名 -> 目标名`。

**10. 为了使文件的所有者有读和写的权限，而同组用户和其他用户只能进行只读访问。在设置文件的许可权时，应当设为(  )**
<table>
  <tr><td width="50%">A. 566</td><td>B. 644</td></tr>
  <tr><td>C. 655</td><td>D. 744</td></tr>
</table>


> **答案：B**
>
> **解析：**权限用三位八进制数表示：所有者 读+写 = 4+2 = 6，同组用户 只读 = 4，其他用户 只读 = 4，合起来即 644（对应 `rw-r--r--`）。
>
> **类似题：**要求文件所有者可读可写可执行、同组用户可读可执行、其他用户无任何权限，权限应设为（　）。A. 750　B. 755　C. 640　D. 705 —— **答案：A**。7=rwx，5=r-x，0=---。
>
> **知识点：**
> - 权限数字：r=4、w=2、x=1；三类用户依次为所有者 u、同组用户 g、其他用户 o。
> - 设置命令：`chmod 644 file`；也可用符号法 `chmod u=rw,go=r file`。

**11. 用户编写了一个文本文件file，想将该文件名称改为text，下列哪个命令可以实现？(  )**
<table>
  <tr><td width="50%">A. cd file text</td><td>B. echo file > text</td></tr>
  <tr><td>C. rm file text</td><td>D. cat file > text</td></tr>
</table>


> **答案：D**
>
> **解析：**严格来说重命名文件应使用 `mv file text`，但本题选项中没有 `mv`。D 选项 `cat file > text` 通过输出重定向把 file 的内容写入新文件 text，能生成名为 text 的文件（但原文件 file 仍存在，实际是复制而非改名），是本题出题意图所指的答案。
>
> **类似题：**将文件 atxt 重命名为 txta 的正确命令是（　）。A. cd atxt txta　B. echo atxt > txta　C. rm atxt txta　D. mv atxt txta —— **答案：D**。
>
> **知识点：**
> - `mv` 既用于移动文件/目录，也用于重命名：`mv 原文件名 新文件名`。
> - `>` 是输出重定向，会新建或清空目标文件；`cat a > b` 的效果是复制内容。

**12. cd~命令的含义是(  )**
<table>
  <tr><td width="50%">A. 到当前目录</td><td>B. 到根目录</td></tr>
  <tr><td>C. 到root目录</td><td>D. 到用户的home目录</td></tr>
</table>


> **答案：D**
>
> **解析：**`~` 是当前用户主目录的缩写。`cd ~` 或单独执行 `cd` 都会切换到当前用户的主目录；`cd /` 才到根目录。
>
> **类似题：**下列命令中，可以切换到上一次所在目录的是（　）。A. cd ~　B. cd ..　C. cd /　D. cd - —— **答案：D**。
>
> **知识点：**
> - `~` 表示当前用户主目录，`~用户名` 表示指定用户的主目录。
> - `cd -` 在最近两次工作目录之间切换，`cd ..` 进入上一级目录。

**13. 在ps命令中，以下哪个参数是用来显示所有用户的进程的？(  )**
<table>
  <tr><td width="50%">A. a</td><td>B. b</td></tr>
  <tr><td>C. u</td><td>D. x</td></tr>
</table>


> **答案：A**
>
> **解析：**BSD 风格的 `ps` 选项中，`a` 表示显示所有与终端关联的用户进程（包括其他用户的进程）；`x` 表示显示没有控制终端的进程；`u` 表示以面向用户的格式显示；`b` 不是有效选项。`ps a` 即显示所有用户的进程。
>
> **类似题：**若要查看系统中所有进程（包括无终端进程），常用的命令是（　）。A. ps　B. ps aux　C. ps -a　D. ps -l —— **答案：B**。`ps aux`（BSD 风格）或 `ps -ef`（System V 风格）可列出所有进程。
>
> **知识点：**
> - `ps` 只显示瞬间进程状态，动态查看用 `top`。
> - 常用组合：`ps aux`、`ps -ef`、`ps -le`、`ps -l`（当前终端进程的详细格式）。

**14. 下列哪个命令可以终止一个用户的所有进程？(  )**
<table>
  <tr><td width="50%">A. skillall</td><td>B. skill</td></tr>
  <tr><td>C. kill</td><td>D. killall</td></tr>
</table>


> **答案：D**
>
> **解析：**`killall` 可按进程名批量终止进程（如 `killall test`），也支持按用户终止其所有进程（`killall -u 用户名`）。`kill` 通过 PID 发送信号；`skill` 也可发信号但常按条件匹配；`skillall` 不存在。
>
> **类似题：**终止 PID 为 1234 的进程，最常用的命令是（　）。A. kill 1234　B. killall 1234　C. stop 1234　D. rm 1234 —— **答案：A**。
>
> **知识点：**
> - `kill -9 PID` 发送 SIGKILL（不可捕获），用于强制结束进程；默认 `kill PID` 发送 SIGTERM（15）。
> - `killall 进程名` 按名称终止，`pkill 进程名` 也按名称匹配。

**15. 在应用程序启动时，可以使用以下哪个命令设置进程的优先级？(  )**
<table>
  <tr><td width="50%">A. priority</td><td>B. nice</td></tr>
  <tr><td>C. renice</td><td>D. setpri</td></tr>
</table>


> **答案：B**
>
> **解析：**`nice` 用于在启动程序时设置进程的 nice 值（优先级）；`renice` 用于调整已在运行进程的优先级；`priority`、`setpri` 不是 Linux 命令。
>
> **类似题：**调整正在运行进程的优先级应使用（　）。A. nice　B. renice　C. chpri　D. top —— **答案：B**。
>
> **知识点：**
> - nice 值范围 -20～19，数值越小优先级越高；普通用户只能调大 nice 值（降低优先级），root 可任意调整。
> - 用法：`nice -n 10 cmd`、`renice -n 5 -p PID`。

**16. 如何从当前系统中卸载一个已装载的文件系统？(  )**
<table>
  <tr><td width="50%">A. umount</td><td>B. dismount</td></tr>
  <tr><td>C. mount -u</td><td>D. 从/etc/fstab中删除这个文件系统项</td></tr>
</table>


> **答案：A**
>
> **解析：**卸载已挂载的文件系统使用 `umount`（注意拼写，不是 unmount）。`mount -u` 不是标准卸载选项；修改 `/etc/fstab` 只是改变开机挂载配置，不会卸载当前已挂载的设备。
>
> **类似题：**将设备 /dev/sdb1 挂载到 /mnt/data 目录的命令是（　）。A. mount /dev/sdb1 /mnt/data　B. umount /dev/sdb1 /mnt/data　C. mount -a /mnt/data　D. fdisk /dev/sdb1 —— **答案：A**。
>
> **知识点：**
> - 挂载：`mount 设备 挂载点`；卸载：`umount 设备名或挂载点`。
> - 卸载前应确保没有进程正在使用该文件系统，否则会提示 device is busy。

**17. 哪个命令用来显示系统中各个分区中inode的使用情况？(  )**
<table>
  <tr><td width="50%">A. df -i</td><td>B. df -H</td></tr>
  <tr><td>C. free -b</td><td>D. du -a -c /</td></tr>
</table>


> **答案：A**
>
> **解析：**`df -i` 显示各文件系统的 inode 使用情况（已用、可用、使用率）；`df -H` 以 1000 进制显示容量；`free` 查看内存；`du` 统计目录/文件占用的磁盘空间。
>
> **类似题：**以人类易读方式（如 GB、MB）显示磁盘空间使用情况的命令是（　）。A. df -h　B. df -i　C. du -h　D. free -h —— **答案：A**。
>
> **知识点：**
> - `df`（disk free）看分区整体空间；`du`（disk usage）看目录/文件占用。
> - inode 记录文件的元信息，inode 耗尽时即使磁盘有空间也无法新建文件。

**18. 为了能够将新建立的文件系统mount到系统目录中，还需要指定该文件系统在整个目录树中的位置，称为(  )**
<table>
  <tr><td width="50%">A. 子目录</td><td>B. 挂载点</td></tr>
  <tr><td>C. 新分区</td><td>D. 目录树</td></tr>
</table>


> **答案：B**
>
> **解析：**新文件系统要接入目录树，必须指定一个已存在的目录作为入口，这个位置称为挂载点（Mount Point）。挂载后通过该目录即可访问设备中的文件。
>
> **类似题：**Linux 中，光盘挂载后通常通过（　）目录访问光盘内容。A. /bin　B. /mnt 或 /media　C. /etc　D. /proc —— **答案：B**。
>
> **知识点：**
> - 常用挂载点：`/mnt`、`/media`；根文件系统的挂载点是 `/`。
> - 一个挂载点同时只能挂载一个文件系统，重复挂载会覆盖原内容（原内容并未删除）。

**19. 在大多数Linux发行版本中，以下哪个属于块设备(block devices)？(  )**
<table>
  <tr><td width="50%">A. 串行口</td><td>B. 硬盘</td></tr>
  <tr><td>C. 虚拟终端</td><td>D. 打印机</td></tr>
</table>


> **答案：B**
>
> **解析：**块设备以“块”为单位传输数据，可随机访问，如硬盘、U 盘、光盘；字符设备以字符流方式传输，如串行口、键盘、打印机、虚拟终端。硬盘属于典型的块设备。
>
> **类似题：**下列设备中属于字符设备的是（　）。A. 硬盘　B. U 盘　C. 键盘　D. 光盘 —— **答案：C**。
>
> **知识点：**
> - 设备文件位于 `/dev` 下，用 `ls -l` 查看时首字符 `b` 为块设备、`c` 为字符设备。
> - 块设备支持缓冲和随机读写；字符设备按字节流顺序读写。

**20. 在Linux中第一个IDE磁盘的名字为(  )**
<table>
  <tr><td width="50%">A. /dev/hdb</td><td>B. /dev/hdc</td></tr>
  <tr><td>C. /dev/hda</td><td>D. /dev/hdd</td></tr>
</table>


> **答案：C**
>
> **解析：**IDE 硬盘命名为 `/dev/hdX`：第一块为 `hda`，第二块 `hdb`，第三块 `hdc`，第四块 `hdd`；分区用数字表示，如第一块盘的第一个主分区是 `hda1`。
>
> **类似题：**第二块 IDE 硬盘上的第一个逻辑分区通常命名为（　）。A. /dev/hdb1　B. /dev/hdb5　C. /dev/hda2　D. /dev/sdb1 —— **答案：B**。主分区编号 1～4，逻辑分区从 5 开始。
>
> **知识点：**
> - IDE 接口：`/dev/hda`～`/dev/hdd`；SATA/SCSI/USB 接口：`/dev/sda`、`/dev/sdb` 等。
> - 一块硬盘最多 4 个主分区，逻辑分区编号从 5 开始。

**21. 查看swap空间的使用情况该使用哪个命令？(  )**
<table>
  <tr><td width="50%">A. df</td><td>B. swapon</td></tr>
  <tr><td>C. free</td><td>D. fdisk</td></tr>
</table>


> **答案：C**
>
> **解析：**`free` 命令显示系统内存和交换空间（swap）的使用情况，`free -m` 以 MB 为单位；`df` 查看磁盘空间；`swapon` 用于启用交换分区；`fdisk` 用于磁盘分区。
>
> **类似题：**以 MB 为单位显示内存使用情况的命令是（　）。A. df -m　B. free -m　C. du -m　D. top -m —— **答案：B**。
>
> **知识点：**
> - `free` 输出中主要看 total、used、free、available 以及 swap 行。
> - swap 是磁盘上的交换空间，物理内存不足时用作虚拟内存。

**22. 下列哪项可能是Linux系统中以太网网络接口？(  )**
<table>
  <tr><td width="50%">A. 10</td><td>B. eth2</td></tr>
  <tr><td>C. net0</td><td>D. neta</td></tr>
</table>


> **答案：B**
>
> **解析：**Linux 中以太网接口通常命名为 `eth0`、`eth1`、`eth2`……（新版本也可能使用 `ens33` 等可预测命名）。`10`、`net0`、`neta` 都不是有效的网卡命名形式。
>
> **类似题：**下列哪项可能是 Linux 系统的以太网网络接口名（　）。A. 10　B. eth2　C. net0　D. neta —— **答案：B**。
>
> **知识点：**
> - 常见网络接口：`lo` 回环接口（127.0.0.1）、`ethX` 以太网、`wlanX` 无线网卡。
> - 查看网卡信息：`ifconfig`（net-tools）或 `ip addr`（iproute2）。

**23. 用什么命令可以直接显示Linux系统当前定义的主机名？(  )**
<table>
  <tr><td width="50%">A. ipname</td><td>B. host</td></tr>
  <tr><td>C. ifconfig</td><td>D. hostname</td></tr>
</table>


> **答案：D**
>
> **解析：**`hostname` 直接显示当前主机名，也可用 `hostname 新名字` 临时修改；`ifconfig` 显示网络接口；`host` 用于 DNS 查询；`ipname` 不是标准命令。
>
> **类似题：**永久修改主机名通常需要编辑的文件是（　）。A. /etc/hosts　B. /etc/hostname 或 /etc/sysconfig/network　C. /etc/resolv.conf　D. /etc/fstab —— **答案：B**。
>
> **知识点：**
> - `hostname` 临时设置主机名，重启失效；RHEL 系可修改 `/etc/sysconfig/network` 或使用 `hostnamectl set-hostname` 永久生效。
> - `/etc/hosts` 是主机名与 IP 的静态映射文件。

**24. 下列哪个命令可以列出所有当前活跃的网络接口？(  )**
<table>
  <tr><td width="50%">A. ifconfig</td><td>B. lsnet</td></tr>
  <tr><td>C. shownet</td><td>D. 以上都不是</td></tr>
</table>


> **答案：A**
>
> **解析：**`ifconfig` 不带参数时会列出当前所有活跃（已启用）的网络接口及其 IP、掩码、MAC 等信息。`lsnet`、`shownet` 不是 Linux 命令。
>
> **类似题：**查看网卡 eth0 详细配置信息的命令是（　）。A. ifconfig eth0　B. ping eth0　C. hostname eth0　D. netstat eth0 —— **答案：A**。
>
> **知识点：**
> - `ifconfig` 属于 net-tools 包，新版系统常用 `ip addr`、`ip link` 代替。
> - `ifconfig eth0 up/down` 可启用/禁用网卡。

**25. 下列哪个程序可以用来确定两台计算机间的IP连接性？(  )**
<table>
  <tr><td width="50%">A. host</td><td>B. netstat</td></tr>
  <tr><td>C. ifconfig</td><td>D. ping</td></tr>
</table>


> **答案：D**
>
> **解析：**`ping` 通过发送 ICMP 回显请求报文测试与目标主机之间的连通性；`host` 用于域名解析；`netstat` 查看网络连接/端口；`ifconfig` 查看和配置网卡。
>
> **类似题：**要测试本机与 192.168.1.1 的连通性，应使用命令（　）。A. ping 192.168.1.1　B. ifconfig 192.168.1.1　C. netstat 192.168.1.1　D. arp 192.168.1.1 —— **答案：A**。
>
> **知识点：**
> - `ping` 常用选项：`-c 次数` 指定发包数、`-i 间隔` 指定间隔、`-s 大小` 指定包大小。
> - `ping` 使用的是 ICMP 协议，不是 TCP/UDP 端口。

**26. 在Linux终端启动Apache的命令为(  )**
<table>
  <tr><td width="50%">A. service httpd start</td><td>B. service start</td></tr>
  <tr><td>C. httpd start</td><td>D. start</td></tr>
</table>


> **答案：A**
>
> **解析：**RHEL/CentOS 6 及以前使用 `service httpd start` 启动 Apache（服务名为 httpd）；也可用 `/etc/init.d/httpd start`。选项 D 的 `start` 单独使用不是有效命令。新版 systemd 系统使用 `systemctl start httpd`。
>
> **类似题：**停止 httpd 服务的命令是（　）。A. service httpd stop　B. service stop httpd　C. httpd stop　D. stop httpd —— **答案：A**。
>
> **知识点：**
> - 服务管理：`service 服务名 start|stop|restart|status`（SysV），`systemctl start|stop|restart|status 服务名`（systemd）。
> - 开机自启：`chkconfig httpd on`（SysV）或 `systemctl enable httpd`（systemd）。

**27. Linux的日志文件通常保存在(  )**
<table>
  <tr><td width="50%">A. /var/log</td><td>B. /usr/adm</td></tr>
  <tr><td>C. /etc/</td><td>D. /var/run</td></tr>
</table>


> **答案：A**
>
> **解析：**Linux 系统的日志文件默认集中存放在 `/var/log` 目录，如 `/var/log/messages`（系统消息）、`/var/log/secure`（安全/登录日志）等。
>
> **类似题：**记录用户登录认证信息的日志文件通常是（　）。A. /var/log/secure　B. /var/log/dmesg　C. /var/log/lastlog　D. /etc/passwd —— **答案：A**（Debian/Ubuntu 上为 `/var/log/auth.log`）。
>
> **知识点：**
> - 常见日志：`messages` 系统日志、`secure` 安全日志、`cron` 计划任务、`boot.log` 启动日志、`wtmp/utmp` 登录记录。
> - 日志由 `rsyslogd` 服务统一管理，`logrotate` 负责日志轮转。

**28. 在VI编辑器最后一行显示“4 lines yanked”，则刚输入的命令是(  )**
<table>
  <tr><td width="50%">A. 4yb</td><td>B. 4p</td></tr>
  <tr><td>C. 4yy</td><td>D. 4yw</td></tr>
</table>


> **答案：C**
>
> **解析：**vi 命令模式下 `yy` 复制（yank）一行，前面加数字表示复制多行，`4yy` 即复制 4 行，底部提示“4 lines yanked”。`4p` 是粘贴，`4yw` 是复制 4 个单词。
>
> **类似题：**在 vi 中，删除光标所在行开始的 3 行，应输入（　）。A. 3dd　B. 3yy　C. 3p　D. d3 —— **答案：A**。
>
> **知识点：**
> - 复制：`yy`（1 行）、`nyy`（n 行）；粘贴：`p`（下方）、`P`（上方）。
> - 删除：`dd`（1 行）、`ndd`（n 行）、`x`（字符）、`dw`（单词）。

**29. 在VI编辑器中，如果用户想在每次对文件进行编辑时收到反馈消息，可以输入(  )**
<table>
  <tr><td width="50%">A. set scroll=0</td><td>B. set shiftwidth=0</td></tr>
  <tr><td>C. set wrapmargin=0</td><td>D. set report=0</td></tr>
</table>


> **答案：D**
>
> **解析：**vi 的 `report` 选项用于设置报告修改行数的阈值，默认值为 2。`:set report=0` 表示只要对文件有任何修改都会在底部给出反馈信息。`scroll`、`shiftwidth`、`wrapmargin` 分别控制滚屏行数、缩进宽度和换行边界。
>
> **类似题：**在 vi 中显示行号的设置命令是（　）。A. :set nu　B. :set number=0　C. :set ruler　D. :set list —— **答案：A**。
>
> **知识点：**
> - `:set nu/nonu` 显示/隐藏行号；`:set all` 显示所有选项；`:set` 显示被修改过的选项。
> - `:set ic` 忽略大小写（ignore case），`:set ai` 自动缩进。

**30. 在Emacs中，以下哪种操作可以实现打开另一个文件以替换当前打开的文件？(  )**
<table>
  <tr><td width="50%">A. 先同时按Ctrl+x组合键, 再同时按Ctrl+s组合键</td><td>B. 先同时按Ctrl+x组合键,再同时按Ctrl+f组合键</td></tr>
  <tr><td>C. 先同时按Ctrl+x组合键, 再同时按Ctrl+b组合键</td><td>D. 先同时按Ctrl+x组合键, 再同时按Ctrl+v组合键</td></tr>
</table>


> **答案：D**
>
> **解析：**Emacs 中 `Ctrl+x Ctrl+v`（find-alternate-file）用于打开另一个文件并替换当前缓冲区；`Ctrl+x Ctrl+f` 是打开文件到新缓冲区（原缓冲区仍保留）；`Ctrl+x Ctrl+s` 是保存；`Ctrl+x Ctrl+b` 是切换缓冲区列表。
>
> **类似题：**Emacs 中保存文件的组合键是（　）。A. Ctrl+x Ctrl+s　B. Ctrl+x Ctrl+c　C. Ctrl+s　D. Ctrl+x Ctrl+w —— **答案：A**。
>
> **知识点：**
> - `C-x C-f` 打开文件、`C-x C-s` 保存、`C-x C-c` 退出、`C-x C-w` 另存为、`C-x C-v` 替换当前缓冲区打开文件。
> - Emacs 组合键记法：`C-x` 表示 Ctrl+x，`M-x` 表示 Alt+x。

**31. Emacs中将光标向前移动一屏的命令是(  )**
<table>
  <tr><td width="50%">A. &lt;Ctrl&gt;+v</td><td>B. &lt;Alt&gt;+v</td></tr>
  <tr><td>C. &lt;Ctrl&gt;+b</td><td>D. &lt;Alt&gt;+b</td></tr>
</table>


> **答案：A**
>
> **解析：**Emacs 中 `Ctrl+v` 向后（向前）翻一屏，即把光标向前移动一屏；`Alt+v` 向后翻一屏；`Ctrl+b` 光标左移一个字符；`Alt+b` 光标左移一个单词。
>
> **类似题：**Emacs 中把光标移动到行尾的组合键是（　）。A. Ctrl+e　B. Ctrl+a　C. Ctrl+f　D. Ctrl+b —— **答案：A**。
>
> **知识点：**
> - 光标移动：`C-a` 行首、`C-e` 行尾、`C-f` 右移一字符、`C-b` 左移一字符、`C-n` 下一行、`C-p` 上一行。
> - 翻页：`C-v` 向前一屏、`M-v` 向后一屏。

**32. Emacs中模式行显示模式字段中“%%”表示(  )**
<table>
  <tr><td width="50%">A. 缓冲区未被编辑</td><td>B. 缓冲区中的文本已被修改</td></tr>
  <tr><td>C. 缓冲区中的文本未被修改</td><td>D. 只读缓冲区中的文本已被修改</td></tr>
</table>


> **答案：C**
>
> **解析：**Emacs 模式行中，`%%` 表示缓冲区为只读（内容未被修改），`**` 表示缓冲区内容已被修改，`--` 表示未被修改且可写，`%*` 表示只读但已被修改。因此 `%%` 对应“缓冲区中的文本未被修改”。
>
> **类似题：**Emacs 模式行中，`**` 表示（　）。A. 缓冲区未被编辑　B. 缓冲区中的文本已被修改　C. 缓冲区中的文本未被修改　D. 只读缓冲区 —— **答案：B**。
>
> **知识点：**
> - Emacs 模式行状态符：`--` 未修改、`**` 已修改、`%%` 只读、`%*` 只读且已修改。
> - 模式行还显示文件名、当前行号（如 L3）、主模式（如 Text）等信息。

**33. SSH是什么协议？(  )**
<table>
  <tr><td width="50%">A. 安全外壳</td><td>B. 请求-响应</td></tr>
  <tr><td>C. 地址解析</td><td>D. 动态主机配置</td></tr>
</table>


> **答案：A**
>
> **解析：**SSH 的全称是 Secure Shell，即“安全外壳协议”，用于在不安全的网络中提供加密的远程登录和其他安全网络服务。B 请求-响应是 HTTP 的工作方式，C 地址解析是 ARP/DNS 的功能，D 动态主机配置是 DHCP。
>
> **类似题：**SSH 协议的中文全称是（　）。A. 安全外壳协议　B. 超文本传输协议　C. 文件传输协议　D. 简单邮件传输协议 —— **答案：A**。
>
> **知识点：**
> - SSH 工作在 TCP 22 端口，采用加密方式传输数据，可替代不安全的 telnet、ftp。
> - OpenSSH 是最常用的 SSH 实现，包含 sshd（服务端）和 ssh、scp、sftp（客户端）。

**34. 在以下协议中，为远程登录会话和其他网络协议提供安全性的协议是(  )**
<table>
  <tr><td width="50%">A. FTP</td><td>B. HTTP</td></tr>
  <tr><td>C. SSH</td><td>D. ICMP</td></tr>
</table>


> **答案：C**
>
> **解析：**SSH 通过加密和认证机制为远程登录会话及其他网络服务提供安全保障。FTP、HTTP 都是明文协议（HTTPS 才加密）；ICMP 是网络控制报文协议，本身不提供安全性。
>
> **类似题：**下列协议中，采用加密方式传输数据、可替代 telnet 的是（　）。A. FTP　B. HTTP　C. SSH　D. ICMP —— **答案：C**。
>
> **知识点：**
> - 明文协议：telnet（23）、FTP（21/20）、HTTP（80）、POP3（110）。
> - 加密协议：SSH（22）、HTTPS（443）、SFTP（基于 SSH）。

**35. OpenSSH的默认端口号为(  )**
<table>
  <tr><td width="50%">A. 80</td><td>B. 8080</td></tr>
  <tr><td>C. 21</td><td>D. 22</td></tr>
</table>


> **答案：D**
>
> **解析：**OpenSSH 服务端 sshd 默认监听 TCP 22 端口。80 是 HTTP、8080 常作 Web 代理/Tomcat 端口、21 是 FTP 控制端口。
>
> **类似题：**若要修改 SSH 服务端口，应编辑的配置文件是（　）。A. /etc/ssh/ssh_config　B. /etc/ssh/sshd_config　C. /etc/services　D. /etc/hosts —— **答案：B**。`sshd_config` 是服务端配置，`ssh_config` 是客户端配置。
>
> **知识点：**
> - 服务端配置文件 `/etc/ssh/sshd_config`，客户端配置文件 `/etc/ssh/ssh_config`。
> - 修改端口后需重启服务：`service sshd restart` 或 `systemctl restart sshd`。

**36. 使用GDB命令查看某个变量类型时，应该用命令(  )**
<table>
  <tr><td width="50%">A. set</td><td>B. whatis</td></tr>
  <tr><td>C. kill</td><td>D. print</td></tr>
</table>


> **答案：B**
>
> **解析：**GDB 中 `whatis 变量名` 用于显示变量的数据类型；`print` 显示变量的值；`set` 用于修改变量值；`kill` 用于终止被调试程序。
>
> **类似题：**GDB 中查看变量 x 当前值的命令是（　）。A. whatis x　B. print x　C. set x　D. list x —— **答案：B**。
>
> **知识点：**
> - GDB 常用命令：`list` 列源码、`break` 设断点、`run` 运行、`next/step` 单步、`print` 查看值、`whatis` 查看类型、`bt` 查看调用栈、`quit` 退出。
> - 编译时须加 `-g` 选项生成调试信息，GDB 才能显示源码和变量类型。

**37. 在使用GCC命令时，如果想产生调试信息，需要加入以下哪个选项？(  )**
<table>
  <tr><td width="50%">A. -w</td><td>B. -l</td></tr>
  <tr><td>C. -g</td><td>D. -d</td></tr>

</table>


> **答案：C**
>
> **解析：**GCC 编译时使用 `-g` 选项生成供 GDB 使用的调试信息；`-w` 关闭警告；`-l` 链接指定库；`-d` 不是标准编译选项。
>
> **类似题：**用 GCC 编译 C 程序时，若要在可执行文件中包含调试信息，应使用的选项是（　）。A. -O2　B. -Wall　C. -g　D. -c —— **答案：C**。
>
> **知识点：**
> - GCC 常用选项：`-o` 指定输出文件名、`-c` 只编译不链接、`-g` 生成调试信息、`-O` 优化、`-Wall` 打开警告、`-I` 头文件路径、`-L` 库文件路径、`-l` 链接库名。
> - 调试流程：`gcc -g test.c -o test` → `gdb test`。

**38. 下面关于make命令，说法错误的是(  )**
<table>
  <tr><td width="50%">A. make工具可以用来维护程序模块关系和生成可执行程序</td><td>B. make命令是GNU的工程化编译工具，它用于编译大量相互关联的源代码</td></tr>
  <tr><td>C. makefile文件中目标文件后跟的是源文件，最后是生成源文件的命令</td><td>D. make命令从makefile文件获取模块之间的依赖关系</td></tr>
</table>


> **答案：C**
>
> **解析：**Makefile 中规则的格式是“目标文件: 依赖文件”，下一行是生成目标的命令。选项 C 把目标与依赖的关系说反了，说法错误。其余选项均正确。
>
> **类似题：**关于 make 命令，下列说法正确的是（　）。A. make 只能编译 C 程序　B. makefile 中命令必须顶格书写　C. make 根据依赖关系和时间戳决定是否重新编译　D. make 不能指定目标 —— **答案：C**。
>
> **知识点：**
> - make 依据 Makefile 中的依赖关系，只重新编译修改过的源文件，提高编译效率。
> - Makefile 命令行的行首必须是 Tab 字符；常用伪目标 `clean` 用于清理，`make clean` 执行。

**39. FTP是什么协议？(  )**
<table>
  <tr><td width="50%">A. 域名协议</td><td>B. 动态主机配置</td></tr>
  <tr><td>C. 文件传输</td><td>D. 文件解析</td></tr>
</table>


> **答案：C**
>
> **解析：**FTP 全称 File Transfer Protocol，即文件传输协议，用于在网络上上传、下载文件。A 域名协议是 DNS，B 动态主机配置是 DHCP，D“文件解析”不是协议。
>
> **类似题：**FTP 协议默认使用的控制端口是（　）。A. 20　B. 21　C. 22　D. 23 —— **答案：B**。20 是数据端口，21 是控制端口。
>
> **知识点：**
> - FTP 采用客户端/服务器模式，有主动和被动两种数据连接方式。
> - 出于安全考虑，现在常用基于 SSH 的 SFTP 或 FTPS 替代明文 FTP。

**40. 下列哪一个不是Web应用开发框架？(  )**
<table>
  <tr><td width="50%">A. Spring</td><td>B. Unity</td></tr>
  <tr><td>C. Struts2</td><td>D. MyBatis</td></tr>
</table>

> **答案：B**
>
> **解析：**Spring、Struts2、MyBatis 都是 Java Web 开发中常用的框架（Spring 全家桶、SSH/SSM 组合等）；Unity 是跨平台游戏开发引擎，不属于 Web 应用开发框架。
>
> **类似题：**下列不属于 Java Web 开发框架的是（　）。A. Spring MVC　B. Hibernate　C. Vue　D. MyBatis —— **答案：C**。Vue 是前端 JavaScript 框架，不是 Java Web 后端框架。
>
> **知识点：**
> - Java Web 常用框架：Spring、Spring MVC、Spring Boot、Struts2、MyBatis、Hibernate。
> - MVC 模式：Model（模型）、View（视图）、Controller（控制器）。


## 二、填空题（共 10 题）

41. 按照冯·诺依曼体系结构，传统的单核CPU由运算器和\_\_\_\_\_\_\_\_\_\_这两个主要部件组成。

> **答案：控制器**
>
> **解析：**按照冯·诺依曼体系结构，CPU（中央处理器）由运算器和控制器两大部件组成。运算器完成算术运算和逻辑运算，控制器负责取指令、分析指令并发出控制信号，协调各部件工作。
>
> **类似题：**冯·诺依曼计算机的五大基本部件是运算器、控制器、______、输入设备和输出设备。**答案：**存储器。
>
> **知识点：**
> - CPU = 运算器 + 控制器（现代 CPU 中还集成了寄存器组和高速缓存）。
> - 冯·诺依曼结构的核心思想：存储程序、程序控制。

42. Linux下的软件包可分为两种，分别是\_\_\_\_\_\_\_\_\_\_和二进制包。

> **答案：源码包**
>
> **解析：**Linux 下的软件包按提供形式可分为源码包和二进制包。源码包是未编译的源代码，需要在本地编译安装；二进制包是已编译好的程序，如 RPM 包、DEB 包，安装更快捷。
>
> **类似题：**Linux 中不需要编译、可直接安装的软件包是（　）。A. 源码包　B. tar.gz 源码包　C. RPM 二进制包　D. 以上都不是 —— **答案：C**。
>
> **知识点：**
> - 源码包：`.tar.gz`、`.tar.bz2` 等，安装步骤一般为 `./configure` → `make` → `make install`。
> - 二进制包：RPM（RedHat 系，`rpm`/`yum` 管理）、DEB（Debian 系，`dpkg`/`apt` 管理）。

43. 在shell中，\_\_\_\_\_\_\_\_\_\_是由竖杠（|）代表，用来连接多条命令。

> **答案：管道符（管道）**
>
> **解析：**竖杠 `|` 是管道符，用于连接多条命令，把前一个命令的标准输出作为后一个命令的标准输入。例如 `ls -l | more` 表示分页显示目录列表。
>
> **类似题：**命令 `cat /etc/passwd | wc -l` 的功能是（　）。A. 统计 /etc/passwd 的行数　B. 显示 /etc/passwd 内容　C. 修改 /etc/passwd　D. 删除 /etc/passwd —— **答案：A**。
>
> **知识点：**
> - 管道只传递标准输出，不传递标准错误；`2>&1 |` 可把错误也传入管道。
> - 管道两端的命令会作为独立的进程并发执行，常用于 `grep`、`wc`、`sort`、`more` 等的组合。

44. DNS服务使用的端口是\_\_\_\_\_\_\_\_\_\_。

> **答案：53**
>
> **解析：**DNS（Domain Name System，域名系统）服务默认使用 53 端口，其中 UDP 53 用于域名查询，TCP 53 用于区域传送等场景。
>
> **类似题：**将域名解析为 IP 地址的服务是（　）。A. DHCP　B. DNS　C. FTP　D. SMTP —— **答案：B**。
>
> **知识点：**
> - 常见服务端口：HTTP 80、HTTPS 443、FTP 21/20、SSH 22、Telnet 23、SMTP 25、POP3 110、DNS 53、DHCP 67/68。
> - 客户端 DNS 服务器地址配置在 `/etc/resolv.conf` 中。

45. 使用\_\_\_\_\_\_\_\_\_\_命令可以将用户身份临时改变为root。

> **答案：su**
>
> **解析：**`su`（switch user）用于切换用户身份，`su -` 或 `su - root` 可切换到 root 并加载其环境变量。使用普通用户执行 `su` 时需要输入 root 的密码。
>
> **类似题：**从普通用户切换到 root 用户并加载 root 的环境变量，应执行（　）。A. su root　B. su -　C. sudo -　D. root —— **答案：B**。
>
> **知识点：**
> - `su 用户名` 切换用户但保留当前环境；`su - 用户名` 以登录方式切换并加载目标用户环境。
> - `sudo 命令` 以其他用户（默认 root）身份执行单条命令，权限由 `/etc/sudoers` 配置。

46. 已知shell变量nu=aaa，现在希望nu的值是aaabbb，则应执行的命令nu=\_\_\_\_\_\_\_\_\_\_。

> **答案：`nu=${nu}bbb`**
>
> **解析：**Shell 中变量名与后续字符直接相连时会产生歧义（`$nubb` 会被当作变量 nubb），用花括号 `${nu}` 明确变量名的边界，再拼接 `bbb` 即可得到 aaabbb。
>
> **类似题：**已知 x=hello，要使变量 y 的值为 helloworld，正确的写法是（　）。A. y=$xworld　B. y=${x}world　C. y=$(x)world　D. y=$[x]world —— **答案：B**。
>
> **知识点：**
> - `${变量名}` 是变量引用的标准形式，可限定变量名边界，也支持 `${var:-默认值}` 等扩展。
> - 单引号内变量不展开，双引号内变量会展开。

47. 命令\_\_\_\_\_\_\_\_\_\_用来装载所有在/etc/fstab中定义的文件系统。

> **答案：`mount -a`**
>
> **解析：**`mount -a` 会读取 `/etc/fstab` 文件，并按其中定义的条目挂载所有（未挂载的）文件系统，常用于系统启动时自动挂载设备。
>
> **类似题：**系统开机自动挂载分区所依据的配置文件是（　）。A. /etc/mtab　B. /etc/fstab　C. /etc/mount.conf　D. /proc/mounts —— **答案：B**。
>
> **知识点：**
> - `/etc/fstab` 每行格式：设备、挂载点、文件系统类型、挂载选项、dump 备份标志、fsck 检查顺序。
> - 查看当前已挂载的文件系统可用 `mount` 或 `df -h`、`cat /proc/mounts`。

48. 通过DHCP获得信息的机器没有固定IP地址，用\_\_\_\_\_\_\_\_\_\_命令增加网络接口时，系统动态获得一个IP地址，用\_\_\_\_\_\_\_\_\_\_命令减少网络接口时，系统就释放一个IP地址。

> **答案：ifup；ifdown**
>
> **解析：**`ifup 网卡名` 启用网络接口并触发 DHCP 获取 IP 地址；`ifdown 网卡名` 停用接口并释放 IP 地址。二者通常读取 `/etc/sysconfig/network-scripts/ifcfg-网卡名` 配置。
>
> **类似题：**启用网卡 eth0 并动态获取 IP 地址的命令是（　）。A. ifup eth0　B. ifdown eth0　C. ifconfig eth0　D. ip eth0 —— **答案：A**。
>
> **知识点：**
> - 网卡配置文件：`/etc/sysconfig/network-scripts/ifcfg-eth0`，其中 `BOOTPROTO=dhcp` 表示动态获取，`static`/`none` 表示静态配置。
> - `ONBOOT=yes` 表示开机自动启用该网卡。

49. Vi打开文档后，想把10-15行中的第一个 `'abc'` 替换为 `'efg'`，应输入命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`:10,15s/abc/efg/`**
>
> **解析：**vi 的替换命令格式为 `:起始行,结束行s/原字符串/新字符串/标志`。这里对 10～15 行进行替换且只替换每行第一个匹配，所以命令末尾不加 `g`；若要替换每行所有匹配则加 `g`。
>
> **类似题：**在 vi 中把整个文件中的所有 old 替换为 new，应输入（　）。A. :s/old/new/　B. :%s/old/new/g　C. :1,$s/old/new/　D. :g/old/new —— **答案：B**。
>
> **知识点：**
> - `:s/old/new/` 替换当前行第一个；`:s/old/new/g` 替换当前行全部。
> - `:%s/old/new/g` 与 `:1,$s/old/new/g` 等价，表示全文替换。

50. 编译器GCC会在系统默认的路径中（如/usr/lib）寻找所需要的库文件，当使用了\_\_\_\_\_\_\_\_\_\_选项指定库文件路径后，会首先到指定的目录去寻找相关库文件。

> **答案：-L**
>
> **解析：**GCC 的 `-L 目录` 选项用于指定额外的库文件（.so/.a）搜索路径，链接时会先到指定目录查找库；`-l 库名` 用于指定要链接的库；`-I 目录` 用于指定头文件搜索路径。
>
> **类似题：**GCC 编译时指定头文件搜索目录应使用（　）。A. -L　B. -l　C. -I　D. -i —— **答案：C**。
>
> **知识点：**
> - `-I`（大写 i）头文件路径、`-L` 库文件路径、`-l`（小写 L）链接库名（如 `-lm` 链接数学库）。
> - 运行时找不到动态库可设置环境变量 `LD_LIBRARY_PATH`。


## 三、综合应用题（共 2 题）

**1. 在 `/tmp/source` 目录下有一个只读 `test.c` 文件如下:**
```c
#include <stdio.h>
int main(){
    printf("This is test.\n");
    printf("This is another test.\n");
    return 0;
}
```
用户当前在 `/` 目录下，进行如下操作：
(1) 在 `/tmp` 目录下创建一个名为 `target` 的目录，应输入命令：【51】\_\_\_\_\_\_\_\_\_\_;
(2) `cd /tmp/target`
(3) 输入命令 `cp /tmp/source/test.c`，将 `test.c` 文件复制到当前目录;
(4) 调用命令【52】\_\_\_\_\_\_\_\_\_\_，将文件改为可写形式;
(5) `vi test.c` 启动编辑;
(6) 使用【53】\_\_\_\_\_\_\_\_\_\_命令把文件中的字符串 `"This"` 全都替换为 `"That"`;
(7) 将光标移到第4行，输入命令【54】\_\_\_\_\_\_\_\_\_\_删除该行;
(8) 输入命令【55】\_\_\_\_\_\_\_\_\_\_保存并退出vi编辑器；
(9) 用户用scp命令把 `/tmp/target/test.c` 文件上传至IP地址为10.0.0.50的服务器的 `/tmp` 目录下，命令中需指定访问服务器的用户名为 `user1`，应输入：
   `[root@localhost ~]#` 【56】\_\_\_\_\_\_\_\_\_\_
(10) 上传成功后，用户使用 ssh 以 user1 身份登录服务器，应输入:
    `[root@localhost ~]#` 【57】\_\_\_\_\_\_\_\_\_\_
(11) 登录服务器后进入 `/tmp` 目录，调用 gcc 编译 `test.c`，指定输出文件名为 `mytest`，并要求产生调试信息，应输入:
    `[user1@10.0.0.50:/tmp]$` 【58】\_\_\_\_\_\_\_\_\_\_
(12) 编译成功后，输入命令【59】\_\_\_\_\_\_\_\_\_\_启动 GDB对 `mytest` 进行调试;
(13) 调试过程中，如需在程序第3行设置断点，可输入命令【60】\_\_\_\_\_\_\_\_\_\_


> **参考答案与解析：**
>
> - 【51】`mkdir /tmp/target` —— 在 /tmp 下创建目录 target。
> - 【52】`chmod +w test.c`（或 `chmod u+w test.c`、`chmod a+w test.c`）—— 原文件只读，需要增加写权限才能编辑。
> - 【53】`:1,$s/This/That/g`（或 `:%s/This/That/g`）—— 把全文所有 "This" 替换为 "That"。
> - 【54】`dd` —— 在命令模式下删除光标所在行（第 4 行）。
> - 【55】`:wq`（或 `:x`、`ZZ`）—— 保存并退出 vi。
> - 【56】`scp /tmp/target/test.c user1@10.0.0.50:/tmp/` —— 将本地文件上传到远程服务器的 /tmp 目录。
> - 【57】`ssh user1@10.0.0.50` —— 以 user1 身份登录远程服务器。
> - 【58】`gcc -g test.c -o mytest` —— `-g` 生成调试信息，`-o mytest` 指定输出文件名。
> - 【59】`gdb mytest` —— 启动 GDB 调试指定可执行文件。
> - 【60】`break 3`（或 `b 3`）—— 在第 3 行设置断点。
>
> **解析要点：**
> - `scp` 的语法为 `scp 本地文件 用户@主机:远程路径`，上传目录需加 `-r`。
> - GDB 必须在编译时加 `-g` 才能获得源码级调试信息。
> - vi 保存退出可用 `:wq`，强制退出用 `:q!`。
>
> **变式练习：**把本地目录 /tmp/target 整个上传到远程 /tmp 下，应执行（　）。A. scp /tmp/target user1@10.0.0.50:/tmp/　B. scp -r /tmp/target user1@10.0.0.50:/tmp/　C. scp -r user1@10.0.0.50:/tmp/ /tmp/target　D. cp -r /tmp/target /tmp/ —— **答案：B**。
>
> **知识点：**
> - SSH 远程操作三件套：`ssh` 登录、`scp` 安全复制、`sftp` 安全文件传输。
> - GCC 编译链：预处理（-E）→ 编译（-S）→ 汇编（-c）→ 链接（-o）。
> - GDB 常用命令：`break`、`run`、`next`、`step`、`continue`、`print`、`list`、`quit`。

**2. 写一个猜数字shell脚本，当用户输入的数字和预设数字（随机生成一个小于100的数字）一样时，直接退出，否则让用户一直输入，并且提示用户的数字比预设数字大或者小。答题要求：在横线处补充相应内容。**
```bash
【61】__________
m=`echo $RANDOM`
n1=$【62】__________
【63】__________
do
    【64】__________ "Please input a number: "n
    if [ $n == $n1 ]
    then
        【65】__________
    【66】__________
    then
        echo "bigger"
        continue
    【67】__________
        echo "smaller"
        continue
    【68】__________
【69】__________
【70】__________ "You are right."
```

> **参考答案与解析（空白已按 61～70 编号）：**
>
> - 【61】`#!/bin/bash` —— 脚本首行指定解释器（Shebang）。
> - 【62】`$[ $m % 100 ]`（或 `$(($m%100))`）—— 对 $RANDOM 取模 100，得到 0～99 的随机数。
> - 【63】`while true`（或 `while :`、`while [ 1 ]`）—— 构造无限循环，直到猜中才退出。
> - 【64】`read -p` —— 带提示信息读取用户输入到变量 n。
> - 【65】`break` —— 猜中时跳出循环。
> - 【66】`elif [ $n -gt $n1 ]` —— 判断输入比预设值大。
> - 【67】`else` —— 剩下的情况即输入比预设值小。
> - 【68】`fi` —— 结束 if 语句。
> - 【69】`done` —— 结束 while 循环。
> - 【70】`echo` —— 循环结束后输出提示信息。
>
> **解析要点：**
> - `$RANDOM` 每次返回 0～32767 的随机整数，`%100` 可限定为小于 100。
> - Shell 数值比较：`-gt`（大于）、`-lt`（小于）、`-eq`（等于）；字符串比较用 `=`、`!=`。
> - `read -p "提示" 变量` 可在一行内完成提示与读取。
>
> **变式练习：**在 Shell 中生成一个 1～10 之间的随机整数，正确的写法是（　）。A. `$RANDOM`　B. `$RANDOM % 10`　C. `$[ $RANDOM % 10 + 1 ]`　D. `$[ $RANDOM / 10 ]` —— **答案：C**。
>
> **知识点：**
> - Shell 算术运算：`$((表达式))`、`$[表达式]`、`let`、`expr`。
> - 循环控制：`break` 跳出循环、`continue` 结束本次循环进入下一次。
> - 条件判断：`if...then...elif...then...else...fi`；`case...esac` 用于多分支。


---


# 全国计算机等级考试Linux应用与开发技术试题（二）

## 一、单项选择题（共 40 题）

**1. 操作系统是一种(  )。**
<table>
  <tr><td width="50%">A. 应用软件</td><td>B. 系统软件</td></tr>
  <tr><td>C. 通用软件</td><td>D. 工具软件</td></tr>
</table>


> **答案：B**
>
> **解析：**操作系统是管理计算机硬件与软件资源、并为用户提供交互接口的程序集合，属于最基本的系统软件；应用软件、通用软件、工具软件都是建立在操作系统之上的软件。
>
> **类似题：**下列软件中属于系统软件的是（　）。A. WPS　B. Linux 操作系统　C. QQ　D. Photoshop —— **答案：B**。
>
> **知识点：**
> - 软件分为系统软件（操作系统、编译程序、数据库管理系统等）和应用软件。
> - 操作系统是裸机之上的第一层软件，其他软件都运行在操作系统之上。

**2. 默认情况下，管理员创建了一个用户，就会在(  )目录下创建一个用户主目录。**
<table>
  <tr><td width="50%">A. /usr</td><td>B. /root</td></tr>
  <tr><td>C. /home</td><td>D. /etc</td></tr>
</table>


> **答案：C**
>
> **解析：**默认情况下，`useradd` 创建用户时会在 `/home` 下创建同名主目录（如 `/home/user1`）；`/root` 是 root 用户的主目录，`/usr`、`/etc` 不是用户主目录。
>
> **类似题：**新建用户 tom 后，其默认主目录是（　）。A. /root/tom　B. /usr/tom　C. /home/tom　D. /etc/tom —— **答案：C**。
>
> **知识点：**
> - 主目录初始内容来自 `/etc/skel`，可用 `useradd -m` 强制创建、`-d` 指定路径。
> - root 的主目录由 `/etc/passwd` 中 UID 0 记录的第六字段决定，通常为 `/root`。

**3. 下面命令的作用是：`PS1="[\u\w]\$"; export PS1` (  )**
<table>
  <tr><td width="50%">A. 改变错误信息提示</td><td>B. 改变命令提示符</td></tr>
  <tr><td>C. 改变一些终端参数</td><td>D. 改变辅助命令提示符</td></tr>
</table>


> **答案：B**
>
> **解析：**`PS1` 是 bash 的主命令提示符环境变量，`\u` 表示用户名、`\w` 表示当前工作目录、`\$` 表示提示符（root 为 #，普通用户为 $）。修改并导出 `PS1` 即改变命令提示符。
>
> **类似题：**bash 中用于设置辅助（续行）提示符的变量是（　）。A. PS1　B. PS2　C. PATH　D. HOME —— **答案：B**。
>
> **知识点：**
> - `PS1` 主提示符、`PS2` 续行提示符、`PS3` select 提示符、`PS4` 调试提示符。
> - 提示符转义：`\u` 用户名、`\h` 主机名、`\w` 完整路径、`\W` 当前目录名、`\t` 时间、`\$` 权限符。

**4. 查询已安装软件包dhcp所包含文件信息的命令是(  )**
<table>
  <tr><td width="50%">A. rpm -qa dhcp</td><td>B. rpm -qf dhcp</td></tr>
  <tr><td>C. rpm -qp dhcp</td><td>D. rpm -ql dhcp</td></tr>
</table>


> **答案：D**
>
> **解析：**`rpm -ql 包名` 列出已安装软件包中包含的所有文件；`-qa` 列出所有已安装的包，`-qf` 查询文件属于哪个包，`-qp` 查询未安装的 rpm 包文件信息。
>
> **类似题：**查询文件 /bin/ls 属于哪个 RPM 包，应使用命令（　）。A. rpm -ql coreutils　B. rpm -qf /bin/ls　C. rpm -qa /bin/ls　D. rpm -qi coreutils —— **答案：B**。
>
> **知识点：**
> - RPM 查询选项：`-q` 查询、`-a` 所有包、`-f` 文件所属包、`-l` 包内文件、`-i` 包信息、`-p` 针对 rpm 文件。
> - 安装 `-ivh`、升级 `-Uvh`、卸载 `-e`、校验 `-V`。

**5. 当字符串用单引号括起来时，Shell将(  )**
<table>
  <tr><td width="50%">A. 解释引号内的特殊字符</td><td>B. 执行引号中的命令</td></tr>
  <tr><td>C. 不解释引号内的特殊字符</td><td>D. 结束进程</td></tr>
</table>


> **答案：C**
>
> **解析：**单引号是“强引用”，引号内的所有字符（包括 `$`、`\`、反引号等）都作为普通字符处理，Shell 不会解释其中的特殊字符；双引号是“弱引用”，`$`、反引号等仍会展开。
>
> **类似题：**设 x=abc，命令 `echo '$x'` 的输出是（　）。A. abc　B. 空　C. $x　D. 'abc' —— **答案：C**。
>
> **知识点：**
> - 单引号：完全屏蔽特殊字符；双引号：保留 `$`、`` ` ``、`\` 的特殊含义。
> - 反引号（`` ` ``）或 `$()` 用于命令替换；`\` 用于转义单个字符。

**6. 对所有用户的变量设置，应当放在哪个文件下？(  )**
<table>
  <tr><td width="50%">A. /etc/bashrc</td><td>B. /etc/profile</td></tr>
  <tr><td>C. ~/.bash_profile</td><td>D. /etc/skel/.bashrc</td></tr>
</table>


> **答案：B**
>
> **解析：**`/etc/profile` 是系统级全局配置文件，对所有用户生效；`/etc/bashrc` 主要定义系统级函数和别名；`~/.bash_profile`、`~/.bashrc` 只对当前用户生效；`/etc/skel/.bashrc` 是新建用户的模板文件。
>
> **类似题：**只对当前用户生效的 bash 环境变量通常配置在（　）。A. /etc/profile　B. ~/.bash_profile　C. /etc/bashrc　D. /etc/environment —— **答案：B**。
>
> **知识点：**
> - 全局：`/etc/profile`、`/etc/profile.d/*.sh`、`/etc/bashrc`。
> - 用户级：`~/.bash_profile`、`~/.bash_login`、`~/.profile`、`~/.bashrc`。
> - 修改后可用 `source 文件名` 或 `. 文件名` 立即生效。

**7. 下列哪个文件定义本地主机名和IP地址间的转换？(  )**
<table>
  <tr><td width="50%">A. /etc/ip.conf</td><td>B. /etc/resolv.conf</td></tr>
  <tr><td>C. /etc/hosts</td><td>D. /etc/sysconfig/dns</td></tr>
</table>


> **答案：C**
>
> **解析：**`/etc/hosts` 是本地静态域名解析文件，格式为“IP 地址 主机名”，用于定义主机名与 IP 的映射；`/etc/resolv.conf` 配置 DNS 服务器地址；其余文件不存在或用途不符。
>
> **类似题：**配置 DNS 服务器地址的文件是（　）。A. /etc/hosts　B. /etc/resolv.conf　C. /etc/hostname　D. /etc/nsswitch.conf —— **答案：B**。
>
> **知识点：**
> - 域名解析顺序由 `/etc/nsswitch.conf` 中的 `hosts:` 行决定，通常先查 hosts 文件再查 DNS。
> - `/etc/hosts` 优先级高，常用于本地开发测试和屏蔽特定域名。

**8. 在Linux中，一般用(  )命令来查看网络接口的状态。**
<table>
  <tr><td width="50%">A. ping</td><td>B. ipconfig</td></tr>
  <tr><td>C. winipcfg</td><td>D. ifconfig</td></tr>
</table>


> **答案：D**
>
> **解析：**`ifconfig` 用于查看和配置网络接口状态；`ping` 测试连通性；`ipconfig` 是 Windows 命令；`winipcfg` 是旧版 Windows 工具。
>
> **类似题：**下列命令中可以查看和临时配置网卡 IP 地址的是（　）。A. ping　B. netstat　C. ifconfig　D. route —— **答案：C**。
>
> **知识点：**
> - `ifconfig eth0 192.168.1.10 netmask 255.255.255.0` 可临时配置 IP（重启失效）。
> - 新版系统推荐使用 `ip addr add 192.168.1.10/24 dev eth0`。

**9. 在接口配置文件中，下列哪一行指定了应该使用DHCP配置接口？(  )**
<table>
  <tr><td width="50%">A. DHCP=yes</td><td>B. IPADDR=DHCP</td></tr>
  <tr><td>C. ONBOOT=DHCP</td><td>D. BOOTPROTO=DHCP</td></tr>
</table>


> **答案：D**
>
> **解析：**网卡配置文件 `ifcfg-eth0` 中，`BOOTPROTO=dhcp` 表示通过 DHCP 动态获取 IP；`BOOTPROTO=static`（或 none）表示使用静态 IP，需配合 `IPADDR`、`NETMASK`、`GATEWAY` 等配置。
>
> **类似题：**网卡配置文件中表示开机自动启用网卡的是（　）。A. BOOTPROTO=dhcp　B. ONBOOT=yes　C. IPADDR=...　D. DEVICE=eth0 —— **答案：B**。
>
> **知识点：**
> - 常用配置项：`DEVICE`、`ONBOOT`、`BOOTPROTO`、`IPADDR`、`NETMASK`/`PREFIX`、`GATEWAY`、`DNS1`。
> - 修改后重启网络：`service network restart` 或 `systemctl restart network`。

**10. Linux的日志文件通常保存在(  )**
<table>
  <tr><td width="50%">A. /var/log</td><td>B. /usr/adm</td></tr>
  <tr><td>C. /etc/</td><td>D. /var/run</td></tr>
</table>


> **答案：A**
>
> **解析：**Linux 系统日志默认存放在 `/var/log` 目录；`/usr/adm` 是部分 UNIX 系统的日志目录，`/etc` 存放配置文件，`/var/run` 存放运行时的 PID 等文件。
>
> **类似题：**记录系统内核启动信息的日志文件通常是（　）。A. /var/log/dmesg　B. /etc/passwd　C. /var/log/lastlog　D. /etc/mtab —— **答案：A**。
>
> **知识点：**
> - `dmesg` 命令可直接查看内核环形缓冲区信息（开机硬件检测）。
> - 日志服务：`rsyslogd` 负责收集，`logrotate` 负责轮转切割。

**11. Linux系统中常用的日志分析工具(  )，能对原始的日志文件进行解析并转换成结构化的文档，也能根据使用情况需求来定制报告。**
<table>
  <tr><td width="50%">A. logrotate</td><td>B. logwatch</td></tr>
  <tr><td>C. tail</td><td>D. lastlog</td></tr>
</table>


> **答案：B**
>
> **解析：**`logwatch` 是日志分析工具，能解析原始日志并生成结构化报告，还可按需定制报告内容；`logrotate` 负责日志轮转；`tail` 查看文件尾部；`lastlog` 显示各用户最近一次登录信息。
>
> **类似题：**用于日志文件轮转、压缩和删除的工具是（　）。A. logwatch　B. logrotate　C. rsyslog　D. journalctl —— **答案：B**。
>
> **知识点：**
> - `logrotate` 配置文件为 `/etc/logrotate.conf` 及 `/etc/logrotate.d/`，可按大小或时间切割日志。
> - `journalctl` 用于查看 systemd 日志。

**12. 用于自动补全功能，输入命令或文件的前一个或几个字母之后按什么键？(  )**
<table>
  <tr><td width="50%">A. ctrl</td><td>B. tab</td></tr>
  <tr><td>C. alt</td><td>D. esc</td></tr>
</table>


> **答案：B**
>
> **解析：**Tab 键用于命令和文件路径的自动补全，连按两次 Tab 可列出所有候选；Ctrl、Alt、Esc 均不具备补全功能。
>
> **类似题：**在 bash 中查看历史命令并支持上下翻找，应使用（　）。A. Tab 键　B. 上下方向键　C. Ctrl+C　D. Ctrl+Z —— **答案：B**。
>
> **知识点：**
> - 常用快捷键：`Tab` 补全、`Ctrl+C` 终止当前命令、`Ctrl+D` 结束输入/退出、`Ctrl+L` 清屏、`Ctrl+R` 反向搜索历史命令。
> - 历史命令保存在 `~/.bash_history`，`history` 命令可查看。

**13. 如果执行命令 `chmod 746 file`，那么该文件的权限是(  )**
<table>
  <tr><td width="50%">A. rwxr--rw-</td><td>B. rw-r--r--</td></tr>
  <tr><td>C. --xr--rwx</td><td>D. rwxr--r--</td></tr>
</table>


> **答案：A**
>
> **解析：**746 的含义：所有者 7=rwx，同组用户 4=r--，其他用户 6=rw-，即 `rwxr--rw-`。
>
> **类似题：**权限 `rw-rw-r--` 对应的八进制数是（　）。A. 644　B. 664　C. 646　D. 466 —— **答案：B**。
>
> **知识点：**
> - 权限换算：r=4、w=2、x=1，三位分别对应所有者、组、其他用户。
> - `chmod` 数字法直接设置权限；符号法如 `chmod g+w file` 增量修改。

**14. 设用户当前所在目录为 `/tmp`，以下(  )指令可以在当前目录下创建目录 `son/grandson`，其中子目录 `son/` 及其下级目录 `grandson/` 当前都不存在。**
<table>
  <tr><td width="50%">A. mkdir son/grandson</td><td>B. rmdir -p son/grandson</td></tr>
  <tr><td>C. rmdir -r son/grandson</td><td>D. mkdir -p son/grandson</td></tr>
</table>


> **答案：D**
>
> **解析：**`mkdir -p` 可递归创建多级目录，父目录不存在时自动创建；`rmdir` 只能删除空目录；`-r` 不是 mkdir 的选项。
>
> **类似题：**要一次性创建目录 /tmp/a/b/c，应使用命令（　）。A. mkdir /tmp/a/b/c　B. mkdir -p /tmp/a/b/c　C. rmdir -p /tmp/a/b/c　D. touch /tmp/a/b/c —— **答案：B**。
>
> **知识点：**
> - `mkdir -p` 递归创建；`mkdir -m 755 dir` 创建时指定权限。
> - `rmdir` 仅删除空目录，删除非空目录用 `rm -r`。

**15. 下面关于文件系统i节点描述错误的是(  )**
<table>
  <tr><td width="50%">A. i节点和文件一一对应</td><td>B. i节点能描述文件占用的块数</td></tr>
  <tr><td>C. i节点描述了文件大小和指向数据块的指针</td><td>D. 通过i节点实现文件的逻辑结构和物理结构的转换</td></tr>
</table>


> **答案：A**
>
> **解析：**inode（索引节点）保存文件的元信息（权限、大小、时间戳、数据块指针等），但多个文件名（硬链接）可以指向同一个 inode，因此“i 节点和文件一一对应”的说法错误。B、C、D 均正确。
>
> **类似题：**下列关于 inode 的说法正确的是（　）。A. inode 中存放文件名　B. inode 中存放文件数据内容　C. inode 中存放文件大小和权限等信息　D. 每个文件一定有多个 inode —— **答案：C**。
>
> **知识点：**
> - 文件名存放在目录项中，目录项通过 inode 号找到 inode，inode 再指向数据块。
> - 硬链接共享 inode，软链接有自己的 inode，内容是目标路径。

**16. 把当前目录下的文件file1复制为file2，正确的命令是(  )**
<table>
  <tr><td width="50%">A. copy file1 file2</td><td>B. cp file1 | file2</td></tr>
  <tr><td>C. cat file2 file1</td><td>D. cat file1 > file2</td></tr>
</table>


> **答案：D**
>
> **解析：**选项中没有 `cp`，但 `cat file1 > file2` 能把 file1 的内容输出并重定向到新文件 file2，效果上完成了复制；`copy` 是 Windows 命令，`cp file1 | file2` 语法错误，`cat file2 file1` 只是显示内容。
>
> **类似题：**复制文件 file1 为 file2 的正确命令是（　）。A. copy file1 file2　B. cp file1 file2　C. mv file1 file2　D. cat file1 file2 —— **答案：B**。
>
> **知识点：**
> - `cp 源 目标` 复制文件，`cp -r` 递归复制目录，`cp -p` 保留属性。
> - `cat file1 > file2` 利用输出重定向也可生成副本，但会清空目标原内容。

**17. 了解在当前目录下还有多大空间，可执行命令(  )**
<table>
  <tr><td width="50%">A. df</td><td>B. du /</td></tr>
  <tr><td>C. du .</td><td>D. df .</td></tr>
</table>


> **答案：C**
>
> **解析：**`du .` 统计当前目录（含子目录）占用的磁盘空间；`df` 查看的是各文件系统的整体空间使用情况；`du /` 统计根目录下所有文件占用。
>
> **类似题：**查看当前目录总占用空间并以易读单位显示的命令是（　）。A. df -h　B. du -sh　C. ls -lh　D. free -h —— **答案：B**。
>
> **知识点：**
> - `du -s` 只显示汇总，`-h` 人类可读，`-a` 包含文件，`-c` 最后加总计。
> - `df` 与 `du` 的差异：df 从文件系统超级块统计，du 逐个文件累加。

**18. Linux中存放加密用户账户信息的文件是(  )**
<table>
  <tr><td width="50%">A. /etc/passwd</td><td>B. /etc/shadow</td></tr>
  <tr><td>C. /etc/group</td><td>D. /etc/security</td></tr>
</table>


> **答案：B**
>
> **解析：**`/etc/passwd` 存放用户基本信息（用户名、UID、GID、主目录、Shell 等），密码字段用 x 占位；加密后的密码散列值存放在 `/etc/shadow` 中，只有 root 可读。
>
> **类似题：**存放用户组基本信息的文件是（　）。A. /etc/shadow　B. /etc/group　C. /etc/gshadow　D. /etc/passwd —— **答案：B**。
>
> **知识点：**
> - `/etc/passwd` 每行 7 个字段，密码字段为 x。
> - `/etc/shadow` 每行 9 个字段，包含加密口令、密码有效期等；`/etc/gshadow` 存组密码。

**19. 下列哪项是Linux系统中以太网网络接口？(  )**
<table>
  <tr><td width="50%">A. eth0</td><td>B. eth2</td></tr>
  <tr><td>C. net0</td><td>D. lo</td></tr>
</table>


> **答案：A 或 B（本题不严谨，题库参考答案为 B）**
>
> **解析：**Linux 中以太网接口通常命名为 `eth0`、`eth1`、`eth2`……，因此 A（eth0）和 B（eth2）都是合法的以太网接口名；`net0` 不是标准命名，`lo` 是本地回环接口而非以太网接口。本题因选项设置不严谨而存在争议，考试题库给出的答案为 B。
>
> **类似题：**下列哪项不是 Linux 系统的网络接口名（　）。A. eth0　B. lo　C. net0　D. ens33 —— **答案：C**。
>
> **知识点：**
> - 传统命名：`eth0`、`eth1`；可预测命名：`ens33`、`enp0s3` 等。
> - `lo` 是回环接口，地址固定为 127.0.0.1，用于本机通信。

**20. 执行以下命令：**
```bash
[student@localhost ~]$ ls
file1
[student@localhost ~]$ ln -s file1 file2
[student@localhost ~]$ rm file1
```
**下列描述正确的是(  )**
<table>
  <tr><td width="50%">A. file2也被随之删除</td><td>B. file2仍存在，但属于无效文件</td></tr>
  <tr><td>C. 因为file2未被删除，所以file1会被系统自动重新建立</td><td>D. file2会随着file1的删除而被系统自动删除</td></tr>
</table>


> **答案：B**
>
> **解析：**`ln -s file1 file2` 创建的是软链接（符号链接），file2 指向 file1。删除源文件 file1 后，file2 本身仍然存在，但失去了目标，成为“死链接/无效文件”，系统不会自动删除它，也不会自动重建 file1。
>
> **类似题：**`ln file1 file2`（不带 -s）创建硬链接后删除 file1，则（　）。A. file2 失效　B. file2 仍可正常访问原数据　C. 系统自动重建 file1　D. file2 也被删除 —— **答案：B**。硬链接与被链接文件共享 inode，删除其中一个名字不影响数据访问。
>
> **知识点：**
> - 硬链接：共享 inode，不能跨文件系统、不能链接目录；删除任一名字，只要链接数为正数据仍存在。
> - 软链接：独立 inode，可跨文件系统、可链接目录；源文件删除后链接失效。

**21. 设用户所使用计算机系统上有两块IDE硬盘，Linux系统位于第一块硬盘上，查询第二块硬盘分区情况的命令是(  )**
<table>
  <tr><td width="50%">A. fdisk -l /dev/hda1</td><td>B. fdisk -l /dev/hdb2</td></tr>
  <tr><td>C. fdisk -l /dev/hdb</td><td>D. fdisk -l /dev/hda</td></tr>
</table>


> **答案：C**
>
> **解析：**第一块 IDE 硬盘为 `/dev/hda`，第二块为 `/dev/hdb`。查看磁盘分区情况使用 `fdisk -l 设备名`，因此查询第二块硬盘应执行 `fdisk -l /dev/hdb`。
>
> **类似题：**查看第一块 SATA 硬盘分区情况的命令是（　）。A. fdisk -l /dev/sda1　B. fdisk -l /dev/sda　C. fdisk /dev/sdb　D. fdisk -l /dev/hda —— **答案：B**。
>
> **知识点：**
> - `fdisk -l` 列出所有磁盘分区表；`fdisk /dev/sda` 进入交互分区界面（修改后需 `w` 写入）。
> - `parted`、`gdisk` 支持 GPT 和大容量磁盘。

**22. 用 `ls -al` 命令列出下面的文件列表，(  )是符号连接文件。**
<table>
  <tr><td width="50%">A. -rw-rw-rw- 2 hel users 56 Sep 09 11:05 hello</td><td>B. -rwxrwxrwx 2 hel users 56 Sep 09 11:05 goodbey</td></tr>
  <tr><td>C. drwxr--r-- 1 hel users 1024 Sep 10 08:10 zhang</td><td>D. lrwxr--r-- 1 hel users 2024 Sep 12 08:12 cheng</td></tr>
</table>


> **答案：D**
>
> **解析：**`ls -l` 输出行首字符为 `l` 的是符号链接文件。A、B 以 `-` 开头是普通文件，C 以 `d` 开头是目录，D 以 `l` 开头且文件名后通常跟 `->` 指向目标，是符号链接。
>
> **类似题：**`ls -l` 结果中以 `c` 开头的文件是（　）。A. 普通文件　B. 目录　C. 字符设备文件　D. 块设备文件 —— **答案：C**。
>
> **知识点：**
> - 文件类型标识：`-` 普通、`d` 目录、`l` 链接、`b` 块设备、`c` 字符设备、`p` 管道、`s` 套接字。
> - `file 文件名` 也可判断文件类型。

**23. 以第三个字段对/etc/passwd的内容排序的命令是(  )**
<table>
  <tr><td width="50%">A. sort -t : -k 3 -n /etc/passwd</td><td>B. sort -k 3 -n /etc/passwd</td></tr>
  <tr><td>C. sort -t : -c 3 -n /etc/passwd</td><td>D. sort -c 3 -n /etc/passwd</td></tr>
</table>


> **答案：A**
>
> **解析：**`/etc/passwd` 以冒号分隔字段，第三个字段是 UID。`sort -t : -k 3 -n` 表示以冒号为分隔符、按第 3 个字段进行数值排序；`-c` 不是排序字段选项，缺少 `-t :` 无法正确按字段切分。
>
> **类似题：**按第 2 个字段（以冒号分隔）对文件 data 排序的命令是（　）。A. sort -t : -k 2 data　B. sort -k : 2 data　C. sort -c 2 data　D. sort -n -t : -k 2 data —— **答案：A**（数值排序则选 D）。
>
> **知识点：**
> - `sort` 常用选项：`-n` 数值、`-r` 逆序、`-t` 指定分隔符、`-k` 指定字段、`-u` 去重。
> - 默认按整行、字典序升序排序。

**24. 对于给定的文件file，统计其中所有包含字符串“edu”的行数的一条命令是(  )**
<table>
  <tr><td width="50%">A. grep "edu" file | wc -l</td><td>B. grep "edu" file | wc -c</td></tr>
  <tr><td>C. find "edu" file | wc -l</td><td>D. find "edu" file | wc -c</td></tr>
</table>


> **答案：A**
>
> **解析：**`grep "edu" file` 筛选出包含 edu 的行，管道传给 `wc -l` 统计行数；`wc -c` 统计字节数，`find` 用于查找文件而不是搜索文件内容。
>
> **类似题：**统计文件 data 中空行的行数，正确的命令是（　）。A. grep -c "^$" data　B. grep -n "" data　C. wc -c data　D. grep "^$" data | wc -c —— **答案：A**。
>
> **知识点：**
> - `wc` 选项：`-l` 行数、`-w` 单词数、`-c` 字节数、`-m` 字符数。
> - `grep -c` 直接输出匹配行数；`grep -v` 反向匹配；`grep -i` 忽略大小写。

**25. 对于用户和用户组，一个用户(  )**
<table>
  <tr><td width="50%">A. 必须只属于一个组</td><td>B. 必须属于多个组</td></tr>
  <tr><td>C. 可以属于一个组或多个组</td><td>D. 可以不属于任何组</td></tr>
</table>


> **答案：C**
>
> **解析：**Linux 中每个用户必须属于一个主组（创建用户时自动创建同名主组），同时可以被加入零个或多个附加组，因此“可以属于一个组或多个组”正确。
>
> **类似题：**将用户 user1 加入附加组 dev 的命令是（　）。A. usermod -g dev user1　B. usermod -G dev user1　C. groupmod -a user1 dev　D. useradd -G dev —— **答案：B**（`-G` 指定附加组，`-g` 指定主组）。
>
> **知识点：**
> - 用户信息：`/etc/passwd`；组信息：`/etc/group`；组密码：`/etc/gshadow`。
> - 查看用户所属组：`groups 用户名` 或 `id 用户名`。

**26. 系统在启动时，通过访问(  )文件中的信息来自动挂载（mount）某些硬件设备。**
<table>
  <tr><td width="50%">A. /etc/hosts</td><td>B. /etc/inittab</td></tr>
  <tr><td>C. /etc/fstab</td><td>D. /etc/rc.d/init.d</td></tr>
</table>


> **答案：C**
>
> **解析：**系统启动时读取 `/etc/fstab` 文件中的条目，自动挂载其中定义的文件系统/硬件设备；`/etc/hosts` 是主机名解析文件，`/etc/inittab` 定义运行级别（旧版），`/etc/rc.d/init.d` 存放服务脚本。
>
> **类似题：**`/etc/fstab` 中每行通常不包含的字段是（　）。A. 设备名　B. 挂载点　C. 文件系统类型　D. 进程号 —— **答案：D**。
>
> **知识点：**
> - fstab 字段顺序：设备、挂载点、类型、选项、dump、fsck 顺序。
> - 手工挂载：`mount -a` 按 fstab 全部挂载；`mount 设备 挂载点` 单次挂载。

**27. 以下关于parted分区工具叙述正确的是(  )**
<table>
  <tr><td width="50%">A. parted不能对高于2TB的硬盘分区</td><td>B. parted不能移动分区</td></tr>
  <tr><td>C. parted不能复制分区</td><td>D. parted对分区的任何改变会立即被写到硬盘中</td></tr>
</table>


> **答案：D**
>
> **解析：**parted 支持 GPT 分区表，可处理大于 2TB 的硬盘，也支持移动和复制分区，因此 A、B、C 均错误。parted 的特点是对分区的修改会立即写入硬盘生效，无需像 fdisk 那样执行保存命令。
>
> **类似题：**下列工具中支持大于 2TB 磁盘分区的是（　）。A. fdisk　B. cfdisk　C. parted　D. sfdisk —— **答案：C**（gdisk 也支持）。
>
> **知识点：**
> - MBR 分区表最大支持 2TB，最多 4 个主分区；GPT 支持更大容量和更多分区。
> - `parted /dev/sdb print` 查看分区，`mklabel gpt` 建立 GPT 分区表。

**28. 在vi编辑器中，输入命令“5G”表示(  )**
<table>
  <tr><td width="50%">A. 将光标移到第5行的行尾</td><td>B. 将光标移到第5行的行首</td></tr>
  <tr><td>C. 将光标移到本行的第五个字符前</td><td>D. 将光标移到本行的第五个字符后</td></tr>
</table>


> **答案：B**
>
> **解析：**vi 命令模式下 `G` 移到文件最后一行，`nG`（如 `5G`）表示移动到第 n 行的行首（等价于 `:5`）。
>
> **类似题：**在 vi 中把光标移动到文件最后一行的命令是（　）。A. 1G　B. G　C. gg　D. 0G —— **答案：B**。
>
> **知识点：**
> - 行间跳转：`gg` 第一行、`G` 最后一行、`nG` 第 n 行、`:n` 第 n 行。
> - 行内跳转：`0` 行首、`^` 第一个非空字符、`$` 行尾。

**29. 如果用户想在每次对文件进行编辑时都收到反馈信息，可以输入(  )**
<table>
  <tr><td width="50%">A. set scroll=0</td><td>B. set shiftwidth=0</td></tr>
  <tr><td>C. set wrapmargin=0</td><td>D. set report=0</td></tr>
</table>


> **答案：D**
>
> **解析：**vi 的 `report` 选项设置反馈阈值，默认为 2；`:set report=0` 表示只要文件被修改（哪怕只改 1 行）都会在底部显示提示信息。`scroll` 控制翻屏行数，`shiftwidth` 控制自动缩进宽度，`wrapmargin` 控制自动换行。
>
> **类似题：**在 vi 中设置自动缩进宽度的命令是（　）。A. :set report=0　B. :set nu　C. :set sw=4　D. :set list —— **答案：C**。
>
> **知识点：**
> - `:set` 显示修改过的选项，`:set all` 显示全部选项，`:set option?` 查看某选项当前值。
> - `:set ic` 忽略大小写，`:set ai` 自动缩进，`:set ts=4` 制表位宽度。

**30. Emacs编辑器中将光标向前移动一屏的命令是(  )**
<table>
  <tr><td width="50%">A. &lt;Ctrl&gt;+v</td><td>B. &lt;Alt&gt;+v</td></tr>
  <tr><td>C. &lt;Ctrl&gt;+b</td><td>D. &lt;Alt&gt;+b</td></tr>
</table>


> **答案：A**
>
> **解析：**Emacs 中 `Ctrl+v` 向前移动一屏（向下翻页）；`Alt+v` 向后翻一屏；`Ctrl+b` 左移一个字符；`Alt+b` 左移一个单词。
>
> **类似题：**Emacs 中把光标向下移动一行的组合键是（　）。A. Ctrl+n　B. Ctrl+p　C. Ctrl+f　D. Ctrl+b —— **答案：A**。
>
> **知识点：**
> - 翻页：`C-v` 向前一屏、`M-v` 向后一屏。
> - 基本移动：`C-f`/`C-b` 左右字符、`C-n`/`C-p` 上下行、`C-a`/`C-e` 行首行尾。

**31. Emacs编辑器模式行的显示模式字段中 `**` 表示(  )**
<table>
  <tr><td width="50%">A. 缓存区未被编辑</td><td>B. 缓存区中的文本已经被修改</td></tr>
  <tr><td>C. 缓存区中的文本未被修改</td><td>D. 只读缓冲区中的文本已经被修改</td></tr>
</table>


> **答案：B**
>
> **解析：**Emacs 模式行状态字段中，`**` 表示当前缓冲区（文件）的内容已被修改且尚未保存；`--` 表示未修改，`%%` 表示只读。
>
> **类似题：**Emacs 模式行中 `--` 表示（　）。A. 缓冲区已被修改　B. 缓冲区未被修改　C. 只读缓冲区　D. 文件不存在 —— **答案：B**。
>
> **知识点：**
> - 状态符：`--` 未修改、`**` 已修改、`%%` 只读、`%*` 只读且已修改。
> - 保存用 `C-x C-s`，退出用 `C-x C-c`。

**32. 在Emacs编辑器中显示的信息为“`U:**- example.Txt All L3 [Text]`”，则光标处于第(  )行。**
<table>
  <tr><td width="50%">A. 3</td><td>B. 1</td></tr>
  <tr><td>C. 最后</td><td>D. 倒数第3</td></tr>
</table>


> **答案：A**
>
> **解析：**Emacs 模式行中 `L3` 表示当前光标位于第 3 行（Line 3）；`All` 表示全部内容可见，`[Text]` 表示当前处于 Text 主模式。
>
> **类似题：**Emacs 模式行显示 `U:**- example.Txt All L5 [Text]`，则光标位于（　）。A. 第 5 行　B. 第 1 行　C. 最后一行　D. 倒数第 5 行 —— **答案：A**。
>
> **知识点：**
> - 模式行结构：缓冲区状态、文件名、位置（All/百分比）、行号、主模式等。
> - 跳转行：`M-g g` 输入行号跳转；`M-<` 到文件开头，`M->` 到文件末尾。

**33. 以下协议中，为远程登录会话和其他网络服务提供安全性的协议是(  )**
<table>
  <tr><td width="50%">A. FTP</td><td>B. HTTP</td></tr>
  <tr><td>C. SSH</td><td>D. ICMP</td></tr>
</table>


> **答案：C**
>
> **解析：**SSH（Secure Shell）通过加密认证为远程登录会话和其他网络服务提供安全保障；FTP、HTTP 为明文协议，ICMP 是网络控制协议。
>
> **类似题：**下列远程登录方式中最安全的是（　）。A. telnet　B. rlogin　C. SSH　D. FTP —— **答案：C**。
>
> **知识点：**
> - SSH 默认端口 22，服务端进程为 sshd。
> - SSH 可实现远程登录、安全文件传输（scp/sftp）、端口转发和免密登录。

**34. 为实现SSH免密登录，需要用ssh-keygen命令生成一对密钥，生成的密钥文件存储在(  )目录下。**
<table>
  <tr><td width="50%">A. etc</td><td>B. .ssh</td></tr>
  <tr><td>C. bin</td><td>D. keys</td></tr>
</table>


> **答案：B**
>
> **解析：**`ssh-keygen` 默认在用户主目录下的 `.ssh` 目录中生成密钥对：`id_rsa`（私钥）和 `id_rsa.pub`（公钥）。
>
> **类似题：**免密登录时，需要放到服务器端 `~/.ssh/authorized_keys` 文件中的是（　）。A. 私钥　B. 公钥　C. 密码　D. 主机密钥 —— **答案：B**。
>
> **知识点：**
> - 生成密钥：`ssh-keygen -t rsa`；分发公钥：`ssh-copy-id user@host`。
> - 相关文件：`~/.ssh/id_rsa`（私钥）、`~/.ssh/id_rsa.pub`（公钥）、`~/.ssh/authorized_keys`（服务端授权列表）、`~/.ssh/known_hosts`（已连接主机指纹）。

**35. OpenSSH中实现客户端与服务器之间安全的文件传输的命令是(  )**
<table>
  <tr><td width="50%">A. ftp</td><td>B. ssh</td></tr>
  <tr><td>C. scp</td><td>D. cp</td></tr>
</table>


> **答案：C**
>
> **解析：**`scp`（Secure Copy）基于 SSH 实现客户端与服务器之间的加密文件传输；`ftp` 不加密，`ssh` 用于登录，`cp` 只能在本地复制。
>
> **类似题：**把本地文件 a.txt 上传到 10.0.0.1 的 /tmp 目录，正确的命令是（　）。A. scp a.txt root@10.0.0.1:/tmp/　B. cp a.txt root@10.0.0.1:/tmp/　C. ssh a.txt root@10.0.0.1:/tmp/　D. ftp a.txt root@10.0.0.1 —— **答案：A**。
>
> **知识点：**
> - `scp 本地文件 用户@主机:远程路径` 上传；`scp 用户@主机:远程文件 本地路径` 下载。
> - 传输目录需加 `-r` 递归选项。

**36. 在GDB调试程序下，若要列出当前断点处的源程序源代码应输入命令(  )**
<table>
  <tr><td width="50%">A. list</td><td>B. print</td></tr>
  <tr><td>C. show</td><td>D. disp</td></tr>
</table>


> **答案：A**
>
> **解析：**GDB 中 `list`（缩写 `l`）列出当前断点附近的源代码；`print` 查看变量值；`show` 查看 GDB 设置；`disp` 设置自动显示表达式。
>
> **类似题：**GDB 中查看函数调用栈的命令是（　）。A. list　B. bt　C. print　D. next —— **答案：B**。
>
> **知识点：**
> - `list` 默认每次显示 10 行，可用 `list 行号` 或 `list 函数名` 定位。
> - 相关：`where`/`bt` 查看调用栈，`frame n` 切换栈帧。

**37. 在使用GCC编译器的过程中，如果只生成目标文件而不进行连接，需要使用选项(  )**
<table>
  <tr><td width="50%">A. -S</td><td>B. -o</td></tr>
  <tr><td>C. -c</td><td>D. -E</td></tr>
</table>


> **答案：C**
>
> **解析：**GCC 的 `-c` 选项表示只编译源文件生成目标文件（.o），不进行链接；`-S` 生成汇编代码，`-E` 只做预处理，`-o` 指定输出文件名。
>
> **类似题：**GCC 中只进行预处理、不编译的命令选项是（　）。A. -c　B. -S　C. -E　D. -o —— **答案：C**。
>
> **知识点：**
> - 编译四阶段：预处理（-E，生成 .i）→ 编译（-S，生成 .s）→ 汇编（-c，生成 .o）→ 链接（生成可执行文件）。
> - `gcc -c a.c` 生成 a.o，可用于多文件分别编译后统一链接。

**38. 用GCC编译一个名为“HelloWorld.c”的C程序文件，编译生成执行文件名为“Hello”，则相应的编译命令是(  )**
<table>
  <tr><td width="50%">A. gcc -i HelloWorld.c Hello</td><td>B. gcc -o Hello HelloWorld.c</td></tr>
  <tr><td>C. gcc -o HelloWorld.c Hello</td><td>D. gcc -i Hello HelloWorld.c</td></tr>
</table>


> **答案：B**
>
> **解析：**`-o` 选项用于指定输出文件名，语法为 `gcc -o 输出文件 源文件`，即 `gcc -o Hello HelloWorld.c`。
>
> **类似题：**编译 test.c 并生成可执行文件 test，正确的命令是（　）。A. gcc test.c -o test　B. gcc -o test.c test　C. gcc test -o test.c　D. gcc test.c test —— **答案：A**（A 与 B 选项在顺序上等价，只要 `-o` 紧跟输出文件名即可）。
>
> **知识点：**
> - `-o` 后面必须是最终输出文件名，位置不固定但必须与文件名相邻。
> - 不指定 `-o` 时，默认输出 `a.out`。

**39. 安装JDK后，可以输入(  )命令，查询已经安装的JAVA版本信息。**
<table>
  <tr><td width="50%">A. java -server</td><td>B. jdk -version</td></tr>
  <tr><td>C. java -help</td><td>D. java -version</td></tr>
</table>


> **答案：D**
>
> **解析：**`java -version` 显示当前 Java 运行环境的版本信息；`java -help` 显示帮助；`jdk -version` 不是命令；`java -server` 是选择服务器模式运行，不显示版本。
>
> **类似题：**查看 Java 编译器版本信息的命令是（　）。A. java -version　B. javac -version　C. java -help　D. jre -version —— **答案：B**。
>
> **知识点：**
> - JDK = JRE + 开发工具；`java` 运行字节码，`javac` 编译源文件。
> - Java 环境变量：`JAVA_HOME`、`PATH`、`CLASSPATH`，通常配置在 `/etc/profile` 或 `~/.bashrc`。

**40. 下列哪一个不是Web应用开发框架？(  )**
<table>
  <tr><td width="50%">A. Spring</td><td>B. Unity</td></tr>
  <tr><td>C. Struts2</td><td>D. MyBatis</td></tr>
</table>

> **答案：B**
>
> **解析：**Spring、Struts2、MyBatis 都是 Java Web 开发框架；Unity 是跨平台游戏开发引擎，不属于 Web 框架。
>
> **类似题：**MVC 模式中的三个核心部件是（　）。A. 模型、视图、控制器　B. 模型、视图、数据库　C. 页面、脚本、服务器　D. 输入、处理、输出 —— **答案：A**。
>
> **知识点：**
> - MVC：Model 处理数据和业务，View 负责展示，Controller 接收请求并调度。
> - 常见 Java Web 框架：Spring、Spring MVC、Spring Boot、Struts2、MyBatis、Hibernate。


## 二、填空题（共 10 题）

41. 从资源管理的角度来看，操作系统具备五大基本功能：处理器管理、\_\_\_\_\_\_\_\_\_\_、\_\_\_\_\_\_\_\_\_\_、文件管理和作业管理。

> **答案：存储管理；设备管理**（顺序可互换）
>
> **解析：**从资源管理角度看，操作系统具备五大基本功能：处理器管理、存储管理、设备管理、文件管理和作业管理。题目已给出处理器管理、文件管理和作业管理，缺存储管理和设备管理。
>
> **类似题：**操作系统的五大基本功能中，负责为进程分配内存的是（　）。A. 处理器管理　B. 存储管理　C. 设备管理　D. 文件管理 —— **答案：B**。
>
> **知识点：**
> - 处理器管理：进程调度与分配 CPU；存储管理：内存分配、回收与虚拟存储。
> - 设备管理：设备驱动与分配；文件管理：文件组织、目录与存取控制；作业管理：用户作业的提交与调度。

42. 在Linux操作系统中，所有被操作系统管理的资源，例如网络接口卡、磁盘驱动器、打印机、输入输出设备、普通文件或者目录都被看作是一个\_\_\_\_\_\_\_\_\_\_。

> **答案：文件**
>
> **解析：**Linux 遵循“一切皆文件”的设计哲学，把网络接口卡、磁盘、打印机等硬件设备以及普通文件、目录都抽象为文件，通过统一的文件接口（open/read/write/close）进行访问。
>
> **类似题：**Linux 中设备文件通常存放在（　）目录下。A. /dev　B. /etc　C. /proc　D. /sys —— **答案：A**。
>
> **知识点：**
> - 设备文件分为块设备文件（b）和字符设备文件（c）。
> - 这种抽象简化了设备管理，使应用程序可用统一方式操作不同硬件。

43. Linux文件系统中，点(`.`)表示当前目录，点点(`..`)表示\_\_\_\_\_\_\_\_\_\_目录。

> **答案：上一级目录（父目录）**
>
> **解析：**每个目录中都有两个特殊目录项：`.` 表示当前目录自身，`..` 表示上一级（父）目录。因此 `cd ..` 表示进入上一级目录。
>
> **类似题：**从 /home/user1 执行 `cd ..` 后所在目录是（　）。A. /home/user1　B. /home　C. /　D. /root —— **答案：B**。
>
> **知识点：**
> - 根目录的 `..` 仍指向根目录自身。
> - `ls -a` 可以显示包含 `.` 和 `..` 在内的所有目录项。

44. 安装Linux系统对硬盘分区时，必须有两种分区类型：Linux文件系统分区（根分区）和\_\_\_\_\_\_\_\_\_\_分区。

> **答案：交换分区（swap）**
>
> **解析：**安装 Linux 时必须划分根分区（/）和交换分区（swap）。swap 用作虚拟内存，当物理内存不足时，系统把暂时不用的内存页换出到 swap 分区。
>
> **类似题：**查看 swap 空间使用情况的命令是（　）。A. free　B. df　C. du　D. fdisk —— **答案：A**。
>
> **知识点：**
> - 常见分区方案：`/`（根）、`/boot`、`swap`、`/home` 等。
> - swap 大小经验值一般为物理内存的 1～2 倍（视内存大小和用途而定）。

45. 通常用\_\_\_\_\_\_\_\_\_\_命令来测试两台机器间网络的连通性。

> **答案：ping**
>
> **解析：**`ping` 命令向目标主机发送 ICMP 回显请求报文，根据是否收到回显应答判断两台机器之间的网络连通性，同时可观察延迟和丢包率。
>
> **类似题：**只发送 4 个 ICMP 包测试连通性的命令是（　）。A. ping -c 4 目标主机　B. ping -t 4 目标主机　C. ping -s 4 目标主机　D. ping -n 4 目标主机 —— **答案：A**。
>
> **知识点：**
> - `ping -c` 指定次数、`-i` 指定间隔、`-s` 指定数据包大小。
> - `ping` 不通不代表服务不可用，还需用 `telnet`/`nc` 测试端口。

46. 每个设备文件名由主设备号和从设备号描述。第二块IDE硬盘的设备名为 `hdb`，它上面的第三个主分区对应的文件名是\_\_\_\_\_\_\_\_\_\_。

> **答案：hdb3**
>
> **解析：**IDE 硬盘命名为 `/dev/hdX`：第二块盘为 hdb。主分区编号 1～4，第三个主分区即 hdb3。注意不要写成 hdb4 或 hdb5（逻辑分区才从 5 开始）。
>
> **类似题：**第三块 IDE 硬盘上的第二个主分区对应的文件名是（　）。A. hdc1　B. hdc2　C. hdc5　D. hdd2 —— **答案：B**。
>
> **知识点：**
> - 命名规律：设备名 + 分区号；主分区 1～4，逻辑分区从 5 开始。
> - 现代 SATA/SCSI 盘为 `/dev/sda`、`/dev/sdb`，分区命名规则相同。

47. 用一个命令行读取文件 `file` 第5~10行：\_\_\_\_\_\_\_\_\_\_。

> **答案：`head -10 file | tail -6`**（或 `sed -n '5,10p' file`、`awk 'NR>=5 && NR<=10' file`）
>
> **解析：**先用 `head -10 file` 取前 10 行，再通过管道交给 `tail -6` 取这 10 行中的后 6 行，即第 5～10 行。`sed -n '5,10p' file` 更直接。
>
> **类似题：**显示文件 file 第 3 行的命令是（　）。A. head -3 file | tail -1　B. head -3 file　C. tail -3 file　D. sed -n '3p' file　—— **答案：A 或 D 均可**。
>
> **知识点：**
> - `head -n` 显示前 n 行（默认 10），`tail -n` 显示后 n 行，`tail -f` 动态跟踪文件末尾。
> - `sed -n 'X,Yp'` 按行号范围输出；`awk 'NR==n'` 输出指定行。

48. 列出当前目录下以 `abc` 开头的文件名，应使用命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`ls abc*`**（或 `ls | grep "^abc"`）
>
> **解析：**通配符 `*` 可匹配任意多个字符，`ls abc*` 即列出当前目录下所有以 abc 开头的文件名。
>
> **类似题：**列出当前目录下所有以 .sh 结尾的文件，应使用（　）。A. ls *.sh　B. ls .sh*　C. ls ?sh　D. ls *sh* —— **答案：A**。
>
> **知识点：**
> - `?` 匹配单个字符，`*` 匹配任意多个字符，`[ ]` 匹配指定字符集合。
> - 通配符由 Shell 先展开，再传给命令。

49. 系统管理员以root账户登录，可以使用\_\_\_\_\_\_\_\_\_\_命令进入普通用户账号，而不必重新登录。

> **答案：`su - 用户名`**（或 `su 用户名`）
>
> **解析：**root 无需密码即可用 `su` 切换到普通用户；加 `-` 表示以登录方式切换并加载目标用户的环境变量，不加 `-` 则保留当前环境。
>
> **类似题：**从 root 切换到普通用户 tom 并加载其环境，应执行（　）。A. su tom　B. su - tom　C. sudo tom　D. login tom —— **答案：B**。
>
> **知识点：**
> - root 切普通用户不需要密码；普通用户切 root 需要 root 密码。
> - `su` 与 `sudo` 的区别：su 切换整个身份，sudo 以其他身份执行单条命令。

50. Linux中，可以用\_\_\_\_\_\_\_\_\_\_命令来启动某个系统服务。

> **答案：service**（或 `systemctl`）
>
> **解析：**`service 服务名 start` 用于启动某个系统服务（SysV 风格），如 `service httpd start`；systemd 系统则使用 `systemctl start 服务名`。
>
> **类似题：**查看 httpd 服务运行状态的命令是（　）。A. service httpd status　B. service httpd start　C. systemctl stop httpd　D. chkconfig httpd on —— **答案：A**。
>
> **知识点：**
> - 服务操作：start 启动、stop 停止、restart 重启、reload 重载配置、status 查看状态。
> - 开机自启：`chkconfig 服务名 on`（SysV）或 `systemctl enable 服务名`（systemd）。


## 三、综合应用题（共 2 题）

**1. 假设某服务器IP地址为10.1.1.10，某用户所在客户端IP地址为10.1.1.50。用户在客户端完成了C语言应用程序，相关文件放在 `/opt/myproject/` 目录下。用户需要将自已的源程序上传到服务器端进行编译、运行和调试。**
(1) 首先用户要用 scp 命令把本地文件目录 `/opt/myproject/` 上传至服务器的 `/opt/` 目录下，应输入：
   `[root@localhost]#` 【51】\_\_\_\_\_\_\_\_\_\_
(2) 然后，用户使用 ssh 以root身份登录服务器，应输入：
   `[root@localhost]#` 【52】\_\_\_\_\_\_\_\_\_\_
(3) 上传成功，进入服务器 `/opt/myproject/` 目录后，用户使用 gcc 编译该目录下的c语言源程序，使用【53】\_\_\_\_\_\_\_\_\_\_编译器选项产生调试信息，使用【54】\_\_\_\_\_\_\_\_\_\_选项将指定目录下的文件加入到程序库文件的搜索目录列表中。
假设源程序 `main.c` 如下：
```c
#include <stdio.h>
int add(int a, int b) {
    int c = a + b;
    return c;
}
int main(void) {
    int i = 0;
    int j = 3;
    int k = add(i,j);
    printf("i=%d, j=%d, k=%d\n", i,j,k);
    return 0;
}
```
(4) 加入调试选项进行编译后，启动gdb调试该程序，应输入：【55】\_\_\_\_\_\_\_\_\_\_
(5) 在 `int k = add(i,j);` 处设置断点，应输入：【56】\_\_\_\_\_\_\_\_\_\_
(6) 然后在gdb的提示符下输入 `run` 运行程序，程序运行到所设断点处，然后输入【57】\_\_\_\_\_\_\_\_\_\_ 命令执行 `int k = add(i,j);` 语句，执行完程序停到 `printf` 处。之后输入【58】\_\_\_\_\_\_\_\_\_\_ 命令继续执行完毕。
(7) 用户进一步将add函数从 `main.c` 文件中分离出来，放到单独的 `add.c` 文件中，并打算使用 `make` 命令进行程序的编译，需要编写【59】\_\_\_\_\_\_\_\_\_\_文件。
(8) 然后执行 `make` 命令进行编译，当用户需要清理编译生成的可执行程序和中间文件时，应执行【60】\_\_\_\_\_\_\_\_\_\_


> **参考答案与解析：**
>
> - 【51】`scp -r /opt/myproject/ root@10.1.1.10:/opt/` —— 上传目录需加 `-r` 递归选项。
> - 【52】`ssh root@10.1.1.10` —— 以 root 身份远程登录服务器。
> - 【53】`-g` —— 生成 GDB 可用的调试信息。
> - 【54】`-L` —— 指定库文件的搜索目录（`-L 目录`）。
> - 【55】`gdb main`（若编译时未用 `-o` 指定输出名，则默认为 `gdb a.out`）。
> - 【56】`break 9`（或 `b 9`）—— `int k = add(i,j);` 位于 main.c 第 9 行（从 `#include` 行算起）。
> - 【57】`next`（或 `n`）—— 单步执行，不进入 add 函数内部，执行完该行后停在 printf 处。
> - 【58】`continue`（或 `c`）—— 继续运行直到程序结束。
> - 【59】`Makefile`（或 `makefile`）—— make 命令读取的编译规则文件。
> - 【60】`make clean` —— 执行 Makefile 中定义的 clean 伪目标，清理可执行文件和中间文件。
>
> **解析要点：**
> - 编译调试版本：`gcc -g main.c -o main`；启动调试：`gdb main`。
> - `next` 与 `step` 的区别：next 把函数调用当作一条语句执行完，step 会进入函数内部。
> - `continue` 让程序从当前断点继续运行到下一个断点或结束。
>
> **变式练习：**GDB 中若要进入 add 函数内部逐行调试，应使用命令（　）。A. next　B. step　C. continue　D. run —— **答案：B**。
>
> **知识点：**
> - GDB 常用命令：`break` 设断点、`run` 运行、`next/step` 单步、`print` 查看值、`bt` 查看栈、`delete` 删除断点、`quit` 退出。
> - Makefile 规则格式：`目标: 依赖`，命令行以 Tab 开头；伪目标 clean 常用 `rm -f 目标 中间文件`。

**2. 当前工作目录下面有三个脚本 ex1，ex2，ex3，写出执行脚本 ex1 后的显示结果：**
```bash
[student@localhost~]$ cat ex1
export score=100
echo "====="
./ex2
echo "====="
./ex3

[student@localhost~]$ cat ex2
echo "2:"$score
export score=90
./ex3

[student@localhost~]$ cat ex3
echo "3:"$score
```
(1) 输出结果依次为：
【61】\_\_\_\_\_\_\_\_\_\_
【62】\_\_\_\_\_\_\_\_\_\_
【63】\_\_\_\_\_\_\_\_\_\_
【64】\_\_\_\_\_\_\_\_\_\_
【65】\_\_\_\_\_\_\_\_\_\_
(2) 继续执行以下shell命令：
```bash
[student@localhost~]$ touch file1 file2 file3 file4
[student@localhost~]$ echo a > file1
[student@localhost~]$ echo -n a > file2
[student@localhost~]$ for ((i=0; i<4096; i++)); do echo a >> file3; done
[student@localhost~]$ for ((i=0; i<4096; i++)); do echo -n a >> file4; done
[student@localhost~]$ wc -c file? | sort -n
```
结果是（列出这5行结果输出）：
【66】\_\_\_\_\_\_\_\_\_\_
【67】\_\_\_\_\_\_\_\_\_\_
【68】\_\_\_\_\_\_\_\_\_\_
【69】\_\_\_\_\_\_\_\_\_\_
【70】\_\_\_\_\_\_\_\_\_\_

> **参考答案与解析：**
>
> - 【61】`=====`
> - 【62】`2:100` —— ex2 作为 ex1 的子进程，继承了 ex1 导出的环境变量 score=100。
> - 【63】`3:90` —— ex3 由 ex2 启动，继承了 ex2 修改后的环境变量 score=90。
> - 【64】`=====`
> - 【65】`3:100` —— ex2 对 score 的修改只影响它自己及其子进程，不影响父进程 ex1；ex1 再次调用 ex3 时传给它的仍是 100。
> - 【66】`1 file2`
> - 【67】`2 file1`
> - 【68】`4096 file4`
> - 【69】`8192 file3`
> - 【70】`12291 total`
>
> **解析要点：**
> - `export` 导出的变量可被子进程继承；子进程对变量的修改不会反向影响父进程。
> - `echo a > file1` 会输出 "a" 加换行符，共 2 字节；`echo -n a` 不输出换行符，共 1 字节。
> - 循环 4096 次追加：file3 每行 2 字节共 8192 字节，file4 每行 1 字节共 4096 字节。
> - `wc -c file?` 依次统计 4 个文件并给出 total，`sort -n` 按字节数从小到大排序输出。
>
> **变式练习：**Shell 中执行 `x=1; bash -c 'echo $x'` 的输出是（　）。A. 1　B. 0　C. 空　D. 报错 —— **答案：C**。普通变量未 export，子 Shell 无法继承。
>
> **知识点：**
> - 环境变量作用域：export 的变量对子进程可见，未导出的变量只在当前 Shell 有效。
> - `wc -c` 统计字节数；`echo` 默认输出换行符，`echo -n` 不换行。
> - `sort -n` 按数值排序，`sort -r` 逆序。


---

# 全国计算机等级考试Linux应用与开发技术试题（三）

## 一、单项选择题（共 40 题）

**1. 下列操作系统特征中，(  )是操作系统最重要的特征，其它三个都以它为前提。**
<table>
  <tr><td width="50%">A. 共享</td><td>B. 并发</td></tr>
  <tr><td>C. 虚拟</td><td>D. 异步</td></tr>
</table>


> **答案：B**
>
> **解析：**现代操作系统的基本特征是并发、共享、虚拟和异步，其中并发（多个程序在同一时间段内交替执行）是最重要的特征，共享、虚拟、异步都以并发为前提。
>
> **类似题：**操作系统的基本特征不包括（　）。A. 并发　B. 共享　C. 虚拟　D. 编译 —— **答案：D**。编译是编译程序的功能，不是操作系统的特征。
>
> **知识点：**
> - 并发：多个程序宏观上同时运行、微观上交替执行；并行：多个程序在同一时刻真正同时执行（需多核）。
> - 共享分为互斥共享（如打印机）和同时共享（如磁盘文件）。

**2. 默认情况下，管理员创建了一个用户，就会在(  )目录下创建一个用户主目录。**
<table>
  <tr><td width="50%">A. /usr</td><td>B. /home</td></tr>
  <tr><td>C. /root</td><td>D. /etc</td></tr>
</table>


> **答案：B**
>
> **解析：**默认情况下创建普通用户会在 `/home` 目录下创建同名主目录；`/usr` 存放系统程序，`/root` 是 root 的主目录，`/etc` 存放配置文件。
>
> **类似题：**创建用户时指定主目录应使用的选项是（　）。A. useradd -g　B. useradd -d　C. useradd -u　D. useradd -s —— **答案：B**。
>
> **知识点：**
> - 用户主目录默认路径由 `/etc/default/useradd` 中 HOME 变量决定。
> - 主目录模板位于 `/etc/skel`，创建用户时自动复制。

**3. `"cd"` 命令可以改变用户的当前目录。当用户输入命令 `"cd"` 并按Enter键后(  )**
<table>
  <tr><td width="50%">A. 当前目录改为根目录</td><td>B. 当前目录不变，屏幕显示当前目录</td></tr>
  <tr><td>C. 当前目录改为用户home目录</td><td>D. 当前目录改为上一级目录</td></tr>
</table>


> **答案：C**
>
> **解析：**`cd` 不带任何参数时，等同于 `cd ~`，切换到当前用户的主目录；`cd /` 才是到根目录，`cd ..` 到上一级目录。
>
> **类似题：**执行 `cd -` 的作用是（　）。A. 回到主目录　B. 回到根目录　C. 切换到上一次所在目录　D. 切换到上一级目录 —— **答案：C**。
>
> **知识点：**
> - `cd`、`cd ~`：主目录；`cd /`：根目录；`cd ..`：上级目录；`cd -`：上次目录。
> - `cd` 是 Shell 内建命令，`type cd` 可查看。

**4. 在Shell脚本中，用来读取键盘输入内容并将其赋值给Shell变量的命令是(  )**
<table>
  <tr><td width="50%">A. fold</td><td>B. join</td></tr>
  <tr><td>C. tr</td><td>D. read</td></tr>
</table>


> **答案：D**
>
> **解析：**`read` 是 Shell 内建命令，用于从标准输入（键盘）读取一行内容并赋给变量，常与 `-p` 选项配合输出提示信息。`fold`、`join`、`tr` 都是文本处理命令。
>
> **类似题：**从键盘读取输入到变量 name 并显示提示，正确的写法是（　）。A. read name -p "name:"　B. read -p "name:" name　C. echo name　D. input name —— **答案：B**。
>
> **知识点：**
> - `read 变量1 变量2` 可一次读取多个变量（以空格分隔）。
> - 常用选项：`-p` 提示、`-n N` 读 N 个字符、`-s` 隐藏输入（密码）、`-t N` 超时。

**5. 下面用于定义Shell全局变量的命令是(  )**
<table>
  <tr><td width="50%">A. exportfs</td><td>B. alias</td></tr>
  <tr><td>C. exports</td><td>D. export</td></tr>
</table>


> **答案：D**
>
> **解析：**`export 变量名` 将 Shell 变量导出为环境变量，使其对当前 Shell 及其所有子进程有效。`alias` 用于设置命令别名；`exportfs` 用于 NFS 共享；`exports` 不是命令。
>
> **类似题：**使变量 MYVAR 成为环境变量的命令是（　）。A. alias MYVAR　B. export MYVAR　C. set MYVAR　D. env MYVAR —— **答案：B**。
>
> **知识点：**
> - 环境变量用 `env` 或 `printenv` 查看；`export -p` 列出所有已导出的变量。
> - 变量赋值时等号两边不能有空格，如 `NAME=tom`。

**6. 为卸载一个软件包，应使用(  )**
<table>
  <tr><td width="50%">A. rpm -i</td><td>B. rpm -e</td></tr>
  <tr><td>C. rpm -q</td><td>D. rpm -V</td></tr>
</table>


> **答案：B**
>
> **解析：**`rpm -e 包名` 用于卸载（erase）已安装的软件包；`-i` 安装，`-q` 查询，`-V` 校验。
>
> **类似题：**安装 RPM 包并显示详细过程，应使用（　）。A. rpm -ivh 包名　B. rpm -e 包名　C. rpm -qa 包名　D. rpm -ql 包名 —— **答案：A**。
>
> **知识点：**
> - RPM 常用操作：`-ivh` 安装、`-Uvh` 升级、`-e` 卸载、`-qa` 列出全部、`-qf` 查文件所属包。
> - 卸载时若存在依赖关系会提示错误，可加 `--nodeps` 强制卸载（慎用）。

**7. 如果想列出一个目录下的所有文件，但不包括 `"."` 与 `".."` 这两个目录，需要使用命令行(  )**
<table>
  <tr><td width="50%">A. ls -l</td><td>B. ls</td></tr>
  <tr><td>C. ls -a</td><td>D. ls -A</td></tr>
</table>


> **答案：D**
>
> **解析：**`ls -A` 列出目录下所有文件（包括隐藏文件），但不包括 `.` 和 `..`；`ls -a` 会包含 `.` 和 `..`；`ls` 不显示隐藏文件。
>
> **类似题：**只显示当前目录下隐藏文件的命令是（　）。A. ls -a　B. ls -A　C. ls -d .*　D. ls -l —— **答案：C**（`ls -a` 也会显示 . 和 ..）。
>
> **知识点：**
> - 以 `.` 开头的文件为隐藏文件，如 `.bashrc`。
> - `ls -l` 长格式、`-h` 易读大小、`-R` 递归、`-d` 显示目录本身。

**8. 当使用mount进行设备或者文件系统挂载的时候，需要用到的设备名称位于(  )目录。**
<table>
  <tr><td width="50%">A. /home</td><td>B. /bin</td></tr>
  <tr><td>C. /etc</td><td>D. /dev</td></tr>
</table>


> **答案：D**
>
> **解析：**Linux 中所有设备都以文件形式存放在 `/dev` 目录下，挂载时使用的设备名称（如 /dev/sdb1、/dev/cdrom）都来自该目录。
>
> **类似题：**光盘设备文件通常表示为（　）。A. /dev/hda　B. /dev/cdrom　C. /etc/fstab　D. /mnt/cdrom —— **答案：B**。
>
> **知识点：**
> - 常见设备文件：/dev/sda（磁盘）、/dev/tty（终端）、/dev/null（空设备）、/dev/zero。
> - 设备文件用主设备号和次设备号标识。

**9. 以下可以终止一个用户所有进程的命令是(  )**
<table>
  <tr><td width="50%">A. skillall</td><td>B. skill</td></tr>
  <tr><td>C. kill</td><td>D. killall</td></tr>
</table>


> **答案：D**
>
> **解析：**`killall` 可按进程名终止进程，配合 `-u 用户名` 可终止某用户的所有进程；`kill` 按 PID 发送信号；`skill` 按条件发送信号；`skillall` 不存在。
>
> **类似题：**终止用户 tom 的所有进程，可执行（　）。A. killall -u tom　B. kill -u tom　C. killall tom　D. skillall tom —— **答案：A**。
>
> **知识点：**
> - `killall -9 进程名` 强制终止；`pkill -u 用户名` 也可按用户终止。
> - 常用信号：15 SIGTERM（默认，可被捕获）、9 SIGKILL（强制，不可捕获）。

**10. Linux文件系统中的文件有三个时间戳，下列哪一个不是Linux文件时间戳？(  )**
<table>
  <tr><td width="50%">A. 创建时间</td><td>B. 修改时间</td></tr>
  <tr><td>C. 访问时间</td><td>D. 状态改变时间</td></tr>
</table>


> **答案：A**
>
> **解析：**Linux 文件的三个时间戳是：atime（访问时间）、mtime（修改时间，内容变化）、ctime（状态改变时间，元信息变化）。没有“创建时间”这一时间戳。
>
> **类似题：**用 `ls -l` 默认显示的文件时间是（　）。A. atime　B. mtime　C. ctime　D. 创建时间 —— **答案：B**。
>
> **知识点：**
> - `ls -lu` 显示访问时间，`ls -lc` 显示状态改变时间，`ls -l --time=atime` 也可。
> - `stat 文件` 可同时查看三种时间戳。

**11. 关于文件 `/etc/sysconfig/network-scripts/ifcfg-eth0` 的描述哪个是正确的？(  )**
<table>
  <tr><td width="50%">A. 它是一个系统脚本文件</td><td>B. 它是可执行文件</td></tr>
  <tr><td>C. 它存放本机的名字</td><td>D. 它指定本机eth0的IP地址</td></tr>
</table>


> **答案：D**
>
> **解析：**`/etc/sysconfig/network-scripts/ifcfg-eth0` 是网卡 eth0 的配置文件，其中包含 `IPADDR`、`NETMASK`、`BOOTPROTO` 等，用于指定该网卡的 IP 地址等网络参数；它不是可执行脚本，主机名在 `/etc/sysconfig/network` 中配置。
>
> **类似题：**设置网卡开机自动启用的配置项是（　）。A. ONBOOT=yes　B. BOOTPROTO=dhcp　C. IPADDR=...　D. GATEWAY=... —— **答案：A**。
>
> **知识点：**
> - 网卡配置文件命名规则：`ifcfg-网卡名`，位于 `/etc/sysconfig/network-scripts/`。
> - 修改后使用 `service network restart` 使配置生效。

**12. 用户编写了一个文本文件atxt，想将该文件名称改为txta，下列哪个命令可以实现？(  )**
<table>
  <tr><td width="50%">A. cd atxt txta</td><td>B. echo atxt > txta</td></tr>
  <tr><td>C. rm atxt txta</td><td>D. mv atxt txta</td></tr>
</table>


> **答案：D**
>
> **解析：**重命名文件使用 `mv 原文件名 新文件名`，即 `mv atxt txta`。`cd` 是切换目录，`echo >` 是写文件，`rm` 是删除文件。
>
> **类似题：**将文件 a 移动到目录 /tmp 下的命令是（　）。A. mv a /tmp　B. cp a /tmp　C. rm a /tmp　D. cat a /tmp —— **答案：A**。
>
> **知识点：**
> - `mv` 同时具有移动和重命名功能；同目录内移动即重命名。
> - 若目标已存在，`mv` 默认覆盖，可用 `-i` 提示、`-n` 不覆盖。

**13. 下列文件中，包含了本地主机名到IP地址的映射关系的文件是(  )**
<table>
  <tr><td width="50%">A. /etc/HOSTNAME</td><td>B. /etc/hosts</td></tr>
  <tr><td>C. /etc/resolv.conf</td><td>D. /etc/networks</td></tr>
</table>


> **答案：B**
>
> **解析：**`/etc/hosts` 是本地主机名到 IP 地址的静态映射文件；`/etc/resolv.conf` 配置 DNS 服务器；`/etc/networks` 定义网络名；`/etc/HOSTNAME` 并非标准文件（通常为 `/etc/hostname`）。
>
> **类似题：**`/etc/hosts` 文件中一行内容为 `127.0.0.1 localhost`，其中 localhost 是（　）。A. IP 地址　B. 主机名　C. 子网掩码　D. 网关 —— **答案：B**。
>
> **知识点：**
> - hosts 文件格式：`IP地址 主机名 [别名]`。
> - 解析顺序由 `/etc/nsswitch.conf` 的 hosts 行控制，通常 `files dns`。

**14. 当用户与某远程网络连接不上时，就需要跟踪路由查看，以便了解在网络的什么位置出现了问题，满足该目的的命令是(  )**
<table>
  <tr><td width="50%">A. ping</td><td>B. checkroute</td></tr>
  <tr><td>C. traceroute</td><td>D. netstat</td></tr>
</table>


> **答案：C**
>
> **解析：**`traceroute`（Windows 下为 tracert）逐跳显示数据包到达目标主机所经过的路由，用于定位网络中断位置；`ping` 只测连通性；`netstat` 查看连接状态；`checkroute` 不是标准命令。
>
> **类似题：**下列命令中可以显示数据包到达目标主机所经过的每一跳路由的是（　）。A. ping　B. ifconfig　C. traceroute　D. netstat —— **答案：C**。
>
> **知识点：**
> - traceroute 利用 IP 报文 TTL 递增和 ICMP 超时消息实现路径探测。
> - 现代系统可用 `tracepath`、`mtr` 等替代工具。

**15. 在给定文件中查找与设定条件相符的字符串的命令为(  )**
<table>
  <tr><td width="50%">A. grep</td><td>B. gzip</td></tr>
  <tr><td>C. find</td><td>D. sort</td></tr>
</table>


> **答案：A**
>
> **解析：**`grep` 在文件中查找符合条件（模式）的字符串并输出匹配行；`gzip` 压缩文件；`find` 按条件查找文件；`sort` 排序。
>
> **类似题：**在文件 file 中查找包含“error”的行并显示行号，应使用（　）。A. grep -n error file　B. find error file　C. grep -v error file　D. sort error file —— **答案：A**。
>
> **知识点：**
> - `grep -i` 忽略大小写、`-v` 反向选择、`-c` 统计行数、`-r` 递归目录。
> - `grep` 支持基本正则表达式，`egrep`/`grep -E` 支持扩展正则。

**16. 在Linux中，更改普通用户为超级用户的命令是(  )**
<table>
  <tr><td width="50%">A. super</td><td>B. su</td></tr>
  <tr><td>C. tar</td><td>D. passwd</td></tr>
</table>


> **答案：B**
>
> **解析：**`su`（switch user）用于在已登录状态下切换用户身份，如从普通用户切换到 root；`passwd` 修改密码；`tar` 是打包工具；`super` 不是标准命令。
>
> **类似题：**从普通用户切换到 root 且加载 root 的环境变量，应使用（　）。A. su root　B. su - root　C. sudo su　D. login —— **答案：B**。
>
> **知识点：**
> - `su` 不加载目标用户环境，`su -` 相当于重新登录。
> - `sudo` 允许授权用户以 root 身份执行指定命令，配置在 `/etc/sudoers`。

**17. 以下经常用于分屏显示文件内容的命令是(  )**
<table>
  <tr><td width="50%">A. ls</td><td>B. cat</td></tr>
  <tr><td>C. file</td><td>D. more</td></tr>
</table>


> **答案：D**
>
> **解析：**`more` 可分屏（分页）显示文件内容，按空格翻页、q 退出；`cat` 一次性输出全部内容；`ls` 列目录；`file` 判断文件类型。
>
> **类似题：**逐屏显示文件内容且支持向前翻页的工具是（　）。A. cat　B. more　C. less　D. head —— **答案：C**。`less` 比 `more` 功能更强，支持上下翻页。
>
> **知识点：**
> - `more` 只能向下翻页；`less` 可前后翻页且不预读整个文件。
> - 常与管道配合：`ls -l | more`。

**18. 以下不是文件或目录访问权限的是(  )**
<table>
  <tr><td width="50%">A. r</td><td>B. w</td></tr>
  <tr><td>C. g</td><td>D. x</td></tr>
</table>


> **答案：C**
>
> **解析：**文件访问权限只有读（r）、写（w）、执行（x）三种；`g` 表示“同组用户”（group），是权限的作用对象而不是权限类型。
>
> **类似题：**文件权限中 u、g、o 分别代表（　）。A. 用户、组、其他　B. 组、用户、其他　C. 其他、组、用户　D. 用户、其他、组 —— **答案：A**。
>
> **知识点：**
> - 权限位：r=4、w=2、x=1；对象：u（所有者）、g（同组）、o（其他）、a（全部）。
> - 目录的 x 权限表示可以进入该目录，r 表示可列出目录内容。

**19. 在Linux目录结构中，Linux的内核及引导程序所需要的文件位于(  )目录。**
<table>
  <tr><td width="50%">A. /bin</td><td>B. /boot</td></tr>
  <tr><td>C. /root</td><td>D. /proc</td></tr>
</table>


> **答案：B**
>
> **解析：**`/boot` 目录存放 Linux 内核（vmlinuz）、引导程序（grub）及初始化内存盘等启动所需文件；`/bin` 存放基本命令，`/root` 是 root 主目录，`/proc` 是虚拟文件系统。
>
> **类似题：**存放系统可执行命令的目录是（　）。A. /boot　B. /bin　C. /dev　D. /etc —— **答案：B**。
>
> **知识点：**
> - `/boot` 内核与引导；`/bin`/`/usr/bin` 用户命令；`/sbin` 管理命令；`/lib` 库文件；`/etc` 配置；`/var` 可变数据；`/tmp` 临时文件。
> - 根文件系统下的目录遵循 FHS（文件系统层次标准）。

**20. 文件exer1的访问权限为 `rw-r--r--`，现要增加所有用户的执行权限和同组用户的写权限，下列命令正确的是(  )**
<table>
  <tr><td width="50%">A. chmod a+x,g+w exer1</td><td>B. chmod 765 exer1</td></tr>
  <tr><td>C. chmod o+x exer1</td><td>D. chmod g+w exer1</td></tr>
</table>


> **答案：A**
>
> **解析：**原权限 `rw-r--r--`（644）。“所有用户增加执行权限”即 `a+x`，“同组用户增加写权限”即 `g+w`，组合命令为 `chmod a+x,g+w exer1`，结果权限为 `rwxrwxr-x`（775）。选项 B 的 765 与要求不符，C、D 都不完整。
>
> **类似题：**将文件权限设置为所有用户可读写执行，应执行（　）。A. chmod 777 file　B. chmod 666 file　C. chmod a+x file　D. chmod u+w file —— **答案：A**。
>
> **知识点：**
> - 符号法权限修改：`+` 增加、`-` 去除、`=` 设置；可组合如 `u+x,go-w`。
> - 数字法：777=rwxrwxrwx，755=rwxr-xr-x，644=rw-r--r--。

**21. 在Linux系统中添加新用户的命令是(  )**
<table>
  <tr><td width="50%">A. groupadd</td><td>B. usermod</td></tr>
  <tr><td>C. userdel</td><td>D. useradd</td></tr>
</table>


> **答案：D**
>
> **解析：**添加新用户使用 `useradd`；`groupadd` 添加组，`usermod` 修改用户，`userdel` 删除用户。
>
> **类似题：**创建新用户 tom 的命令是（　）。A. useradd tom　B. adduser tom　C. newuser tom　D. usermod tom —— **答案：A**（部分发行版也提供更友好的 `adduser`）。
>
> **知识点：**
> - 用户管理命令：useradd、usermod、userdel；组管理命令：groupadd、groupmod、groupdel。
> - 创建用户后可用 `passwd 用户名` 设置初始密码。

**22. 系统服务的启动脚本通常都存放在(  )目录中。**
<table>
  <tr><td width="50%">A. /etc/init.d/</td><td>B. /bin/init.d/</td></tr>
  <tr><td>C. /etc/init/</td><td>D. /bin/init</td></tr>
</table>


> **答案：A**
>
> **解析：**SysV 风格的服务启动脚本通常存放在 `/etc/init.d/` 目录（RHEL 中为 `/etc/rc.d/init.d` 的符号链接），如 `/etc/init.d/httpd`；systemd 的服务单元文件存放在 `/usr/lib/systemd/system/`。
>
> **类似题：**使用脚本启动 httpd 服务的方式是（　）。A. /etc/init.d/httpd start　B. /etc/httpd start　C. /bin/httpd start　D. /usr/httpd start —— **答案：A**。
>
> **知识点：**
> - 服务脚本可通过 `service 服务名 动作` 或脚本绝对路径调用。
> - `chkconfig` 管理不同运行级别的自启状态。

**23. 以下日志等级最高（最严重）的是(  )**
<table>
  <tr><td width="50%">A. info</td><td>B. alert</td></tr>
  <tr><td>C. notice</td><td>D. warning</td></tr>
</table>


> **答案：B**
>
> **解析：**syslog 日志级别从高到低为：emerg(0) > alert(1) > crit(2) > err(3) > warning(4) > notice(5) > info(6) > debug(7)。选项中 alert 级别最高（最严重）。
>
> **类似题：**下列日志级别中最低（最不严重）的是（　）。A. info　B. debug　C. warning　D. err —— **答案：B**。
>
> **知识点：**
> - 日志配置在 `/etc/rsyslog.conf`，格式为“设施.级别 动作”。
> - 如 `*.info` 表示记录 info 及以上级别（更严重的）日志。

**24. 当进程当前分配的时间片用完时，进程状态会转换为(  )**
<table>
  <tr><td width="50%">A. 运行态</td><td>B. 就绪态</td></tr>
  <tr><td>C. 等待态</td><td>D. 终止态</td></tr>
</table>


> **答案：B**
>
> **解析：**时间片用完时，进程还没有执行完毕，但需要让出 CPU 给其他进程，此时进程从运行态转换到就绪态，等待下一次调度；等待 I/O 才进入等待（阻塞）态。
>
> **类似题：**进程等待 I/O 完成时，其状态变为（　）。A. 运行态　B. 就绪态　C. 等待态　D. 终止态 —— **答案：C**。
>
> **知识点：**
> - 三态模型：就绪（等待 CPU）、运行（占用 CPU）、阻塞/等待（等待资源或事件）。
> - 进程调度：时间片轮转、优先级调度等。

**25. 存放用户账号基本信息的文件是(  )**
<table>
  <tr><td width="50%">A. shadow</td><td>B. group</td></tr>
  <tr><td>C. passwd</td><td>D. gshadow</td></tr>
</table>


> **答案：C**
>
> **解析：**`/etc/passwd` 存放用户账号基本信息（用户名、UID、GID、主目录、登录 Shell）；`/etc/shadow` 存密码散列；`/etc/group` 存组信息；`/etc/gshadow` 存组密码。
>
> **类似题：**`/etc/passwd` 文件中每一行包含的字段个数是（　）。A. 5　B. 6　C. 7　D. 9 —— **答案：C**。
>
> **知识点：**
> - passwd 字段：用户名:密码占位:UID:GID:注释:主目录:Shell。
> - UID：0 为 root，1～499 系统用户，500/1000 起为普通用户。

**26. 检查磁盘空间占用情况应该使用哪个命令？(  )**
<table>
  <tr><td width="50%">A. df -k</td><td>B. ps -k</td></tr>
  <tr><td>C. format</td><td>D. disk</td></tr>
</table>


> **答案：A**
>
> **解析：**`df -k` 以 KB 为单位显示各文件系统的磁盘空间占用情况；`ps` 查看进程；`format` 是 Solaris 等系统的磁盘格式化命令；`disk` 不是标准命令。
>
> **类似题：**以易读单位显示磁盘空间使用情况的命令是（　）。A. df -h　B. df -k　C. du -h　D. free -h —— **答案：A**。
>
> **知识点：**
> - `df -T` 显示文件系统类型，`df -i` 显示 inode 使用情况。
> - `du` 用于统计目录/文件占用，与 df 互补。

**27. 下列哪个命令可以查看系统中所有进程？(  )**
<table>
  <tr><td width="50%">A. ps</td><td>B. ps -la</td></tr>
  <tr><td>C. top</td><td>D. ps -le</td></tr>
</table>


> **答案：D**
>
> **解析：**`ps -le` 采用长格式列出系统中所有进程（-e 表示所有进程，-l 表示长格式），包含 F、S、UID、PID、PPID、PRI、NI 等字段；`ps` 只显示当前终端相关进程；`top` 是动态监视工具；`ps -la` 不能显示全部进程。
>
> **类似题：**显示所有进程并采用完整格式的命令还可以是（　）。A. ps aux　B. ps -l　C. ps -a　D. ps -u —— **答案：A**（`ps -ef` 也可以）。
>
> **知识点：**
> - System V 风格：`-e` 所有进程、`-f` 全格式；BSD 风格：`a` 所有用户、`x` 无终端、`u` 用户格式。
> - `ps -le` 输出中 S 列表示进程状态，如 R 运行、S 睡眠、Z 僵死。

**28. 在vi编辑器里，命令 `dd` 用来删除当前的(  )**
<table>
  <tr><td width="50%">A. 行</td><td>B. 变量</td></tr>
  <tr><td>C. 字</td><td>D. 字符</td></tr>
</table>


> **答案：A**
>
> **解析：**vi 命令模式下 `dd` 删除（剪切）光标所在的整行，`ndd` 删除 n 行；`dw` 删除一个单词，`x` 删除一个字符。
>
> **类似题：**在 vi 中删除从光标行开始的 5 行，应输入（　）。A. 5dd　B. 5yy　C. d5　D. 5x —— **答案：A**。
>
> **知识点：**
> - 删除操作：`x` 字符、`dw` 单词、`dd` 行、`d$` 到行尾、`d0` 到行首。
> - 删除的内容会进入剪贴板，可用 `p` 粘贴。

**29. 在vi编辑器中，哪条命令是不保存强制退出？(  )**
<table>
  <tr><td width="50%">A. :wq</td><td>B. :wq!</td></tr>
  <tr><td>C. :q!</td><td>D. :quit</td></tr>
</table>


> **答案：C**
>
> **解析：**`:q!` 表示不保存强制退出（放弃修改）；`:wq` 保存并退出；`:wq!` 强制保存并退出（针对只读文件等）；`:quit` 不是标准命令。
>
> **类似题：**保存对文件的修改并退出 vi，应输入（　）。A. :q!　B. :wq　C. :w　D. :e! —— **答案：B**。
>
> **知识点：**
> - `:w` 保存、`:q` 退出（有未保存修改时拒绝）、`:q!` 放弃修改退出、`:wq`/`:x` 保存退出。
> - 命令模式下 `ZZ` 也可保存并退出。

**30. 在vi编辑器中的命令模式下，重复上一次对编辑的文本进行的操作，可使用(  )命令。**
<table>
  <tr><td width="50%">A. 上箭头</td><td>B. 下箭头</td></tr>
  <tr><td>C. “.”</td><td>D. “*”</td></tr>
</table>


> **答案：C**
>
> **解析：**vi 命令模式下，`.`（点）重复执行上一次的编辑操作，例如上次执行了 `dd`，按 `.` 再删除一行；方向键用于移动光标，`*` 用于查找当前单词。
>
> **类似题：**在 vi 中撤销上一次操作的命令是（　）。A. u　B. r　C. x　D. p —— **答案：A**。
>
> **知识点：**
> - `u` 撤销、`Ctrl+r` 重做（部分版本）、`.` 重复上次操作。
> - `J` 合并当前行与下一行。

**31. 在vi编辑器打开文档，默认是不显示行号的，输入(  )让它显示行号。**
<table>
  <tr><td width="50%">A. set ruler</td><td>B. set nu</td></tr>
  <tr><td>C. set ic</td><td>D. set nonu</td></tr>
</table>


> **答案：B**
>
> **解析：**`:set nu`（number）在 vi 中显示行号，`:set nonu` 取消行号；`:set ruler` 在右下角显示光标行列位置；`:set ic` 忽略大小写。
>
> **类似题：**在 vi 中取消行号显示的命令是（　）。A. :set nu　B. :set nonu　C. :set number=0　D. :set noruler —— **答案：B**。
>
> **知识点：**
> - `:set all` 查看所有设置，`:set` 查看已修改的设置。
> - 配置可写入 `~/.vimrc` 持久生效。

**32. 以下哪种不是vi编辑器的工作模式？(  )**
<table>
  <tr><td width="50%">A. 命令模式</td><td>B. 编辑模式</td></tr>
  <tr><td>C. 末行模式</td><td>D. 检查模式</td></tr>
</table>


> **答案：D**
>
> **解析：**vi 有三种基本工作模式：命令模式（默认）、编辑模式（插入/替换）、末行模式（执行 : 开头的命令）。“检查模式”不是 vi 的工作模式。
>
> **类似题：**vi 启动后默认处于（　）。A. 编辑模式　B. 末行模式　C. 命令模式　D. 插入模式 —— **答案：C**。
>
> **知识点：**
> - 命令模式按 `i`/`a`/`o` 进入编辑模式；编辑模式按 Esc 返回命令模式。
> - 命令模式输入 `:` 进入末行模式，执行完自动返回命令模式。

**33. 编辑 `.bashrc` 配置完java环境变量，在终端应执行(  )命令使环境变量立即生效。**
<table>
  <tr><td width="50%">A. source ~/.bashrc</td><td>B. export ~/.bashrc</td></tr>
  <tr><td>C. ~/.bashrc</td><td>D. ~/.bashrc &</td></tr>
</table>


> **答案：A**
>
> **解析：**`source ~/.bashrc`（或 `. ~/.bashrc`）在**当前** Shell 中执行配置文件，使修改立即生效；直接执行 `~/.bashrc` 是在子 Shell 中运行，对当前环境无效。
>
> **类似题：**修改 `/etc/profile` 后使其立即生效，可执行（　）。A. source /etc/profile　B. /etc/profile　C. bash /etc/profile　D. export /etc/profile —— **答案：A**。
>
> **知识点：**
> - `source` 与 `.` 等价，不创建子进程。
> - 环境变量配置文件：`/etc/profile`（全局）、`~/.bash_profile`、`~/.bashrc`（用户）。

**34. 以下哪个GDB调试命令是单步执行下一条语句，如遇到函数则进入函数内部？(  )**
<table>
  <tr><td width="50%">A. next</td><td>B. step</td></tr>
  <tr><td>C. nexti</td><td>D. stepi</td></tr>
</table>


> **答案：B**
>
> **解析：**GDB 中 `step`（s）单步执行下一条语句，遇到函数调用会进入函数内部；`next`（n）把函数调用当作一条语句执行完；`nexti`/`stepi` 针对汇编指令级别。
>
> **类似题：**GDB 中单步执行但不想进入函数内部，应使用（　）。A. step　B. next　C. run　D. continue —— **答案：B**。
>
> **知识点：**
> - `step` 步入（step into）、`next` 步过（step over）、`finish` 执行完当前函数并返回、`continue` 运行到下一断点。
> - 汇编级单步：`stepi`、`nexti`。

**35. gcc可以对编译阶段进行控制，其中(  )选项设定库文件的路径。**
<table>
  <tr><td width="50%">A. -L path</td><td>B. -l path</td></tr>
  <tr><td>C. -l library</td><td>D. -static</td></tr>
</table>


> **答案：A**
>
> **解析：**GCC 的 `-L path` 指定库文件的搜索路径；`-l library` 指定要链接的库（如 -lm）；`-static` 进行静态链接。
>
> **类似题：**链接数学库 libm 时应添加的选项是（　）。A. -Lm　B. -lm　C. -lm.so　D. -lm.a —— **答案：B**。
>
> **知识点：**
> - `-I` 头文件路径、`-L` 库路径、`-l` 库名（去掉前缀 lib 和后缀 .so/.a）。
> - 动态库运行时搜索路径可用 `LD_LIBRARY_PATH` 或 `/etc/ld.so.conf` 配置。

**36. 使用scp从服务器10.1.0.2下下载目录 `/opt/www/` 到本地目录 `/opt` 下的命令是(  )**
<table>
  <tr><td width="50%">A. scp -r /opt/ root@10.1.0.2:/opt/www/</td><td>B. scp root@10.1.0.2:/opt/www/ /opt/</td></tr>
  <tr><td>C. scp -r root@10.1.0.2:/opt/www/ /opt/</td><td>D. scp /opt /root@10.1.0.2:/opt/www/</td></tr>
</table>


> **答案：C**
>
> **解析：**下载目录需要 `-r` 递归选项，远程路径格式为 `用户@主机:路径`，本地目标目录为 `/opt/`，因此正确命令是 `scp -r root@10.1.0.2:/opt/www/ /opt/`。
>
> **类似题：**把本地文件 a.txt 上传到服务器 10.0.0.2 的 /root 目录，正确的命令是（　）。A. scp a.txt root@10.0.0.2:/root/　B. scp root@10.0.0.2:/root/ a.txt　C. scp -r a.txt /root/　D. cp a.txt 10.0.0.2:/root/ —— **答案：A**。
>
> **知识点：**
> - scp 语法：`scp [选项] 源 目标`，源或目标带 `用户@主机:` 即为远程。
> - 传输目录必须加 `-r`；`-P` 指定 SSH 端口。

**37. 在GDB调试程序时，若需要查看当前断点处的堆栈情况应输入命令(  )**
<table>
  <tr><td width="50%">A. l</td><td>B. bt</td></tr>
  <tr><td>C. s</td><td>D. disp</td></tr>
</table>


> **答案：B**
>
> **解析：**GDB 中 `bt`（backtrace）显示当前函数调用栈（各层栈帧、函数名、参数）；`l` 列源码、`s` 单步进入、`disp` 自动显示表达式。
>
> **类似题：**GDB 中切换到上一层栈帧的命令是（　）。A. up　B. down　C. bt　D. list —— **答案：A**。
>
> **知识点：**
> - 调用栈相关：`bt` 查看、`frame n` 选择栈帧、`up`/`down` 上下切换、`info frame` 查看当前帧信息。
> - 栈帧中可查看局部变量、参数和返回地址。

**38. 为了使生成的目标文件能够用于GDB调试，在编译时GCC应使用(  )选项。**
<table>
  <tr><td width="50%">A. -c</td><td>B. -w</td></tr>
  <tr><td>C. -g</td><td>D. -o</td></tr>
</table>


> **答案：C**
>
> **解析：**要使生成的目标文件可被 GDB 调试，编译时必须使用 `-g` 选项加入调试信息；`-c` 只编译不链接，`-w` 关闭警告，`-o` 指定输出文件名。
>
> **类似题：**编译并调试程序的正确流程是（　）。A. gcc -c test.c 后直接 gdb test　B. gcc -g test.c -o test 后 gdb test　C. gcc -O2 test.c 后 gdb a.out　D. gcc -w test.c 后 gdb test —— **答案：B**。
>
> **知识点：**
> - `-g` 生成调试符号表；`-O` 优化可能影响调试时的变量观察。
> - 常用组合：`gcc -g -Wall test.c -o test`。

**39. Apache服务器是(  )**
<table>
  <tr><td width="50%">A. DNS服务器</td><td>B. Web服务器</td></tr>
  <tr><td>C. FTP服务器</td><td>D. Sendmail服务器</td></tr>
</table>


> **答案：B**
>
> **解析：**Apache 是最流行的 Web 服务器软件，提供 HTTP 服务；DNS 服务器如 BIND/named；FTP 服务器如 vsftpd；Sendmail 是邮件服务器。
>
> **类似题：**Apache 服务对应的进程/服务名通常是（　）。A. apache　B. httpd　C. www　D. webd —— **答案：B**。
>
> **知识点：**
> - Apache 主配置文件：`/etc/httpd/conf/httpd.conf`（RHEL 系）。
> - 默认网站根目录：`/var/www/html`；默认端口 80。

**40. 以下不是开源的Web服务器的是(  )**
<table>
  <tr><td width="50%">A. Apache</td><td>B. Nginx</td></tr>
  <tr><td>C. Tomcat</td><td>D. IIS</td></tr>
</table>

> **答案：D**
>
> **解析：**Apache、Nginx、Tomcat 都是开源的 Web 服务器/容器；IIS（Internet Information Services）是微软 Windows 平台上的商业 Web 服务器，不是开源的。
>
> **类似题：**下列属于商业 Web 服务器的是（　）。A. Apache　B. Nginx　C. Tomcat　D. IIS —— **答案：D**。
>
> **知识点：**
> - Apache：模块化、功能全面；Nginx：高并发、反向代理强；Tomcat：Servlet/JSP 容器。
> - 三者均可运行在 Linux 上，IIS 仅运行在 Windows 上。


## 二、填空题（共 10 题）

41. 多道程序环境下，操作系统分配资源以\_\_\_\_\_\_\_\_\_\_为基本单位。

> **答案：进程**
>
> **解析：**在多道程序环境下，进程既是资源分配的基本单位，也是独立运行的基本单位；引入线程后，进程仍是资源分配的基本单位，线程成为处理器调度的最小单位。
>
> **类似题：**现代操作系统中，处理器调度运行的最小单位是（　）。A. 进程　B. 线程　C. 程序　D. 作业 —— **答案：B**。
>
> **知识点：**
> - 进程 = 程序 + 数据 + 进程控制块（PCB）。
> - 进程与线程：线程共享进程资源，切换开销更小。

42. \_\_\_\_\_\_\_\_\_\_是用来连接多条命令的，前一个命令的输出是下一个命令的输入。

> **答案：管道符（管道）**
>
> **解析：**管道符 `|` 把前一个命令的标准输出连接到后一个命令的标准输入，实现命令的组合。例如 `ps -ef | grep sshd`。
>
> **类似题：**命令 `cat file | wc -l` 中，`|` 的作用是（　）。A. 逻辑或　B. 管道　C. 重定向　D. 后台运行 —— **答案：B**。
>
> **知识点：**
> - 管道只传递标准输出（stdout），标准错误需用 `2>&1` 合并。
> - 管道中的每个命令都在独立子进程中执行。

43. 在Linux操作系统中，设备都是作为特殊的\_\_\_\_\_\_\_\_\_\_来访问。

> **答案：文件**
>
> **解析：**Linux 中设备以特殊文件的形式存在于 `/dev` 目录，通过文件接口进行访问，体现“一切皆文件”的设计思想。
>
> **类似题：**Linux 中将设备抽象为文件的好处是（　）。A. 提高 CPU 速度　B. 统一访问接口　C. 增加内存　D. 减少磁盘占用 —— **答案：B**。
>
> **知识点：**
> - 设备文件分为字符设备（c）和块设备（b）。
> - 设备文件通过主、次设备号与驱动程序关联。

44. MVC框架将应用程序分成了三个核心部件：模型、\_\_\_\_\_\_\_\_\_\_、控制器。

> **答案：视图**
>
> **解析：**MVC 框架将应用分为 Model（模型，处理数据与业务逻辑）、View（视图，负责界面展示）、Controller（控制器，接收请求并协调模型与视图）。
>
> **类似题：**MVC 模式中负责接收用户请求并调度的部件是（　）。A. 模型　B. 视图　C. 控制器　D. 数据库 —— **答案：C**。
>
> **知识点：**
> - MVC 的优点：职责分离、便于维护和复用、支持并行开发。
> - 常见实现：Spring MVC、Struts2 等。

45. 命令\_\_\_\_\_\_\_\_\_\_显示文件系统空间使用情况，命令\_\_\_\_\_\_\_\_\_\_显示目录或文件占用磁盘空间量。

> **答案：df；du**
>
> **解析：**`df` 显示各文件系统的空间使用情况（disk free）；`du` 显示目录或文件占用的磁盘空间量（disk usage）。
>
> **类似题：**统计目录 /var 总占用空间并以易读单位显示，应使用（　）。A. df -h /var　B. du -sh /var　C. ls -lh /var　D. free -h —— **答案：B**。
>
> **知识点：**
> - df 关注“文件系统还剩多少”，du 关注“某个目录占了多少”。
> - 两者结果可能有差异，因文件被删除但仍被进程占用等原因。

46. 每块设备文件名由主设备号和从设备号描述。第三块IDE硬盘的设备名为\_\_\_\_\_\_\_\_\_\_，它上面的第二个主分区对应的文件名是\_\_\_\_\_\_\_\_\_\_。

> **答案：hdc；hdc2**
>
> **解析：**第三块 IDE 硬盘的设备名为 `hdc`（hda、hdb、hdc、hdd 依次对应第一至第四块），其第二个主分区为 `hdc2`。
>
> **类似题：**第四块 IDE 硬盘上第一个主分区的设备文件名是（　）。A. hdd1　B. hdc1　C. hdb4　D. hdd5 —— **答案：A**。
>
> **知识点：**
> - IDE 命名：hda～hdd；SATA/SCSI 命名：sda、sdb……
> - 主分区编号 1～4，逻辑分区从 5 开始。

47. 将光盘 `/dev/cdrom` 卸载的命令是\_\_\_\_\_\_\_\_\_\_。

> **答案：`umount /dev/cdrom`**
>
> **解析：**卸载文件系统使用 `umount`，参数可以是设备名或挂载点，因此 `umount /dev/cdrom` 与 `umount /mnt/cdrom` 均可（取决于实际挂载点）。
>
> **类似题：**卸载挂载在 /mnt 的 /dev/sdb1，正确的命令是（　）。A. umount /mnt　B. mount /mnt　C. umount -a　D. fdisk /mnt —— **答案：A**。
>
> **知识点：**
> - 卸载前先退出挂载目录、停止使用该文件系统的进程，否则提示 target is busy。
> - 可用 `fuser -m 挂载点` 或 `lsof` 查找占用进程。

48. 想知道当前目录的绝对路径，可以在字符界面下输入\_\_\_\_\_\_\_\_\_\_命令。

> **答案：pwd**
>
> **解析：**`pwd`（print working directory）显示当前工作目录的绝对路径，是字符界面下最直接的查看方式。
>
> **类似题：**提示符中 `\w` 的作用是显示（　）。A. 用户名　B. 主机名　C. 当前完整路径　D. 时间 —— **答案：C**。
>
> **知识点：**
> - `pwd -P` 显示物理路径（解析符号链接后的真实路径）。
> - 环境变量 `PWD` 中也保存当前目录。

49. 统计当前有多少用户正在使用系统的一行命令是\_\_\_\_\_\_\_\_\_\_。

> **答案：`who | wc -l`**
>
> **解析：**`who` 列出当前登录系统的用户（每行一个），通过管道交给 `wc -l` 统计行数，即当前登录用户数。
>
> **类似题：**统计当前登录用户数还可以使用（　）。A. users | wc -w　B. who | wc -c　C. last | wc -l　D. w | wc -c —— **答案：A**。
>
> **知识点：**
> - `who` 显示登录用户、终端、登录时间；`w` 额外显示用户正在执行的命令；`last` 显示历史登录记录。
> - `wc -l` 统计行数、`wc -w` 统计单词数。

50. 用GDB调试时，如果想把变量 `i=6` 在15行设置断点，应输入命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`b 15 if i==6`**（或 `break 15 if i==6`）
>
> **解析：**GDB 中设置条件断点的语法为 `break 行号 if 条件`，即只有条件成立时才在第 15 行中断，常用于循环中定位特定次数的执行。
>
> **类似题：**在 GDB 中查看第 15 行已设置断点的信息，可使用（　）。A. info breakpoints　B. list 15　C. print 15　D. run 15 —— **答案：A**。
>
> **知识点：**
> - 断点管理：`break`/`b` 设置、`info break` 查看、`delete 编号` 删除、`disable/enable 编号` 禁用/启用。
> - 条件断点对循环调试非常有用。


## 三、综合应用题（共 2 题）

**1. 假设某用户所在客户端IP地址为10.1.1.60，服务器IP地址为10.1.1.5。该用户要以zhangsan的身份ssh远程登录到服务器上，编写和运行一个C语言程序。**
(1) 首先，用户应输入如下命令登录服务器：
`[root@10.1.1.60]#` 【51】\_\_\_\_\_\_\_\_\_\_
(2) 为实现免密登录，可用【52】\_\_\_\_\_\_\_\_\_\_命令生成密钥对，在客户端保存私钥，服务器端保存公钥。
(3) 登录服务器后，用户启动emacs，先按 `<Ctrl>+x` 组合键，再按 `<Ctrl>`+【53】\_\_\_\_\_\_\_\_\_\_组合键，新建文件 `test.c`。
(4) 编辑文件后按 `<Ctrl>+x` 和 `<Ctrl>`+【54】\_\_\_\_\_\_\_\_\_\_组合键保存并退出emacs。
(5) 接着用户编辑 `makefile` 文件如下：
```makefile
test: test.o
    gcc -o test test.o
test.o: test.c
    【55】__________
clean:
    【56】__________ test test.o
```
(6) 执行make命令进行编译，编译成功后输入如下命令在后台运行test程序：
`[zhangsan@10.1.1.5 ~]$` 【57】\_\_\_\_\_\_\_\_\_\_
(7) 用户使用【58】\_\_\_\_\_\_\_\_\_\_命令查看当前登录产生的进程（如下所示）：
```
F S UID PID PPID C PRI NI ADDR SZ WCHAN TTY TIME CMD
0 S 1000 1555 1554 0 80 0 - 28893 do_wai pts/0 00:00:00 bash
0 S 1000 1677 1555 0 80 0 - 7774 futex_ pts/0 00:00:00 test
0 R 1000 1681 1555 0 80 0 - 38301 - pts/0 00:00:00 ps
```
(8) 由于 `test` 程序执行时间过长，用户输入以下命令强制结束该进程：
`[zhangsan@10.1.1.5 ~]$` 【59】\_\_\_\_\_\_\_\_\_\_
(9) 之后执行【60】\_\_\_\_\_\_\_\_\_\_清理编译生成的可执行程序和中间文件。


> **参考答案与解析：**
>
> - 【51】`ssh zhangsan@10.1.1.5` —— 以 zhangsan 身份登录服务器。
> - 【52】`ssh-keygen` —— 生成密钥对（默认 RSA），私钥留客户端，公钥放服务器。
> - 【53】`f` —— Emacs 中 `C-x C-f` 打开/新建文件。
> - 【54】`s`（参考答案）—— `C-x C-s` 保存文件。严格来说题目要求“保存并退出”，退出应再用 `C-x C-c`；若只填一个字母且强调退出，可填 `c`（退出时会提示保存）。
> - 【55】`gcc -c test.c` —— 由 test.c 生成目标文件 test.o（-c 只编译不链接）。
> - 【56】`rm` —— clean 目标中删除可执行文件和中间文件，如 `rm test test.o`。
> - 【57】`./test &` —— 命令后加 `&` 表示在后台运行。
> - 【58】`ps -l` —— 输出为长格式（含 F、S、UID、PID、PPID、PRI、NI 等字段）。
> - 【59】`kill -9 1677`（或 `kill 1677`）—— 根据输出，test 进程的 PID 为 1677，强制结束用 -9。
> - 【60】`make clean` —— 执行 Makefile 中的 clean 目标清理编译产物。
>
> **解析要点：**
> - `&` 后台运行，`jobs` 查看后台任务，`fg`/`bg` 前后台切换，`Ctrl+Z` 挂起。
> - `ps -l` 与 `ps -le` 的区别：-e 显示所有进程，-l 只是长格式。
> - `kill -9` 发送 SIGKILL，进程无法忽略，属于强制结束。
>
> **变式练习：**查看当前 shell 下的后台任务应使用（　）。A. jobs　B. ps -l　C. top　D. bg —— **答案：A**。
>
> **知识点：**
> - 后台运行：`命令 &`；`nohup 命令 &` 可在退出终端后继续运行。
> - Makefile 中 clean 通常写为：`rm -f test test.o`。

**2. 编写shell脚本实现石头、剪刀、布游戏如下:**
```bash
【61】__________
#编写脚本,实现人机<石头,剪刀,布>游戏
#出拳的可能性保存在一个数组中，game[0],game[1],game[2]分别是3中不同的可能
game=(石头 剪刀 布)
#通过随机数获取计算机的出拳
num=【62】__________
#通过num和game确定计算机的出拳
computer=【63】__________
echo "请根据下列提示选择您的出拳手势"
echo "1.石头"
echo "2.剪刀"
echo "3.布"
【64】__________ "请选择1-3:" person
case $person 【65】__________
1)
    if [ $num -eq 0 ]
    then
        echo "平局"
    elif [ $num -eq 1 ]
    then
        echo "你赢"
    else
        echo "计算机赢"
    fi;;
2)
    if [ $num -eq 0 ]
    then
        【66】__________
    elif [ $num -eq 1 ]
    then
        【67】__________
    else
        【68】__________
    fi;;
3)
    if [ $num -eq 0 ]
    then
        echo "你赢"
    elif [ $num -eq 1 ]
    then
        echo "计算机赢"
    else
        echo "平局"
    fi;;
【69】__________
    echo "必须输入 1-3 的数字"
【70】__________
#显示计算机的出拳
echo "计算机的出拳是:" $computer
```

> **参考答案与解析：**
>
> - 【61】`#!/bin/bash` —— 指定脚本解释器。
> - 【62】`$[RANDOM%3]`（或 `$(($RANDOM%3))`）—— 产生 0、1、2 的随机数，作为数组下标。
> - 【63】`${game[$num]}` —— 取出数组中对应的手势（石头/剪刀/布）。
> - 【64】`read -p` —— 带提示读取用户输入到变量 person。
> - 【65】`in` —— case 语句的语法关键字。
> - 【66】`echo "计算机赢"` —— 用户出剪刀(2)、电脑出石头(0)，电脑赢。
> - 【67】`echo "平局"` —— 用户出剪刀(2)、电脑出剪刀(1)，平局。
> - 【68】`echo "你赢"` —— 用户出剪刀(2)、电脑出布(2)，用户赢。
> - 【69】`*)` —— case 的默认分支，处理非法输入。
> - 【70】`esac` —— 结束 case 语句。
>
> **解析要点：**
> - 数组定义：`game=(石头 剪刀 布)`，下标从 0 开始，`${game[0]}` 为石头。
> - case 语法：`case 变量 in 模式) 命令;; ... *) 默认命令;; esac`。
> - `read -p "提示" 变量` 一次完成提示与读取。
>
> **变式练习：**Shell 中定义数组并取出第一个元素，正确的写法是（　）。A. `arr=(a b c); echo $arr[0]`　B. `arr=(a b c); echo ${arr[0]}`　C. `arr=a,b,c; echo $arr(0)`　D. `arr=[a b c]; echo $arr{0}` —— **答案：B**。
>
> **知识点：**
> - 数组：`${arr[@]}` 所有元素、`${#arr[@]}` 元素个数、`${arr[$i]}` 指定下标。
> - 随机数：`$RANDOM`（0～32767），取模可限定范围。
> - 多分支：`case...esac` 比多重 if 更清晰。


---

# 全国计算机等级考试Linux应用与开发技术试题（四）

## 一、单项选择题（共 40 题）

**1. Linux是“free software”，free的含义是(  )**
<table>
  <tr><td width="50%">A. 使用Linux不需要付费</td><td>B. Linux发行商不能向任何用户收费</td></tr>
  <tr><td>C. Linux可以自由修改和发布</td><td>D. 只有Linux开发者才能向用户收费</td></tr>
</table>


> **答案：C**
>
> **解析：**自由软件（free software）中的 free 指“自由”，即用户有运行、复制、研究、修改和再发布软件的自由，而不是指价格免费；Linux 遵循 GPL 协议，可以自由修改和发布。
>
> **类似题：**GNU GPL 协议的核心要求是（　）。A. 软件必须免费　B. 发布修改后的软件必须公开源代码　C. 禁止商业使用　D. 禁止修改 —— **答案：B**。
>
> **知识点：**
> - 自由软件四大自由：运行、研究修改、再发布、改进后发布。
> - Linux 内核采用 GPLv2 协议；开源不等于免费，也不等于无版权。

**2. 改变bash的提示符实际上就是改变变量(  )**
<table>
  <tr><td width="50%">A. $HOME</td><td>B. $PWD</td></tr>
  <tr><td>C. $PS1</td><td>D. $PS2</td></tr>
</table>


> **答案：C**
>
> **解析：**bash 的主命令提示符由环境变量 `PS1` 定义，修改 PS1 即改变提示符；`PS2` 是续行提示符；`$HOME` 是主目录；`$PWD` 是当前目录。
>
> **类似题：**将提示符设置为 `[用户名@主机名 当前目录]$` 形式的变量是（　）。A. PS2　B. PATH　C. PS1　D. HOME —— **答案：C**。
>
> **知识点：**
> - PS1 常用转义：`\u` 用户、`\h` 主机名、`\w` 完整路径、`\$` 提示符、`\t` 时间。
> - 修改后写入 `~/.bashrc` 可持久生效。

**3. 下列对shell变量FRUIT的操作中，正确的是(  )**
<table>
  <tr><td width="50%">A. 为变量赋值：$FRUIT=apple</td><td>B. 显示变量的值：fruit=apple</td></tr>
  <tr><td>C. 显示变量的值：echo $FRUIT</td><td>D. 判断变量是否有值：[ -f $FRUIT ]</td></tr>
</table>


> **答案：C**
>
> **解析：**显示变量值使用 `echo $变量名`，即 `echo $FRUIT`；赋值时不能写 `$`（`$FRUIT=apple` 错误）；`[ -f $FRUIT ]` 是判断文件是否存在，不是判断变量是否有值（判断变量可用 `[ -n "$FRUIT" ]`）。
>
> **类似题：**下列 Shell 变量赋值正确的是（　）。A. $NAME=tom　B. NAME = tom　C. NAME=tom　D. NAME:tom —— **答案：C**。等号两边不能有空格，变量名不加 $。
>
> **知识点：**
> - 赋值：`变量=值`；取值：`$变量` 或 `${变量}`。
> - `unset 变量` 删除变量；`readonly 变量` 设为只读。

**4. 下面哪项不属于shell中的通配符？(  )**
<table>
  <tr><td width="50%">A. *</td><td>B. ?</td></tr>
  <tr><td>C. #</td><td>D. [ ]</td></tr>
</table>


> **答案：C**
>
> **解析：**Shell 常用通配符有 `*`（任意多个字符）、`?`（任意单个字符）、`[ ]`（字符集合）。`#` 在 Shell 中是注释符号，不是通配符。
>
> **类似题：**下列符号中表示“任意单个字符”的通配符是（　）。A. *　B. ?　C. #　D. & —— **答案：B**。
>
> **知识点：**
> - `*` 任意多个字符、`?` 单个字符、`[abc]` 集合内任一字符、`[!abc]` 集合外任一字符。
> - `#` 注释、`&` 后台运行、`;` 命令分隔。

**5. 下列变量名中有效的shell变量名是(  )**
<table>
  <tr><td width="50%">A. -2-time</td><td>B. _2$3</td></tr>
  <tr><td>C. trust_no_1</td><td>D. 2004file</td></tr>
</table>


> **答案：C**
>
> **解析：**Shell 变量名只能由字母、数字和下划线组成，且不能以数字开头。`trust_no_1` 合法；`-2-time` 含非法字符 `-` 且以数字开头，`_2$3` 含 `$`，`2004file` 以数字开头。
>
> **类似题：**下列变量名合法的是（　）。A. 2var　B. my-var　C. my_var　D. my var —— **答案：C**。
>
> **知识点：**
> - 变量名区分大小写；习惯上环境变量用大写，普通变量用小写。
> - 给变量赋值时等号两边不能有空格。

**6. 从后台启动进程，应在命令的结尾加上符号(  )**
<table>
  <tr><td width="50%">A. &</td><td>B. @</td></tr>
  <tr><td>C. #</td><td>D. $</td></tr>
</table>


> **答案：A**
>
> **解析：**命令末尾加 `&` 表示把命令放到后台执行，Shell 会立即返回提示符；`@`、`#`、`$` 均没有后台运行的含义。
>
> **类似题：**将命令 `sleep 100` 放到后台运行，正确的写法是（　）。A. sleep 100 &　B. sleep 100 #　C. sleep 100 @　D. bg sleep 100 —— **答案：A**。
>
> **知识点：**
> - 后台任务管理：`jobs` 查看、`fg %n` 调到前台、`bg %n` 继续后台、`Ctrl+Z` 挂起。
> - `nohup 命令 &` 可让进程在退出终端后继续运行。

**7. 在Linux中，每个进程都由一个唯一的ID号来标识，这个ID是(  )**
<table>
  <tr><td width="50%">A. NID</td><td>B. PID</td></tr>
  <tr><td>C. UID</td><td>D. CID</td></tr>
</table>


> **答案：B**
>
> **解析：**Linux 中每个进程都有唯一的进程号 PID（Process ID）；UID 是用户 ID，NID、CID 不是进程标识。
>
> **类似题：**查看进程的父进程号字段是（　）。A. PID　B. PPID　C. UID　D. GID —— **答案：B**。
>
> **知识点：**
> - PID 1 通常是 init/systemd，是所有进程的祖先。
> - `$$` 表示当前 Shell 的 PID；`$!` 表示最后一个后台进程的 PID。

**8. 下列关于链接描述，错误的是(  )**
<table>
  <tr><td width="50%">A. 硬链接就是让链接文件的i节点号指向被链接文件的i节点</td><td>B. 硬链接和符号链接都是产生一个新的i节点</td></tr>
  <tr><td>C. 链接分为硬链接和符号链接</td><td>D. 硬链接不能链接目录文件</td></tr>
</table>


> **答案：B**
>
> **解析：**硬链接与被链接文件共享同一个 inode，不会产生新 inode；符号链接是新建一个文件（有独立 inode），内容为目标路径。因此“硬链接和符号链接都产生新 i 节点”的说法错误。A、C、D 均正确。
>
> **类似题：**关于软链接与硬链接，下列说法正确的是（　）。A. 硬链接可以跨文件系统　B. 软链接可以链接目录　C. 硬链接有独立 inode　D. 删除源文件后硬链接失效 —— **答案：B**。
>
> **知识点：**
> - 硬链接：`ln 源 目标`，共享 inode，不能跨分区、不能链接目录。
> - 软链接：`ln -s 源 目标`，独立 inode，可跨分区、可链接目录，源删除后失效。

**9. 对file文件执行 `chmod 551 file` 进行了修改，则它的权限是(  )**
<table>
  <tr><td width="50%">A. -rwxr-xr-x</td><td>B. -rwxr--r--</td></tr>
  <tr><td>C. -r--r--r--</td><td>D. -r-xr-x--x</td></tr>
</table>


> **答案：D**
>
> **解析：**551 对应：所有者 5=r-x，同组用户 5=r-x，其他用户 1=--x，即 `r-xr-x--x`（对应选项 D 去掉文件类型后的权限部分）。
>
> **类似题：**权限 `rwxr-x---` 对应的八进制数是（　）。A. 750　B. 755　C. 705　D. 640 —— **答案：A**。
>
> **知识点：**
> - 数字权限：r=4、w=2、x=1，三位依次为 u、g、o。
> - `chmod 551 file` 与 `chmod u=rx,g=rx,o=x file` 等价。

**10. Linux文件系统的文件都按其作用分门别类地存放在相应的目录中，对于外部设备文件，一般应将其放在(  )目录中。**
<table>
  <tr><td width="50%">A. /bin</td><td>B. /etc</td></tr>
  <tr><td>C. /dev</td><td>D. /lib</td></tr>
</table>


> **答案：C**
>
> **解析：**Linux 中所有设备文件（外部设备）都存放在 `/dev` 目录下；`/bin` 存命令，`/etc` 存配置，`/lib` 存库文件。
>
> **类似题：**`/dev/sda` 表示（　）。A. 第一个 SCSI/SATA 磁盘　B. 第一个 IDE 磁盘　C. 光盘　D. 终端设备 —— **答案：A**。
>
> **知识点：**
> - 常见设备文件：/dev/sda（磁盘）、/dev/tty（终端）、/dev/null（丢弃数据）、/dev/zero。
> - 设备由主设备号（类型）和次设备号（实例）标识。

**11. 超级用户root当前所在目录为 `/usr/local`，键入 `cd` 命令后，用户当前所在目录为(  )**
<table>
  <tr><td width="50%">A. /home</td><td>B. /root</td></tr>
  <tr><td>C. /home/root</td><td>D. /usr/local</td></tr>
</table>


> **答案：B**
>
> **解析：**root 用户的主目录是 `/root`，执行 `cd`（不带参数）会回到当前用户的主目录，因此 root 执行后进入 `/root`。
>
> **类似题：**普通用户 student 执行 `cd` 后进入的目录是（　）。A. /root　B. /home/student　C. /　D. /tmp —— **答案：B**。
>
> **知识点：**
> - 单独 `cd`、`cd ~` 都回到当前用户主目录。
> - root 的提示符为 `#`，普通用户为 `$`，可用于快速区分身份。

**12. 分页显示当前目录下所有文件的文件名和目录名、用户组、用户、文件大小、文件或目录权限、文件创建时间等信息的命令是(  )**
<table>
  <tr><td width="50%">A. more ls -al</td><td>B. more -al ls</td></tr>
  <tr><td>C. more < ls -al</td><td>D. ls -al | more</td></tr>
</table>


> **答案：D**
>
> **解析：**分页显示需要把 `ls -al` 的输出通过管道交给 `more`，即 `ls -al | more`。`more ls -al` 是把 ls 和 -al 当作文件名，`more < ls -al` 语法错误。
>
> **类似题：**分页查看 /etc/passwd 内容，正确的命令是（　）。A. more < cat /etc/passwd　B. cat /etc/passwd | more　C. more /etc/passwd | cat　D. cat more /etc/passwd —— **答案：B**。
>
> **知识点：**
> - `ls -al`：-a 显示隐藏文件，-l 长格式。
> - 管道 `|` 将前一个命令的输出作为后一个命令的输入，常配合 more/less/grep/wc 使用。

**13. 系统中有一用户user1和user2，同属于users组。在user1用户目录下有一文件file1，如果user2用户想修改user1用户目录下的file1文件，应拥有(  )权限。**
<table>
  <tr><td width="50%">A. 744</td><td>B. 664</td></tr>
  <tr><td>C. 646</td><td>D. 746</td></tr>
</table>


> **答案：B**
>
> **解析：**user1、user2 同属 users 组，user2 要修改 user1 主目录下的 file1，需要该文件对“同组用户”开放读写权限，即组权限为 6；所有者权限保持读写 6，其他用户只读 4，因此设为 664 最合适。
>
> **类似题：**要使同组用户能读写文件、其他用户无任何权限，权限应设为（　）。A. 600　B. 660　C. 606　D. 666 —— **答案：B**。
>
> **知识点：**
> - 注意：能否进入 user1 主目录还取决于主目录的执行权限，实际使用中还需目录权限配合。
> - `chmod g+rw file` 也可实现同组读写。

**14. 假设umask为002，则新建立的文件的权限是(  )**
<table>
  <tr><td width="50%">A. rw-rw-r--</td><td>B. rwxrwx-w-</td></tr>
  <tr><td>C. -------w-</td><td>D. rwxrwxr-x</td></tr>
</table>


> **答案：A**
>
> **解析：**新建普通文件的基准权限为 666，umask 为 002 时实际权限 = 666 - 002 = 664，即 `rw-rw-r--`。注意目录的基准权限是 777，相同 umask 下新建目录权限为 775。
>
> **类似题：**umask 为 022 时，新建文件的默认权限是（　）。A. 644　B. 755　C. 666　D. 600 —— **答案：A**。
>
> **知识点：**
> - umask 是“权限掩码”，表示要从基准权限中去掉的位。
> - 查看：`umask`；设置：`umask 022`。

**15. 下面哪个命令不是磁盘管理命令？(  )**
<table>
  <tr><td width="50%">A. dd</td><td>B. du</td></tr>
  <tr><td>C. df</td><td>D. fsck</td></tr>
</table>


> **答案：A**
>
> **解析：**`du`（统计目录/文件占用）、`df`（查看文件系统空间）、`fsck`（文件系统检查修复）都属于磁盘管理命令；`dd` 是数据复制/转换命令（convert and copy），主要功能是复制文件和转换数据格式，不属于典型的磁盘管理命令。
>
> **类似题：**用于检查并修复文件系统的命令是（　）。A. fsck　B. du　C. dd　D. df —— **答案：A**。
>
> **知识点：**
> - `dd if=源 of=目标 bs=块大小 count=数量` 常用于备份 MBR、制作镜像。
> - `fsck` 一般在卸载文件系统后运行，否则可能损坏数据。

**16. 在ps的返回信息中，其中stat标记位显示为z，该显示表示该进程(  )**
<table>
  <tr><td width="50%">A. 正在运行</td><td>B. 僵死</td></tr>
  <tr><td>C. 睡眠</td><td>D. 停止</td></tr>
</table>


> **答案：B**
>
> **解析：**`ps` 输出中 STAT（状态）字段的常见取值：R 运行、S 可中断睡眠、D 不可中断睡眠、T 停止、Z 僵死（zombie）。因此 z 表示僵死进程。
>
> **类似题：**ps 状态字段中 `S` 表示（　）。A. 运行　B. 睡眠　C. 停止　D. 僵死 —— **答案：B**。
>
> **知识点：**
> - 僵死进程：子进程已终止但父进程未回收其 PCB，用 `ps` 可看到 defunct。
> - 处理方法：结束其父进程，由 init 收养回收。

**17. 编辑和编译在产生许多临时文件，所以在源代码软件安装包完成后，通常使用命令(  )清除临时文件。**
<table>
  <tr><td width="50%">A. make tidy</td><td>B. make close</td></tr>
  <tr><td>C. make clear</td><td>D. make clean</td></tr>
</table>


> **答案：D**
>
> **解析：**源码安装后清理编译生成的临时文件使用 `make clean`，它执行 Makefile 中定义的 clean 目标；`make tidy`、`make close`、`make clear` 都不是通用目标。
>
> **类似题：**Makefile 中 clean 目标通常定义为（　）。A. `clean: rm -f *.o`　B. `clean: rm -f *.o`（需 Tab 缩进）　C. `rm -f *.o clean`　D. `clean = rm -f *.o` —— **答案：B**。命令必须以 Tab 开头。
>
> **知识点：**
> - 伪目标不生成文件，常用 `.PHONY: clean` 声明。
> - `make` 默认执行第一个目标；`make 目标名` 执行指定目标。

**18. 匹配以 `.001` 结尾的行，可以使用正则表达式(  )**
<table>
  <tr><td width="50%">A. ^001</td><td>B. 001$</td></tr>
  <tr><td>C. *001</td><td>D. /001</td></tr>
</table>


> **答案：B**
>
> **解析：**正则表达式中 `$` 表示行尾锚点，`001$` 匹配以 001 结尾的行；`^001` 匹配以 001 开头的行，`*001` 中 `*` 表示前面字符重复，`/001` 是普通字符。
>
> **类似题：**匹配以 root 开头的行，应使用正则表达式（　）。A. root$　B. ^root　C. *root　D. root* —— **答案：B**。
>
> **知识点：**
> - 锚点：`^` 行首、`$` 行尾；`.` 任意字符、`*` 前导字符重复 0 次以上、`[ ]` 字符集合。
> - 若要匹配字面意义的点号应写成 `\.`，如 `\.001$`。

**19. 每个硬盘主分区的个数最多可以有(  )**
<table>
  <tr><td width="50%">A. 1个</td><td>B. 3个</td></tr>
  <tr><td>C. 4个</td><td>D. 无限制</td></tr>
</table>


> **答案：C**
>
> **解析：**MBR 分区表结构中，分区表项共 4 个，因此一块硬盘最多可划分 4 个主分区；如需更多分区，可用“3 个主分区 + 1 个扩展分区 + 若干逻辑分区”的方式。
>
> **类似题：**逻辑分区的编号从（　）开始。A. 1　B. 4　C. 5　D. 0 —— **答案：C**。
>
> **知识点：**
> - 主分区 1～4；扩展分区占用一个主分区名额；逻辑分区编号从 5 开始。
> - GPT 分区表最多支持 128 个分区，且支持大于 2TB 的磁盘。

**20. 让普通用户以超级管理员的身份执行文件，应用命令(  )来修改文件权限。**
<table>
  <tr><td width="50%">A. chmod a+v</td><td>B. usermod a+v</td></tr>
  <tr><td>C. chmod u+s</td><td>D. usermod a+s</td></tr>
</table>


> **答案：C**
>
> **解析：**SUID（Set User ID）位使普通用户在执行该文件时临时获得文件所有者的权限，用 `chmod u+s 文件` 设置。`a+v`、`a+s` 等写法不是标准权限操作（`+s` 需配合 u/g）。
>
> **类似题：**设置 SUID 权限后，`ls -l` 中所有者执行位显示为（　）。A. x　B. s　C. r　D. w —— **答案：B**（如 `-rwsr-xr-x`）。
>
> **知识点：**
> - SUID 针对可执行文件，临时获得所有者权限；SGID 使进程临时获得所属组权限，对目录则使新文件继承目录属组。
> - Sticky 位（`o+t`，如 /tmp）限制用户只能删除自己的文件。

**21. 下面关于passwd命令的说法错误的是(  )**
<table>
  <tr><td width="50%">A. 普通用户可以用passwd命令修改自己的密码</td><td>B. 超级用户可以用passwd命令修改自己和其他用户的密码</td></tr>
  <tr><td>C. 普通用户不可以用passwd命令修改其他用户的密码</td><td>D. 普通用户可以用passwd命令修改自己和其他用户的密码</td></tr>
</table>


> **答案：D**
>
> **解析：**普通用户只能修改自己的密码，不能修改其他用户的密码，因此 D 的说法错误。root 可以修改自己和其他所有用户的密码；A、B、C 均正确。
>
> **类似题：**root 锁定用户 tom 密码的命令是（　）。A. passwd -l tom　B. passwd -u tom　C. passwd -d tom　D. passwd tom —— **答案：A**（-u 解锁，-d 清空密码）。
>
> **知识点：**
> - `passwd` 无参数改自己的密码；`passwd 用户名` 需 root 权限。
> - `passwd -S 用户名` 查看密码状态（是否锁定、有效期）。

**22. 当父目录不存在的时候，用mkdir命令创建新目录应添加参数(  )**
<table>
  <tr><td width="50%">A. -P</td><td>B. -d</td></tr>
  <tr><td>C. -f</td><td>D. -p</td></tr>
</table>


> **答案：D**
>
> **解析：**`mkdir -p` 可以递归创建目录，当父目录不存在时自动逐级创建；`-P` 不是标准选项（选项区分大小写），`-d`、`-f` 不是 mkdir 的创建父目录选项。
>
> **类似题：**一次性创建 /data/app/logs 三级目录的命令是（　）。A. mkdir /data/app/logs　B. mkdir -p /data/app/logs　C. mkdir -m /data/app/logs　D. mkdir -v /data/app/logs —— **答案：B**。
>
> **知识点：**
> - `mkdir -m 权限 目录` 创建时指定权限；`mkdir -v` 显示创建过程。
> - `rmdir -p a/b/c` 可递归删除空目录链。

**23. 卸载一个软件包，应用用命令(  )**
<table>
  <tr><td width="50%">A. rpm -i</td><td>B. rpm -e</td></tr>
  <tr><td>C. rpm -q</td><td>D. rpm -V</td></tr>
</table>


> **答案：B**
>
> **解析：**卸载软件包使用 `rpm -e 包名`（erase）；`-i` 安装、`-q` 查询、`-V` 校验。
>
> **类似题：**查询系统中是否安装了名为 dhcp 的软件包，应使用（　）。A. rpm -qa dhcp　B. rpm -q dhcp　C. rpm -e dhcp　D. rpm -ivh dhcp —— **答案：B**。
>
> **知识点：**
> - RPM 包名与包内文件查询：`rpm -ql` 列文件、`rpm -qf` 反查文件所属包。
> - 依赖问题可用 `yum remove 包名` 自动处理。

**24. 观察系统动态进程的命令是(  )**
<table>
  <tr><td width="50%">A. free</td><td>B. top</td></tr>
  <tr><td>C. lastcomm</td><td>D. df</td></tr>
</table>


> **答案：B**
>
> **解析：**`top` 动态实时显示系统进程和资源占用情况，默认按 CPU 占用排序；`free` 看内存、`df` 看磁盘、`lastcomm` 查看已执行命令的历史记录。
>
> **类似题：**在 top 界面中按 CPU 占用率排序，应按下（　）。A. M　B. P　C. q　D. k —— **答案：B**（M 按内存排序，q 退出，k 终止进程）。
>
> **知识点：**
> - top 顶部显示负载、进程数、CPU、内存、swap 汇总信息。
> - `top -d 秒数` 设置刷新间隔，`top -p PID` 只监视指定进程。

**25. 在给定文件中查找与设定条件相符的字符串的命令为(  )**
<table>
  <tr><td width="50%">A. grep</td><td>B. gzip</td></tr>
  <tr><td>C. find</td><td>D. sort</td></tr>
</table>


> **答案：A**
>
> **解析：**`grep` 在文件内容中查找与指定模式匹配的字符串；`gzip` 压缩、`find` 查找文件、`sort` 排序。
>
> **类似题：**在 /var/log/messages 中查找包含“error”的行，应使用（　）。A. grep error /var/log/messages　B. find error /var/log/messages　C. sort error /var/log/messages　D. gzip error /var/log/messages —— **答案：A**。
>
> **知识点：**
> - `grep` 与 `find` 的区别：grep 查“文件内容”，find 查“文件本身”。
> - `grep -r` 递归目录，`grep -E` 扩展正则，`grep -v` 反向匹配。

**26. 引用useradd命令添加新用户时，使用(  )参数可以指定用户目录。**
<table>
  <tr><td width="50%">A. -d</td><td>B. -p</td></tr>
  <tr><td>C. -u</td><td>D. -c</td></tr>
</table>


> **答案：A**
>
> **解析：**`useradd -d 路径 用户名` 用于指定新用户的主目录；`-p` 指定加密密码，`-u` 指定 UID，`-c` 添加注释。
>
> **类似题：**创建用户时指定其登录 Shell 应使用（　）。A. useradd -s /bin/bash tom　B. useradd -d /bin/bash tom　C. useradd -g /bin/bash tom　D. useradd -u /bin/bash tom —— **答案：A**。
>
> **知识点：**
> - useradd 常用选项：`-d` 主目录、`-s` Shell、`-u` UID、`-g` 主组、`-G` 附加组、`-m` 强制建主目录。
> - 修改用 `usermod`，删除用 `userdel`。

**27. Linux缺省的文件系统是(  )**
<table>
  <tr><td width="50%">A. VFAT</td><td>B. ISO9660</td></tr>
  <tr><td>C. EXT系列</td><td>D. NTFS</td></tr>
</table>


> **答案：C**
>
> **解析：**Linux 缺省（默认）文件系统是 EXT 系列，如 ext2、ext3、ext4；VFAT、NTFS 是 Windows 文件系统，ISO9660 是光盘文件系统。
>
> **类似题：**CentOS 7 默认使用的文件系统是（　）。A. ext3　B. ext4　C. xfs　D. vfat —— **答案：C**（RHEL 7 及以后默认 XFS；传统 Linux 教材常以 ext 系列为默认）。
>
> **知识点：**
> - ext4 支持更大文件和分区、日志功能；XFS 擅长处理大文件和高并发。
> - `mkfs.ext4 /dev/sdb1` 用于格式化分区。

**28. 在vi编辑器中，保存并强制退出的命令是(  )**
<table>
  <tr><td width="50%">A. :wq</td><td>B. :wq!</td></tr>
  <tr><td>C. :q!</td><td>D. :quit</td></tr>
</table>


> **答案：B**
>
> **解析：**`:wq!` 表示强制保存并退出，常用于文件只读或权限受限但需要写入的场景；`:wq` 保存退出但可能因只读失败，`:q!` 不保存退出。
>
> **类似题：**只读打开文件后想强制写入并退出，应输入（　）。A. :q!　B. :wq!　C. :w　D. :e! —— **答案：B**。
>
> **知识点：**
> - `:w !sudo tee %` 可在无权限时借助 sudo 保存。
> - `:x` 与 `:wq` 类似，但仅在文件被修改时才写盘。

**29. 退出交互模式的shell，应键入(  )**
<table>
  <tr><td width="50%">A. :wq!</td><td>B. &lt;ctrl&gt;+q</td></tr>
  <tr><td>C. exit</td><td>D. quit</td></tr>
</table>


> **答案：C**
>
> **解析：**退出交互式 Shell 使用 `exit`（或 `logout`，或按 Ctrl+D）；`:wq!` 是 vi 命令，`Ctrl+Q`、`quit` 不是标准退出方式。
>
> **类似题：**结束当前 Shell 会话的快捷键是（　）。A. Ctrl+C　B. Ctrl+Z　C. Ctrl+D　D. Ctrl+L —— **答案：C**。
>
> **知识点：**
> - `exit` 返回上一个 Shell；多层 Shell 可用 `exit` 逐层退出。
> - `logout` 用于登录 Shell 退出，`Ctrl+D` 发送 EOF。

**30. 以下不是vi工作模式的是(  )**
<table>
  <tr><td width="50%">A. 命令模式</td><td>B. 删除模式</td></tr>
  <tr><td>C. 编辑模式</td><td>D. 末行模式</td></tr>
</table>


> **答案：B**
>
> **解析：**vi 的三种工作模式是命令模式、编辑模式（插入模式）和末行模式，没有“删除模式”；删除操作是在命令模式下执行的。
>
> **类似题：**vi 中按 Esc 键的作用是（　）。A. 保存文件　B. 返回命令模式　C. 退出 vi　D. 删除字符 —— **答案：B**。
>
> **知识点：**
> - 命令模式：移动光标、删除、复制、粘贴等。
> - 编辑模式：输入文本；末行模式：执行保存、退出、替换等命令。

**31. 在vi中，输入哪条命令可以撤销上次操作？(  )**
<table>
  <tr><td width="50%">A. d</td><td>B. u</td></tr>
  <tr><td>C. $</td><td>D. x</td></tr>
</table>


> **答案：B**
>
> **解析：**vi 命令模式下 `u`（undo）撤销上一次操作；`d` 是删除操作前缀，`$` 移到行尾，`x` 删除一个字符。
>
> **类似题：**在 vi 中撤销最近多次修改，可以多次按（　）。A. p　B. u　C. r　D. c —— **答案：B**。
>
> **知识点：**
> - `u` 撤销；部分版本支持 `Ctrl+r` 重做。
> - `.` 重复上一次操作，`p` 粘贴。

**32. 在vi编辑器的命令模式下，键入(  )可在光标当前行下添加一新行。**
<table>
  <tr><td width="50%">A. O</td><td>B. o</td></tr>
  <tr><td>C. i</td><td>D. a</td></tr>
</table>


> **答案：B**
>
> **解析：**命令模式下 `o`（小写）在当前行下方插入新行并进入编辑模式；`O`（大写）在当前行上方插入新行；`i` 在光标前插入，`a` 在光标后插入。
>
> **类似题：**在 vi 中要在光标当前行上方插入一个新行，应输入（　）。A. o　B. O　C. i　D. a —— **答案：B**。
>
> **知识点：**
> - 插入类命令：`i` 光标前、`a` 光标后、`o` 下行、`O` 上行、`A` 行尾、`I` 行首。
> - 插入完成后按 Esc 返回命令模式。

**33. 下列哪项命令常用于检测网络主机是否可达？(  )**
<table>
  <tr><td width="50%">A. ssh</td><td>B. netstat</td></tr>
  <tr><td>C. ping</td><td>D. exit</td></tr>
</table>


> **答案：C**
>
> **解析：**`ping` 通过 ICMP 协议检测网络主机是否可达；`ssh` 用于远程登录，`netstat` 查看网络连接，`exit` 退出 Shell。
>
> **类似题：**检测本机到网关 192.168.1.1 是否连通，应使用（　）。A. ping 192.168.1.1　B. ssh 192.168.1.1　C. netstat 192.168.1.1　D. arp 192.168.1.1 —— **答案：A**。
>
> **知识点：**
> - ping 结果中的 ttl 可粗略判断目标操作系统类型。
> - 防火墙可能屏蔽 ICMP 导致 ping 不通，但服务实际可用。

**34. 实现从IP地址到以太网MAC地址转换的命令为(  )**
<table>
  <tr><td width="50%">A. ping</td><td>B. ifconfig</td></tr>
  <tr><td>C. arp</td><td>D. traceroute</td></tr>
</table>


> **答案：C**
>
> **解析：**`arp` 命令用于查看和管理 ARP 缓存，实现 IP 地址到以太网 MAC 地址的转换；`ping` 测连通性，`ifconfig` 配置网卡，`traceroute` 跟踪路由。
>
> **类似题：**查看本机 ARP 缓存表的命令是（　）。A. arp -a　B. arp -d　C. ifconfig -a　D. netstat -a —— **答案：A**。
>
> **知识点：**
> - ARP（Address Resolution Protocol）把 IP 地址解析为 MAC 地址；RARP 相反。
> - ARP 缓存可用 `arp -d IP` 删除、`arp -s IP MAC` 静态绑定。

**35. telnet服务通常使用哪个服务端口？(  )**
<table>
  <tr><td width="50%">A. 20</td><td>B. 21</td></tr>
  <tr><td>C. 22</td><td>D. 23</td></tr>
</table>


> **答案：D**
>
> **解析：**Telnet 服务默认使用 TCP 23 端口；20/21 是 FTP，22 是 SSH。
>
> **类似题：**由于 Telnet 明文传输不安全，通常使用（　）替代它。A. FTP　B. HTTP　C. SSH　D. SMTP —— **答案：C**。
>
> **知识点：**
> - 常见端口：FTP 20/21、SSH 22、Telnet 23、SMTP 25、DNS 53、HTTP 80、POP3 110、HTTPS 443。
> - `/etc/services` 文件中记录了服务与端口的对应关系。

**36. NFS提供的服务是(  )**
<table>
  <tr><td width="50%">A. 文件服务</td><td>B. 远程登录</td></tr>
  <tr><td>C. 共享服务</td><td>D. 配置IP地址</td></tr>
</table>


> **答案：C**
>
> **解析：**NFS（Network File System，网络文件系统）允许网络中的计算机之间共享目录和文件，提供的是文件共享服务；它不是远程登录、也不是分配 IP 的服务。
>
> **类似题：**NFS 服务端共享目录的配置文件是（　）。A. /etc/fstab　B. /etc/exports　C. /etc/nfs.conf　D. /etc/samba/smb.conf —— **答案：B**。
>
> **知识点：**
> - NFS 基于 RPC 机制，服务进程包括 nfs、rpcbind。
> - 客户端通过 `mount -t nfs 服务器:/共享目录 /本地挂载点` 使用共享。

**37. 关于SSH描述正确的是(  )**
<table>
  <tr><td width="50%">A. 需要启动sshd服务</td><td>B. 采用加密的方式保证连接安全</td></tr>
  <tr><td>C. 监听TCP 22端口</td><td>D. 以上都正确</td></tr>
</table>


> **答案：D**
>
> **解析：**SSH 需要服务端启动 sshd 服务，采用加密方式保证连接安全，默认监听 TCP 22 端口，因此 A、B、C 都正确。
>
> **类似题：**启动 SSH 服务的命令是（　）。A. service sshd start　B. service ssh start　C. start sshd　D. sshd on —— **答案：A**。
>
> **知识点：**
> - 服务端配置文件 `/etc/ssh/sshd_config`，客户端 `/etc/ssh/ssh_config`。
> - 免密登录原理：客户端私钥 + 服务端 authorized_keys 中的公钥。

**38. SSH服务端默认的配置文件是(  )**
<table>
  <tr><td width="50%">A. /etc/ssh/sshd.conf</td><td>B. /etc/sshd.conf</td></tr>
  <tr><td>C. /etc/ssh.conf</td><td>D. /etc/ssh/sshd_config</td></tr>
</table>


> **答案：D**
>
> **解析：**OpenSSH 服务端（sshd）的默认配置文件是 `/etc/ssh/sshd_config`；客户端配置文件是 `/etc/ssh/ssh_config`。
>
> **类似题：**修改 SSH 默认端口 22 应编辑的文件是（　）。A. /etc/ssh/ssh_config　B. /etc/ssh/sshd_config　C. /etc/services　D. /etc/sysconfig/sshd —— **答案：B**。
>
> **知识点：**
> - sshd_config 常用配置：Port、PermitRootLogin、PasswordAuthentication、AllowUsers。
> - 修改后重启服务：`service sshd restart`。

**39. 配置Apache服务器需要修改的配置文件为(  )**
<table>
  <tr><td width="50%">A. httpd.conf</td><td>B. access.conf</td></tr>
  <tr><td>C. srm.conf</td><td>D. named.conf</td></tr>
</table>


> **答案：A**
>
> **解析：**Apache 的主配置文件是 `httpd.conf`（RHEL 系路径 `/etc/httpd/conf/httpd.conf`）；`access.conf`、`srm.conf` 是旧版本中曾用的辅助文件；`named.conf` 是 DNS 服务的配置。
>
> **类似题：**Apache 默认网站根目录通常是（　）。A. /var/www/html　B. /etc/httpd　C. /usr/share　D. /home/www —— **答案：A**。
>
> **知识点：**
> - httpd.conf 主要配置项：Listen、ServerName、DocumentRoot、Directory、VirtualHost。
> - 修改配置后可用 `httpd -t` 检查语法，再重启服务。

**40. Apache服务器默认的监听连接端口号是(  )**
<table>
  <tr><td width="50%">A. 1024</td><td>B. 800</td></tr>
  <tr><td>C. 80</td><td>D. 8</td></tr>
</table>

> **答案：C**
>
> **解析：**Apache 服务器默认监听 TCP 80 端口（HTTP 服务）；HTTPS 默认 443，1024、800、8 均不是默认端口。
>
> **类似题：**Apache 默认的配置文件和网站根目录分别是（　）。A. httpd.conf 与 /var/www/html　B. named.conf 与 /var/named　C. smb.conf 与 /home　D. sshd_config 与 /root —— **答案：A**。
>
> **知识点：**
> - 配置 `Listen 8080` 可修改监听端口。
> - 启动服务：`service httpd start`，设置自启：`chkconfig httpd on`。


## 二、填空题（共 10 题）

41. 在Linux的两种链接文件方式中，\_\_\_\_\_\_\_\_\_\_不能实现对目录链接。

> **答案：硬链接**
>
> **解析：**Linux 的两种链接方式为硬链接和软链接（符号链接）。为避免目录树出现环路，系统不允许对目录创建硬链接，只允许创建软链接。
>
> **类似题：**可以跨文件系统创建的链接是（　）。A. 硬链接　B. 软链接　C. 两者都可以　D. 两者都不可以 —— **答案：B**。
>
> **知识点：**
> - 硬链接共享 inode，链接计数增加；软链接有独立 inode。
> - `ln` 创建硬链接，`ln -s` 创建软链接。

42. 在Linux操作系统中，设备文件分为块设备文件和\_\_\_\_\_\_\_\_\_\_设备文件。

> **答案：字符设备**
>
> **解析：**设备文件分为块设备文件和字符设备文件：块设备以数据块为单位随机访问（如硬盘），字符设备以字符流为单位顺序访问（如键盘、打印机）。
>
> **类似题：**下列属于块设备的是（　）。A. 键盘　B. 鼠标　C. 硬盘　D. 打印机 —— **答案：C**。
>
> **知识点：**
> - `ls -l /dev` 中首字符 b 为块设备、c 为字符设备。
> - 设备文件由主设备号和次设备号标识。

43. vi编辑器的默认模式是\_\_\_\_\_\_\_\_\_\_。任何时候，不管用户处于何种工作模式，按下ESC键，即可进入该模式。

> **答案：命令模式**
>
> **解析：**vi 启动后默认进入命令模式；在编辑模式或末行模式下按 Esc 键都会回到命令模式。命令模式下可以执行光标移动、删除、复制、粘贴等操作。
>
> **类似题：**在 vi 的命令模式下输入 `:` 会进入（　）。A. 编辑模式　B. 末行模式　C. 插入模式　D. 替换模式 —— **答案：B**。
>
> **知识点：**
> - 三种模式：命令模式（默认）、编辑模式（i/a/o 进入）、末行模式（: 进入）。
> - Esc 返回命令模式；命令模式下 `:` 进入末行模式。

44. 执行一个shell命令时，通常会自动打开三个标准文件，标准输入文件(stdin)，它对应的设备是\_\_\_\_\_\_\_\_\_\_，另外两个则分别是标准输出文件(stdout)和标准错误输出文件(stderr)。

> **答案：键盘**
>
> **解析：**Shell 执行命令时会自动打开三个标准文件：标准输入（stdin，文件描述符 0，默认设备为键盘）、标准输出（stdout，1，默认终端）、标准错误输出（stderr，2，默认终端）。
>
> **类似题：**标准输出对应的文件描述符是（　）。A. 0　B. 1　C. 2　D. 3 —— **答案：B**。
>
> **知识点：**
> - stdin 0、stdout 1、stderr 2。
> - 重定向：`<` 输入、`>` 输出、`2>` 错误、`2>&1` 合并。

45. 某文件的权限为：`drw-r--r--`，用数值形式表示该权限，则该八进制数为\_\_\_\_\_\_\_\_\_\_。

> **答案：644**
>
> **解析：**`drw-r--r--` 中首字符 d 表示目录，权限部分为 `rw-r--r--`：所有者 6、同组 4、其他 4，八进制表示为 644。
>
> **类似题：**权限字符串 `-rwxr-xr-x` 对应的八进制数是（　）。A. 755　B. 644　C. 750　D. 777 —— **答案：A**。
>
> **知识点：**
> - 文件类型字符不参与权限数值计算。
> - 目录权限中 r 表示可列目录、w 表示可在目录内创建删除文件、x 表示可进入目录。

46. `mv` 命令用来将文件从一个目录移到另一个目录中，还可以对文件\_\_\_\_\_\_\_\_\_\_。

> **答案：重命名**
>
> **解析：**`mv` 命令既可以移动文件/目录到其他位置，也可以在原位置改变文件名（重命名）。例如 `mv a.txt b.txt` 即重命名。
>
> **类似题：**将文件 /tmp/a 重命名为 /tmp/b 的命令是（　）。A. mv /tmp/a /tmp/b　B. cp /tmp/a /tmp/b　C. rename /tmp/a /tmp/b　D. cat /tmp/a > /tmp/b —— **答案：A**。
>
> **知识点：**
> - 同一目录下使用 mv 即重命名；跨目录使用即移动。
> - `mv -i` 覆盖前提示，`mv -f` 强制覆盖，`mv -n` 不覆盖。

47. 在linux系统中，所有的变量都默认是字符串，如果进行算术运算，将变量a和b的值相加，赋给变量c，其语法格式为\_\_\_\_\_\_\_\_\_\_。

> **答案：`c=$(($a+$b))`**（或 `c=$[ $a+$b ]`、`let c=a+b`、`expr $a + $b`）
>
> **解析：**Shell 变量默认按字符串处理，直接 `c=$a+$b` 得到的是字符串拼接。进行算术运算需使用 `$(( ))`、`$[ ]`、`let` 或 `expr` 等。
>
> **类似题：**已知 a=1、b=2，命令 `c=$a+$b` 后 `echo $c` 的输出是（　）。A. 3　B. 12　C. 1+2　D. 报错 —— **答案：C**。
>
> **知识点：**
> - 整数运算：`$((a+b))`、`$[a+b]`、`let`、`expr`。
> - 小数运算需借助 `bc` 或 `awk`。

48. 给出一分命令统计当前工作目录下所有以m开头的C语言源文件的数量\_\_\_\_\_\_\_\_\_\_。

> **答案：`ls m*.c | wc -l`**
>
> **解析：**先用通配符 `m*.c` 匹配所有以 m 开头的 C 源文件，通过管道交给 `wc -l` 统计行数（每个文件名一行），即文件数量。
>
> **类似题：**统计当前目录下 .txt 文件的个数，正确的命令是（　）。A. ls *.txt | wc -l　B. ls *.txt | wc -c　C. find *.txt | wc -c　D. count *.txt —— **答案：A**。
>
> **知识点：**
> - 无匹配时通配符保持原样，可先 `shopt -s nullglob` 或改用 `find`。
> - `find . -name "*.txt" | wc -l` 也可统计（含子目录）。

49. 在linux系统中，用gcc编译c语言的源文件 `f1.c`，生成可执行文件 `f1` 的命令是\_\_\_\_\_\_\_\_\_\_。

> **答案：`gcc f1.c -o f1`**
>
> **解析：**GCC 使用 `-o` 选项指定输出的可执行文件名；若不指定，默认输出 `a.out`。
>
> **类似题：**编译 f1.c 且只生成目标文件 f1.o 的命令是（　）。A. gcc f1.c -o f1.o　B. gcc -c f1.c　C. gcc -S f1.c　D. gcc -E f1.c —— **答案：B**。
>
> **知识点：**
> - `gcc 源文件 -o 可执行文件` 是最常用的编译命令。
> - 多文件编译：`gcc a.c b.c -o app`；分步编译：`gcc -c a.c` 后链接。

50. Linux系统的守护进程及多数日志文件是由\_\_\_\_\_\_\_\_\_\_服务来统一管理的，只要各个进程将信息给予这个服务，它就会自动地把日志按特定的格式记录到不同的日志文件中。

> **答案：rsyslogd**（旧版本为 syslogd）
>
> **解析：**Linux 系统的守护进程及多数日志文件由 `rsyslogd`（syslogd 的增强版）服务统一管理，各进程将日志交给它，由它按配置的格式和设施分类记录到不同的日志文件中。
>
> **类似题：**rsyslog 服务的主配置文件是（　）。A. /etc/rsyslog.conf　B. /etc/syslog.conf　C. /etc/logrotate.conf　D. /var/log/messages —— **答案：A**。
>
> **知识点：**
> - 日志设施（facility）：auth、cron、daemon、kern、mail、user 等。
> - 日志级别：debug 最低，emerg 最高；`*.info` 表示 info 及以上。


## 三、综合应用题（共 2 题）

**1. (1) 当前目录下存放下列文件：**
```bash
[student@localhost~]$ ls
bgi bin doc dosbox email
exam  glasslib  include iomega lib
objs  offices source sys
```
则执行下列命令后，结果是
`[student@localhost~]$ ls [abc]*`
【51】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ ls s[m-ox-z]*`
【52】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ ls ?i?`
【53】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ ls [!a-go-z]*`
【54】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ ls [eo]???`
【55】\_\_\_\_\_\_\_\_\_\_

**(2) 已知脚本sh01能接收用户从键盘输入的若干个整数，然后输出其最大值和最小值，如下：**
```bash
[student@localhost~]$ cat sh01
#! /bin/bash
echo -n "please input the data:"
read x
max=$x
min=$x
while
【56】__________
do
    if 【57】__________
    then
        max=$x
    fi
    if 【58】__________
    then
        min=$x
    fi
done
echo max= $max
echo min=$min
```
查询权限：
`[student@localhost~]$ ls -l sh01`
`5756126 -rw-r--r-- 1 student student 34 2020-1-21 sh01`
则运行该脚本，应执行以下命令：
【59】\_\_\_\_\_\_\_\_\_\_
【60】\_\_\_\_\_\_\_\_\_\_


> **参考答案与解析：**
>
> **（1）通配符匹配结果：**
> - 【51】`bgi bin` —— `[abc]*` 匹配以 a、b、c 开头的文件名。
> - 【52】`source sys` —— `s[m-ox-z]*` 匹配 s 开头、第二个字母在 m～o 或 x～z 之间的文件名。
> - 【53】`bin lib` —— `?i?` 匹配三个字符且中间为 i 的文件名。
> - 【54】`include iomega lib` —— `[!a-go-z]*` 匹配首字母不在 a～g、o～z 范围内（即 h～n）的文件名。
> - 【55】`exam objs` —— `[eo]???` 匹配 e 或 o 开头、总长度为 4 的文件名。
>
> **（2）脚本填空：**
> - 【56】`read x` —— 在 while 循环条件中读取用户输入，读到 EOF 或非法输入时退出循环。
> - 【57】`[ $x -gt $max ]` —— 若当前输入大于当前最大值则更新 max。
> - 【58】`[ $x -lt $min ]` —— 若当前输入小于当前最小值则更新 min。
> - 【59】`chmod u+x sh01`（或 `chmod +x sh01`）—— 脚本原权限为 rw-r--r--，需要增加执行权限。
> - 【60】`./sh01` —— 在当前目录下执行脚本（不加 ./ 时 Shell 会在 PATH 中查找）。
>
> **解析要点：**
> - 字符范围 `[m-o]` 表示 m、n、o；`[x-z]` 表示 x、y、z；`!` 或 `^` 表示取反。
> - `[eo]???` 中 `?` 只匹配一个字符，总长度固定为 4。
> - 数值比较用 `-gt`、`-lt`，字符串比较用 `=`、`!=`。
>
> **变式练习：**`ls [a-c]??` 可以匹配的文件名是（　）。A. abc　B. a1　C. bcd　D. c123 —— **答案：A**（a～c 开头、总长 3）。
>
> **知识点：**
> - 通配符：`*`、`?`、`[ ]`、`[! ]`、`{ }`（大括号展开）。
> - 脚本执行方式：`./脚本`、`bash 脚本`、`source 脚本`；前两者在新进程执行，source 在当前 Shell 执行。

**2. 已知目录状态：**
```bash
[student@localhost~]$ ls
file1 file2 file3 file4
[student@localhost~]$ x=*
```
(1) 则执行下列命令后，得到的输出结果是：
`[student@localhost~]$ echo $x`
【61】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ echo '$x'`
【62】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ echo "$x"`
【63】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ ls file || echo "not exist" && echo "exist"`
【64】\_\_\_\_\_\_\_\_\_\_
【65】\_\_\_\_\_\_\_\_\_\_

(2) 已知 `[student@localhost~]$ nu=aaa`，分别执行下列命令后，得到的输出结果是：
`[student@localhost~]$ nu="$nu"bbb`
`[student@localhost~]$ echo $nu`
【66】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ nu={$nu}bbb`
`[student@localhost~]$ echo $nu`
【67】\_\_\_\_\_\_\_\_\_\_
`[student@localhost~]$ nu=${nu}bbb`
`[student@localhost~]$ echo $nu`
【68】\_\_\_\_\_\_\_\_\_\_
若继续执行
`[student@localhost~]$ nu1=111`
`[student@localhost~]$ nu2=222`
`[student@localhost~]$ nu3=$nu1+$nu2`
`[student@localhost~]$ echo $nu3`
则输出结果为：
【69】\_\_\_\_\_\_\_\_\_\_
继续执行
`[student@localhost~]$ city=(Beijing Shanghai)`
`[student@localhost~]$ echo $city[0]`
则输出结果为：
【70】\_\_\_\_\_\_\_\_\_\_

> **参考答案与解析：**
>
> **（1）通配符与变量引用：**
> - 【61】`file1 file2 file3 file4` —— `$x` 展开为 `*`，再经通配符展开为当前目录所有文件名。
> - 【62】`$x` —— 单引号强引用，`$` 不展开，原样输出。
> - 【63】`*` —— 双引号弱引用，变量展开为 `*`，但引号阻止了通配符展开，因此输出 `*`。
> - 【64】`not exist` —— `ls file` 失败（file 不存在），`||` 执行后面的 echo。
> - 【65】`exist` —— `echo "not exist"` 执行成功，`&&` 继续执行后面的 echo。
>
> **（2）变量拼接：**
> - 【66】`aaabbb` —— `"$nu"` 展开为 aaa，与 bbb 拼接。
> - 【67】`{aaa}bbb` —— `{$nu}` 中花括号不构成有效的花括号展开（无逗号或范围），`$nu` 展开为 aaa，结果原样保留花括号。（注意：若在同一会话中连续执行，此时 nu 已为 aaabbb，结果为 `{aaabbb}bbb`。）
> - 【68】`aaabbb` —— `${nu}` 限定变量名边界后拼接 bbb。
> - 【69】`111+222` —— Shell 默认按字符串处理，未做算术运算。
> - 【70】`Beijing[0]` —— `$city` 只取数组第一个元素，`[0]` 作为普通字符输出；取数组元素应写 `${city[0]}`。
>
> **解析要点：**
> - 单引号强引用：不展开任何变量；双引号弱引用：展开变量但阻止通配符。
> - `||` 前一个命令失败时执行后面；`&&` 前一个命令成功时执行后面。
> - 数组引用必须使用 `${数组名[下标]}`。
>
> **变式练习：**已知 `arr=(a b c)`，`echo $arr[1]` 的输出是（　）。A. b　B. a[1]　C. a　D. 报错 —— **答案：B**（`$arr` 取第一个元素 a，`[1]` 原样输出）。
>
> **知识点：**
> - 变量引用：`$var`、`${var}`；数组：`${arr[i]}`、`${arr[@]}`、`${#arr[@]}`。
> - 逻辑运算符：`&&` 与、`||` 或、`!` 非。
> - `echo` 双引号与单引号在变量展开上的差异是常考点。


---

# 全国计算机等级考试Linux应用与开发技术试题（五）

## 一、单项选择题（共 40 题）

**1. 以下Linux内核版本属于稳定版的是(  )**
<table>
  <tr><td width="50%">A. 4.6.22</td><td>B. 2.5.32</td></tr>
  <tr><td>C. 3.3.21</td><td>D. 4.5.24</td></tr>
</table>


> **答案：A**
>
> **解析：**Linux 内核版本号格式为“主版本号.次版本号.修订号”。按传统规则，次版本号为偶数表示稳定版，奇数表示开发版。4.6.22 的次版本号 6 为偶数，是稳定版；2.5.32、3.3.21、4.5.24 的次版本号 5、3、5 均为奇数，属于开发版。
>
> **类似题：**下列 Linux 内核版本中属于开发版的是（　）。A. 2.4.20　B. 2.6.32　C. 3.3.21　D. 4.6.22 —— **答案：C**。
>
> **知识点：**
> - 内核版本：主版本.次版本.修订号；偶数次版本=稳定版，奇数次版本=开发版（2.6 以后该规则逐渐淡化）。
> - `uname -r` 查看当前内核版本。

**2. 以下不属于Linux发行版本的是(  )**
<table>
  <tr><td width="50%">A. Gentoo</td><td>B. Debian</td></tr>
  <tr><td>C. Solaris</td><td>D. Fedora</td></tr>
</table>


> **答案：C**
>
> **解析：**Gentoo、Debian、Fedora 都是常见的 Linux 发行版；Solaris 是 Sun 公司的 UNIX 操作系统，不属于 Linux 发行版。
>
> **类似题：**下列属于 Linux 发行版的是（　）。A. Solaris　B. AIX　C. Ubuntu　D. HP-UX —— **答案：C**。
>
> **知识点：**
> - 主流发行版：RHEL/CentOS/Fedora、Debian/Ubuntu、openSUSE、Gentoo、Arch。
> - UNIX 系统：Solaris、AIX、HP-UX、FreeBSD（类 UNIX）。

**3. 用来查看当前Linux发行版使用了哪种桌面的环境变量是(  )**
<table>
  <tr><td width="50%">A. DESKTOP_SESSION</td><td>B. DESKTOP</td></tr>
  <tr><td>C. DESKTOP_SHOW</td><td>D. DESKTOP_ENV</td></tr>
</table>


> **答案：A**
>
> **解析：**环境变量 `DESKTOP_SESSION` 记录当前登录会话使用的桌面环境名称（如 gnome、kde）。其他选项不是标准环境变量。
>
> **类似题：**显示当前桌面环境变量值的命令是（　）。A. echo $DESKTOP_SESSION　B. set DESKTOP_SESSION　C. env DESKTOP_SESSION　D. print DESKTOP_SESSION —— **答案：A**。
>
> **知识点：**
> - 查看环境变量：`echo $变量名`、`env`、`printenv`、`set`。
> - 常见桌面环境：GNOME、KDE、XFCE、MATE。

**4. Linux文件有三种时间戳，以下错误的是(  )**
<table>
  <tr><td width="50%">A. mtime</td><td>B. ntime</td></tr>
  <tr><td>C. ctime</td><td>D. atime</td></tr>
</table>


> **答案：B**
>
> **解析：**Linux 文件的三个时间戳是 atime（访问时间）、mtime（修改时间，文件内容变化）、ctime（状态改变时间，元数据变化）。ntime 不是 Linux 文件时间戳。
>
> **类似题：**修改文件内容后，发生变化的时间戳是（　）。A. atime　B. mtime　C. ctime　D. 创建时间 —— **答案：B**（同时 ctime 也会更新）。
>
> **知识点：**
> - `ls -l` 默认显示 mtime，`ls -lu` 显示 atime，`ls -lc` 显示 ctime。
> - `stat 文件` 可查看全部时间信息。

**5. 命令行 `echo -e "ab\bc"` 的结果是(  )**
<table>
  <tr><td width="50%">A. ac</td><td>B. abc</td></tr>
  <tr><td>C. abbc</td><td>D. ab\bc</td></tr>
</table>


> **答案：A**
>
> **解析：**`echo -e` 使转义字符生效。字符串 "ab\bc" 中 `\b` 是退格符：输出 a、b 后光标退回一位，再输出 c 覆盖 b，最终显示 ac。
>
> **类似题：**命令 `echo -e "a\tb"` 的输出是（　）。A. a 和 b 之间有一个制表符　B. a\tb　C. ab　D. 报错 —— **答案：A**。
>
> **知识点：**
> - 常用转义：`\n` 换行、`\t` 制表、`\b` 退格、`\r` 回车、`\\` 反斜杠。
> - 不加 `-e` 时 echo 原样输出转义序列（bash 中可用 `$'...'` 语法）。

**6. 为显示当前已经登录的用户信息，包括用户执行任务的情况，应执行命令(  )**
<table>
  <tr><td width="50%">A. ps</td><td>B. who</td></tr>
  <tr><td>C. who -l</td><td>D. w -s</td></tr>
</table>


> **答案：D**
>
> **解析：**`w` 命令显示当前登录用户及其正在执行的任务（比 who 信息更丰富），`w -s` 使用短格式输出；`who` 只显示登录用户基本信息；`ps` 显示进程。
>
> **类似题：**显示当前登录用户及其正在执行的命令，应使用（　）。A. who　B. ps　C. w　D. last —— **答案：C**。
>
> **知识点：**
> - `who`：登录用户、终端、时间；`w`：额外显示负载、当前执行的命令。
> - `last` 查看历史登录记录，`lastlog` 查看每个用户最近一次登录。

**7. 用 `ps -le` 命令列出系统中所有进程的详细信息如下：**
<style>
  .ps-table {
    border: 1px solid black;
    border-collapse: collapse;
    font-family: monospace;
  }
  .ps-table th,
  .ps-table td {
    border: none;
    padding: 4px 8px;
    text-align: left;
    font-weight: normal; /* 确保所有文字（包括表头）不加粗 */
  }
</style>

<table class="ps-table">
  <!-- 命令提示符行，跨所有列 -->
  <tr>
    <td colspan="14">[root@localhost ~]# ps -le</td>
  </tr>
  <!-- 空行（模拟原输出中的空白行） -->
  <tr>
    <td colspan="14">&nbsp;</td>
  </tr>
  <!-- 表头行 -->
  <tr>
    <th>F</th><th>S</th><th>UID</th><th>PID</th><th>PPID</th><th>C</th>
    <th>PRI</th><th>NI</th><th>ADDR</th><th>SZ</th><th>WCHAN</th>
    <th>TTY</th><th>TIME</th><th>CMD</th>
  </tr>
  <!-- 数据行 -->
  <tr>
    <td>4</td><td>S</td><td>0</td><td>1</td><td>0</td><td>0</td>
    <td>80</td><td>-10</td><td>-</td><td>18</td><td>-</td>
    <td>?</td><td>00:00:01</td><td>init</td>
  </tr>
  <tr>
    <td>1</td><td>I</td><td>0</td><td>3</td><td>2</td><td>0</td>
    <td>60</td><td>15</td><td>-</td><td>12</td><td>-</td>
    <td>?</td><td>00:00:00</td><td>rcu_gp</td>
  </tr>
  <tr>
    <td>5</td><td>S</td><td>0</td><td>63</td><td>2</td><td>0</td>
    <td>80</td><td>-15</td><td>-</td><td>10</td><td>-</td>
    <td>?</td><td>00:00:00</td><td>cpuhp</td>
  </tr>
  <tr>
    <td>1</td><td>S</td><td>0</td><td>12</td><td>2</td><td>0</td>
    <td>9</td><td>-</td><td>-</td><td>8</td><td>-</td>
    <td>?</td><td>00:00:02</td><td>migration</td>
  </tr>
</table>

**优先级最高的进程是(  )**
<table>
  <tr><td width="50%">A. rcu_gp</td><td>B. init</td></tr>
  <tr><td>C. cpuhp</td><td>D. migration</td></tr>
</table>


> **答案：C**
>
> **解析：**Linux 中进程优先级由 PRI 和 NI 共同决定，二者都是数值越小优先级越高。NI（nice 值）中 cpuhp 为 -15，是表中最小（优先级最高）的；init 为 -10 次之；rcu_gp 为 15 优先级最低。因此 cpuhp 优先级最高。
>
> **类似题：**进程 nice 值范围是（　）。A. 0～99　B. -20～19　C. -10～10　D. 1～100 —— **答案：B**。
>
> **知识点：**
> - nice 值越小优先级越高；普通用户只能调大 nice（降低优先级），root 可任意调整。
> - `nice -n 值 命令` 启动时设置，`renice -n 值 -p PID` 调整运行中的进程。

**8. 在bash中，`export` 命令的作用是(  )**
<table>
  <tr><td width="50%">A. 为其它应用程序设置环境变量</td><td>B. 在子shell中运行命令</td></tr>
  <tr><td>C. 使在子shell中可以使用命令历史记录</td><td>D. 提供NFS分区给网络中的其它程序使用</td></tr>
</table>


> **答案：A**
>
> **解析：**`export 变量` 将 Shell 变量导出为环境变量，使其对当前 Shell 及其派生的子进程（其他应用程序）可见；在子 Shell 中运行命令用 `(命令)` 或 `bash -c`。
>
> **类似题：**下列命令中可以把变量 PATH 导出为环境变量的是（　）。A. export PATH　B. alias PATH　C. set PATH　D. env PATH —— **答案：A**。
>
> **知识点：**
> - 环境变量可被子进程继承，普通变量只在当前 Shell 有效。
> - 查看：`export -p`、`env`；取消：`unset 变量`。

**9. 用来装载所有在 `/etc/fstab` 中定义的文件系统的命令是(  )**
<table>
  <tr><td width="50%">A. amount</td><td>B. mount -a</td></tr>
  <tr><td>C. fmount</td><td>D. mount -f</td></tr>
</table>


> **答案：B**
>
> **解析：**`mount -a` 读取 `/etc/fstab` 并挂载其中定义的所有文件系统；`amount`、`fmount` 不是命令，`mount -f` 是伪装挂载（不真正执行系统调用），不是全部挂载。
>
> **类似题：**系统启动时自动挂载文件系统依据的文件是（　）。A. /etc/mtab　B. /etc/fstab　C. /etc/mounts　D. /proc/mounts —— **答案：B**。
>
> **知识点：**
> - `/etc/fstab` 每行六字段：设备、挂载点、类型、选项、dump、fsck 顺序。
> - `mount` 不带参数显示当前挂载情况。

**10. 显示一个文件最后几行内容的命令是(  )**
<table>
  <tr><td width="50%">A. tail</td><td>B. tac</td></tr>
  <tr><td>C. rear</td><td>D. last</td></tr>
</table>


> **答案：A**
>
> **解析：**`tail` 显示文件末尾内容（默认 10 行）；`tac` 是反向逐行显示；`rear`、`last` 不用于查看文件尾部。
>
> **类似题：**动态查看日志文件新增内容的命令是（　）。A. tail -f 文件　B. head 文件　C. cat 文件　D. more 文件 —— **答案：A**。
>
> **知识点：**
> - `tail -n 20` 显示最后 20 行，`tail -f` 跟踪文件增长（常用于日志监控）。
> - `head` 显示文件开头，`head -n` 指定行数。

**11. 使用 `ln` 命令生成一个指向文件old的符号链接new，如果将文件夹old删除，是否还能够访问文件中的数据？(  )**
<table>
  <tr><td width="50%">A. 仍旧可以访问</td><td>B. 不能再访问</td></tr>
  <tr><td>C. 能否访问取决于文件夹之下文件的所有者</td><td>D. 能否访问取决于文件的权限</td></tr>
</table>


> **答案：B**
>
> **解析：**软链接类似快捷方式，指向源文件 old。删除源文件后，软链接失去目标，无法再通过它访问数据（成为死链接）；是否可访问与所有者、权限无关。
>
> **类似题：**删除软链接自身（保留源文件）的命令是（　）。A. rm 链接名　B. rm 源文件名　C. unlink 源文件名　D. rm -r 源文件名 —— **答案：A**。
>
> **知识点：**
> - 软链接文件很小，只存储目标路径；`ls -l` 显示为 `链接 -> 目标`。
> - 硬链接删除任一名字后数据仍可通过其他名字访问。

**12. 显示Linux系统中已注册的用户数（包含系统用户）的命令是(  )**
<table>
  <tr><td width="50%">A. wc --lines /etc/passwd</td><td>B. nl /etc/passwd | head</td></tr>
  <tr><td>C. wc --users /etc/passwd</td><td>D. account -l</td></tr>
</table>


> **答案：A**
>
> **解析：**`/etc/passwd` 每行对应一个注册用户，`wc --lines /etc/passwd`（等价 `wc -l`）统计行数即用户数（含系统用户）。`wc --users` 不是有效选项，`nl | head` 只能看开头，`account -l` 不是标准命令。
>
> **类似题：**统计系统中普通用户数量可执行（　）。A. grep -c "/bin/bash" /etc/passwd　B. wc -c /etc/passwd　C. cat /etc/passwd　D. grep /etc/passwd —— **答案：A**。
>
> **知识点：**
> - `/etc/passwd` 字段：用户名:密码占位:UID:GID:注释:主目录:Shell。
> - 系统用户 Shell 通常为 `/sbin/nologin` 或 `/bin/false`。

**13. 下面不能用来查看网络故障的命令是(  )**
<table>
  <tr><td width="50%">A. netstat</td><td>B. ping</td></tr>
  <tr><td>C. traceroute</td><td>D. init</td></tr>
</table>


> **答案：D**
>
> **解析：**`netstat`、`ping`、`traceroute` 都是网络故障排查工具；`init` 是系统初始化进程（PID 1），用于管理系统运行级别，与网络故障排查无关。
>
> **类似题：**查看本机监听端口和网络连接状态的命令是（　）。A. netstat -tlnp　B. init　C. hostname　D. uname —— **答案：A**。
>
> **知识点：**
> - `netstat -tlnp`：t=TCP、l=监听、n=数字显示、p=进程。
> - 新版系统可用 `ss -tlnp` 替代 netstat。

**14. 删除非空子目录 `/tmp` ，应使用命令(  )**
<table>
  <tr><td width="50%">A. rm -rf /tmp</td><td>B. del /tmp/*</td></tr>
  <tr><td>C. rm -Ra /tmp/*</td><td>D. rm -rf /tmp/*</td></tr>
</table>


> **答案：A**
>
> **解析：**`rm -rf 目录` 递归且强制删除非空目录（-r 递归，-f 不提示），因此删除非空子目录 /tmp 用 `rm -rf /tmp`；`del` 是 Windows 命令；`rm -Ra /tmp/*` 只删除目录内容且 -R 非标准选项组合；`rm -rf /tmp/*` 只删除内容不删除目录本身。
>
> **类似题：**强制递归删除目录 /data 及其内容，应执行（　）。A. rm -rf /data　B. rmdir /data　C. rm -r /data/*　D. del /data —— **答案：A**。
>
> **知识点：**
> - `rm -r` 递归删除，`rm -f` 强制不提示，`rm -rf` 最危险，慎用。
> - `rmdir` 只能删除空目录。

**15. Linux系统中，文件描述符0表示(  )**
<table>
  <tr><td width="50%">A. 标准输出设备</td><td>B. 标准输入设备</td></tr>
  <tr><td>C. 管道</td><td>D. 标准错误输出设备</td></tr>
</table>


> **答案：B**
>
> **解析：**文件描述符 0 表示标准输入（stdin，默认键盘），1 表示标准输出（stdout），2 表示标准错误输出（stderr）。
>
> **类似题：**标准错误输出对应的文件描述符是（　）。A. 0　B. 1　C. 2　D. 3 —— **答案：C**。
>
> **知识点：**
> - 重定向：`0<` 输入、`1>` 输出、`2>` 错误。
> - `2>&1` 把标准错误重定向到标准输出。

**16. 如果想列出当前目录及其子目录下所有扩展名为“.txt”的文件，可以使用命令(  )**
<table>
  <tr><td width="50%">A. find . -name "*.txt"</td><td>B. ls *.txt</td></tr>
  <tr><td>C. ls -d .txt</td><td>D. find . ".txt"</td></tr>
</table>


> **答案：A**
>
> **解析：**`find . -name "*.txt"` 从当前目录递归查找所有扩展名为 .txt 的文件；`ls *.txt` 只查找当前目录（不递归）；`ls -d .txt` 是查看名为 .txt 的目录本身；`find . ".txt"` 语法不完整。
>
> **类似题：**在 /home 下递归查找所有名为 test 的文件，应使用（　）。A. find /home -name test　B. ls /home test　C. find /home test　D. grep test /home —— **答案：A**。
>
> **知识点：**
> - `find 路径 -name 模式` 按名称查找，`-type f/d` 按类型，`-size` 按大小，`-mtime` 按时间。
> - find 的 `-exec 命令 {} \;` 可对结果执行操作。

**17. 执行命令 `chmod 664 file1` 后，该文件的访问权限是(  )**
<table>
  <tr><td width="50%">A. rw-r-r--</td><td>B. rw-rw-r--</td></tr>
  <tr><td>C. rwxr--rw-</td><td>D. rwxrw-rw-</td></tr>
</table>


> **答案：B**
>
> **解析：**664 对应所有者 6=rw-、同组用户 6=rw-、其他用户 4=r--，即 `rw-rw-r--`。
>
> **类似题：**权限 `rw-r--r--` 对应的数字权限是（　）。A. 644　B. 664　C. 646　D. 744 —— **答案：A**。
>
> **知识点：**
> - 权限计算：r=4、w=2、x=1，三位依次为 u、g、o。
> - `chmod 664 file1` 即设置所有者与同组可读写、其他只读。

**18. 下列可以设定使用者密码的命令是(  )**
<table>
  <tr><td width="50%">A. password</td><td>B. pwd</td></tr>
  <tr><td>C. newpwd</td><td>D. passwd</td></tr>
</table>


> **答案：D**
>
> **解析：**设置用户密码使用 `passwd` 命令；`pwd` 显示当前目录；`password`、`newpwd` 不是标准命令。
>
> **类似题：**root 为用户 tom 设置密码的命令是（　）。A. passwd tom　B. pwd tom　C. password tom　D. tom passwd —— **答案：A**。
>
> **知识点：**
> - `passwd` 直接回车修改当前用户密码。
> - 密码复杂度、有效期等由 PAM 和 `/etc/shadow` 控制。

**19. 以下可以切换使用者身份的命令是(  )**
<table>
  <tr><td width="50%">A. su</td><td>B. who</td></tr>
  <tr><td>C. log</td><td>D. passwd</td></tr>
</table>


> **答案：A**
>
> **解析：**`su`（switch user）用于切换使用者身份；`who` 显示登录用户，`log`、`passwd` 与身份切换无关。
>
> **类似题：**普通用户临时以 root 身份执行一条命令，应使用（　）。A. su root　B. sudo 命令　C. login root　D. whoami —— **答案：B**。
>
> **知识点：**
> - `su 用户名` 切换用户；`sudo 命令` 以授权身份执行单条命令。
> - `whoami` 显示当前有效用户名。

**20. 用户组的密码信息存储在哪个文件？(  )**
<table>
  <tr><td width="50%">A. shadow</td><td>B. group</td></tr>
  <tr><td>C. passwd</td><td>D. gshadow</td></tr>
</table>


> **答案：D**
>
> **解析：**用户组密码信息存储在 `/etc/gshadow` 文件中；`/etc/shadow` 存用户密码，`/etc/group` 存组基本信息，`/etc/passwd` 存用户基本信息。
>
> **类似题：**`/etc/group` 文件中不包含的字段是（　）。A. 组名　B. 组密码占位符　C. GID　D. 用户密码散列 —— **答案：D**。
>
> **知识点：**
> - `/etc/group` 字段：组名:密码占位:GID:组成员列表。
> - `/etc/gshadow` 字段：组名:加密密码:管理员:成员列表。

**21. 文本文件fruits内容示例如下：**
<style>
  .fruit-table {
    border: 1px solid black;
    border-collapse: collapse;
    font-family: monospace;
  }
  .fruit-table td {
    border: none;
    padding: 4px 8px;
    font-weight: normal;
    text-align: left;
  }
</style>

<table class="fruit-table">
  <tr><td>banana:30:5.5</td></tr>
  <tr><td>apple:10:2.5</td></tr>
  <tr><td>pear:90:2.3</td></tr>
  <tr><td>orange:20:3.4</td></tr>
</table>

**该文件有三列（冒号隔开），分别表示水果名称、数量和价格。如果想以水果数量从多到少排序，可执行命令(  )**
<table>
  <tr><td width="50%">A. sort -r -n -k 2 -t ":" fruits</td><td>B. sort -r -n -k 1 -t ":" fruits</td></tr>
  <tr><td>C. sort -n -k 2 -t ":" fruits</td><td>D. sort -n -k 1 -t ":" fruits</td></tr>
</table>


> **答案：A**
>
> **解析：**文件以冒号分隔，第 2 列是数量。`sort -r -n -k 2 -t ":"`：`-t ":"` 指定分隔符，`-k 2` 按第 2 列，`-n` 按数值，`-r` 从大到小（从多到少）排序。
>
> **类似题：**以冒号分隔、按第 3 列数值升序排序的命令是（　）。A. sort -t : -k 3 -n file　B. sort -t : -k 3 -r file　C. sort -k : -n 3 file　D. sort -n -k 2 -t : file —— **答案：A**。
>
> **知识点：**
> - `sort -k` 字段编号从 1 开始；`-t` 指定分隔符；`-n` 数值排序（否则按字典序，10 会排在 9 前面）。
> - `-u` 去重，`-f` 忽略大小写。

**22. 下列关于kill命令的说法错误的是(  )**
<table>
  <tr><td width="50%">A. 使用kill命令一定可以终止一个进程</td><td>B. kill命令只能识别 PID，而不识别进程名</td></tr>
  <tr><td>C. kill命令用来向进程发送一个信号</td><td>D. kill -9常用来强制中止进程</td></tr>
</table>


> **答案：A**
>
> **解析：**`kill` 本质是向进程发送信号（默认 SIGTERM 15），如果进程捕获或忽略了该信号，就不一定会终止，因此“使用 kill 命令一定可以终止一个进程”错误。kill 只能按 PID 操作，`kill -9` 发送不可捕获的 SIGKILL，常用于强制终止。
>
> **类似题：**强制终止 PID 为 123 的进程，应使用（　）。A. kill -9 123　B. kill -15 123　C. kill -1 123　D. kill -2 123 —— **答案：A**。
>
> **知识点：**
> - 常用信号：1 SIGHUP、2 SIGINT、9 SIGKILL、15 SIGTERM。
> - `kill -l` 列出所有信号；`pkill`、`killall` 可按名称匹配。

**23. 在 `top` 命令的显示窗口中，如果要使显示按照内存占用率排序，则应按下(  )**
<table>
  <tr><td width="50%">A. m</td><td>B. M</td></tr>
  <tr><td>C. p</td><td>D. q</td></tr>
</table>


> **答案：B**
>
> **解析：**top 交互界面中，大写 `M` 按内存占用率排序，大写 `P` 按 CPU 占用率排序，`q` 退出，小写 `m` 切换内存显示方式。
>
> **类似题：**top 界面中按 CPU 占用率排序应按（　）。A. m　B. P　C. M　D. q —— **答案：B**。
>
> **知识点：**
> - top 汇总区：系统负载、任务数、CPU、内存、swap。
> - 交互键：`k` 终止进程、`r` renice、`1` 显示各 CPU、`h` 帮助。

**24. 为了显示当前目录下的磁盘占用量大小，应使用命令(  )**
<table>
  <tr><td width="50%">A. du -sh</td><td>B. df</td></tr>
  <tr><td>C. ls -l</td><td>D. ls -r</td></tr>
</table>


> **答案：A**
>
> **解析：**`du -sh` 统计当前目录占用的磁盘空间总量（-s 汇总，-h 易读格式）；`df` 查看文件系统整体空间，`ls -l` 只列文件属性，`ls -r` 逆序显示。
>
> **类似题：**统计 /var/log 目录占用空间并汇总显示，应使用（　）。A. du -sh /var/log　B. df -h /var/log　C. ls -lh /var/log　D. free -h —— **答案：A**。
>
> **知识点：**
> - `du -s` 只输出总计，`-a` 显示每个文件，`-c` 最后加总，`--exclude` 排除。
> - 磁盘空间使用率用 `df -h`。

**25. 能够列出系统中的所有可用于块设备的信息，并显示他们之间依赖关系的命令是(  )**
<table>
  <tr><td width="50%">A. lscpu</td><td>B. dmesg</td></tr>
  <tr><td>C. vmstat</td><td>D. lsblk</td></tr>
</table>


> **答案：D**
>
> **解析：**`lsblk`（list block devices）以树状结构列出系统中所有块设备及其依赖关系（磁盘、分区、挂载点）；`lscpu` 看 CPU，`dmesg` 看内核消息，`vmstat` 看系统资源统计。
>
> **类似题：**查看块设备分区结构树，应使用（　）。A. lsblk　B. lsusb　C. lspci　D. lscpu —— **答案：A**。
>
> **知识点：**
> - `lsblk -f` 额外显示文件系统类型和 UUID。
> - `blkid` 查看块设备 UUID，`df -h` 查看挂载使用情况。

**26. 可以配置机器主机名的文件是(  )**
<table>
  <tr><td width="50%">A. /etc/sysconfig/network</td><td>B. /etc/hosts</td></tr>
  <tr><td>C. /etc/sysconfig/network-scripts/ifcfg-eth0</td><td>D. /etc/fstab</td></tr>
</table>


> **答案：A**
>
> **解析：**在 RedHat 系中，`/etc/sysconfig/network` 文件用于配置主机名、网关等全局网络参数；`/etc/hosts` 是主机名解析文件，`ifcfg-eth0` 配置网卡，`/etc/fstab` 配置文件系统挂载。
>
> **类似题：**永久设置主机名为 server1 还可以使用命令（　）。A. hostnamectl set-hostname server1　B. hostname -a server1　C. ifconfig server1　D. echo server1 > /etc/hosts —— **答案：A**。
>
> **知识点：**
> - `hostname` 临时修改，重启失效；修改配置文件永久生效。
> - 新版 systemd 系统使用 `hostnamectl` 管理主机名。

**27. 可以启动httpd服务的命令是(  )**
<table>
  <tr><td width="50%">A. /etc/init.d/httpd start</td><td>B. httpd start</td></tr>
  <tr><td>C. /etc/init.d/httpd status</td><td>D. service httpd</td></tr>
</table>


> **答案：A**
>
> **解析：**SysV 风格下可以通过服务脚本的绝对路径启动 httpd：`/etc/init.d/httpd start`；也可用 `service httpd start`。`httpd start`、`service httpd`（缺少动作）、`/etc/init.d/httpd status`（只是查看状态）均不符合题意。
>
> **类似题：**设置 httpd 开机自启的命令是（　）。A. chkconfig httpd on　B. chkconfig httpd off　C. service httpd start　D. /etc/init.d/httpd stop —— **答案：A**。
>
> **知识点：**
> - 服务操作：start/stop/restart/status。
> - `chkconfig --list` 查看各服务在各运行级别的自启状态。

**28. 在Vi编辑器的命令模式下，要删除光标所在位置的一个字（单词），应使用命令(  )**
<table>
  <tr><td width="50%">A. db</td><td>B. dd</td></tr>
  <tr><td>C. dw</td><td>D. cc</td></tr>
</table>


> **答案：C**
>
> **解析：**vi 命令模式下 `dw`（delete word）删除光标所在处到单词末尾的内容；`dd` 删除整行，`db` 删除光标前一个单词，`cc` 修改整行。
>
> **类似题：**vi 中删除光标处到行尾的命令是（　）。A. d$　B. d0　C. dd　D. dw —— **答案：A**。
>
> **知识点：**
> - 删除命令可与移动命令组合：`dw`、`d$`、`d0`、`dG`。
> - `cw` 修改单词，`cc` 修改行，`r` 替换单个字符。

**29. 在vi编辑器中，用字符串str2替换正文中所有的出现的字符串str1，应使用命令(  )**
<table>
  <tr><td width="50%">A. :1,$ s/str1/str2/g</td><td>B. :s/str/str2/</td></tr>
  <tr><td>C. :s/str1/str2/g</td><td>D. :%s/str/str2/</td></tr>
</table>


> **答案：A**
>
> **解析：**全文替换所有 str1 为 str2 的命令是 `:1,$ s/str1/str2/g` 或等价的 `:%s/str1/str2/g`。`1,$` 表示从第 1 行到最后一行，`g` 表示每行全部替换。
>
> **类似题：**vi 中只替换当前行所有 old 的命令是（　）。A. :s/old/new/g　B. :%s/old/new/g　C. :1,$s/old/new/　D. :s/old/new/ —— **答案：A**。
>
> **知识点：**
> - 替换范围：当前行（:s）、指定行（:n,ms）、全文（:%s 或 :1,$s）。
> - `g` 全局替换，`c` 替换前确认。

**30. 用来显示vi编辑器所有设置选项的命令是(  )**
<table>
  <tr><td width="50%">A. set</td><td>B. set all</td></tr>
  <tr><td>C. set list</td><td>D. set c?</td></tr>
</table>


> **答案：B**
>
> **解析：**`:set all` 列出 vi 的所有设置选项及当前值；`:set` 只显示被修改过的选项；`:set list` 显示不可见字符；`:set c?` 查询某个选项的值。
>
> **类似题：**查看 vi 中某个选项当前值的命令是（　）。A. :set all　B. :set option?　C. :set option!　D. :show option —— **答案：B**。
>
> **知识点：**
> - `:set nu` 显示行号、`:set ic` 忽略大小写、`:set ai` 自动缩进、`:set ts=4` 制表位。
> - 设置可写入 `~/.vimrc` 持久化。

**31. 在Emacs编辑器中，将光标移到当前行的行首的命令是(  )**
<table>
  <tr><td width="50%">A. &lt;Ctrl&gt;+a</td><td>B. &lt;Ctrl&gt;+e</td></tr>
  <tr><td>C. &lt;Alt&gt;+<</td><td>D. &lt;Alt&gt;+v</td></tr>
</table>


> **答案：A**
>
> **解析：**Emacs 中 `Ctrl+a` 把光标移到当前行的行首，`Ctrl+e` 移到行尾；`Alt+<` 移到文件开头；`Alt+v` 向后翻一屏。
>
> **类似题：**Emacs 中把光标移到文件开头的组合键是（　）。A. Alt+<　B. Ctrl+a　C. Alt+>　D. Ctrl+Home —— **答案：A**。
>
> **知识点：**
> - 行首行尾：`C-a`、`C-e`；文件首尾：`M-<`、`M->`。
> - 翻页：`C-v`、`M-v`。

**32. 在Emacs编辑器中，保存文件并退出的正确做法是(  )**
<table>
  <tr><td width="50%">A. 先按&lt;Ctrl&gt;+x，再按&lt;Ctrl&gt;+w</td><td>B. 先按&lt;Ctrl&gt;+x，再按&lt;Ctrl&gt;+c</td></tr>
  <tr><td>C. 先按&lt;Ctrl&gt;+x，再按&lt;Ctrl&gt;+s</td><td>D. 按&lt;Ctrl&gt;+w</td></tr>
</table>


> **答案：B**
>
> **解析：**Emacs 中 `Ctrl+x Ctrl+c` 是退出命令，退出时若缓冲区有未保存的修改会提示是否保存；`Ctrl+x Ctrl+s` 只是保存当前缓冲区，不会退出；`Ctrl+x Ctrl+w` 是另存为；`Ctrl+w` 是剪切区域。因此“保存文件并退出”应选 B。
>
> **类似题：**Emacs 中把文件另存为其他名字的组合键是（　）。A. Ctrl+x Ctrl+w　B. Ctrl+x Ctrl+s　C. Ctrl+x Ctrl+c　D. Ctrl+x Ctrl+f —— **答案：A**。
>
> **知识点：**
> - `C-x C-s` 保存、`C-x C-c` 退出、`C-x C-w` 另存为、`C-x C-f` 打开文件。
> - `C-x C-c` 会对所有未保存缓冲区依次询问。

**33. 用于存储所有授权的远程客户系统的公钥，使远程客户端系统能够ssh免密登录的文件是(  )**
<table>
  <tr><td width="50%">A. authorized_keys</td><td>B. ssh_keys</td></tr>
  <tr><td>C. authorized_hosts</td><td>D. know_hosts</td></tr>
</table>


> **答案：A**
>
> **解析：**服务端 `~/.ssh/authorized_keys` 文件存放允许免密登录该账户的客户端公钥；客户端 `~/.ssh/known_hosts` 存放已连接过的主机公钥指纹。
>
> **类似题：**客户端首次 SSH 连接服务器时提示确认指纹，确认后的信息保存在（　）。A. ~/.ssh/known_hosts　B. ~/.ssh/authorized_keys　C. /etc/ssh/ssh_config　D. /etc/hosts —— **答案：A**。
>
> **知识点：**
> - 免密登录配置：`ssh-keygen` 生成密钥 → `ssh-copy-id user@host` 分发公钥。
> - 服务端权限要求：`.ssh` 目录 700，authorized_keys 600。

**34. 用于启动SSH服务的命令是(  )**
<table>
  <tr><td width="50%">A. service sshd start</td><td>B. service ssh start</td></tr>
  <tr><td>C. chkconfig sshd on</td><td>D. chkconfig ssh on</td></tr>
</table>


> **答案：A**
>
> **解析：**SSH 服务端进程名为 sshd，启动命令为 `service sshd start`；`service ssh start` 服务名错误；`chkconfig` 只管理自启状态，不启动服务。
>
> **类似题：**重启 SSH 服务的命令是（　）。A. service sshd restart　B. service sshd on　C. chkconfig sshd on　D. sshd start —— **答案：A**。
>
> **知识点：**
> - sshd 服务配置文件 `/etc/ssh/sshd_config`。
> - 修改配置后需 restart 或 reload 生效。

**35. 使用scp上传本地目录 `/opt/www` 到服务器10.1.0.2目录/opt下的命令是(  )**
<table>
  <tr><td width="50%">A. scp -r /opt/www/ root@10.1.0.2:/opt/</td><td>B. scp root@10.1.0.2:/opt/ /opt/www/</td></tr>
  <tr><td>C. scp -r root@10.1.0.2:/opt/ /opt/www/</td><td>D. scp /opt/www/ root@10.1.0.2:/opt/</td></tr>
</table>


> **答案：A**
>
> **解析：**上传目录需要 `-r` 递归选项，命令格式为 `scp -r 本地目录 用户@远程主机:远程路径`，即 `scp -r /opt/www/ root@10.1.0.2:/opt/`。
>
> **类似题：**从服务器下载目录 /opt/www 到本地 /opt，应使用（　）。A. scp -r root@10.1.0.2:/opt/www/ /opt/　B. scp -r /opt/ root@10.1.0.2:/opt/www/　C. cp -r root@10.1.0.2:/opt/www/ /opt/　D. scp /opt/www/ /opt/ —— **答案：A**。
>
> **知识点：**
> - scp 上传：本地在前，远程在后；下载：远程在前，本地在后。
> - 目录必须加 `-r`；指定端口用 `-P`。

**36. 在GDB中，要删除行号为22处的断点，应使用命令(  )**
<table>
  <tr><td width="50%">A. delete 22</td><td>B. clear 22</td></tr>
  <tr><td>C. d 22</td><td>D. r 22</td></tr>
</table>


> **答案：B**
>
> **解析：**GDB 中 `clear 行号` 删除指定源文件行号处的断点；`delete 编号` 删除指定断点编号的断点；`d 22` 若 22 是断点编号也可删除，但题目明确“行号为 22 处”，应选 clear 22。
>
> **类似题：**GDB 中删除 1 号断点的命令是（　）。A. delete 1　B. clear 1　C. disable 1　D. remove 1 —— **答案：A**（clear 1 表示删除第 1 行的断点）。
>
> **知识点：**
> - `info breakpoints` 查看断点编号与位置。
> - `disable 编号` 禁用、`enable 编号` 启用、`delete` 删除。

**37. 在GDB调试中，如果想单步执行下一条指令，而不是下一行程序，应使用命令(  )**
<table>
  <tr><td width="50%">A. next</td><td>B. nexti</td></tr>
  <tr><td>C. step</td><td>D. continue</td></tr>
</table>


> **答案：B**
>
> **解析：**`nexti`（next instruction）单步执行下一条机器指令（不进入被调函数）；`next` 单步执行下一行源码；`step` 单步执行并进入函数；`continue` 继续运行。
>
> **类似题：**GDB 中单步执行一条机器指令且遇到函数调用时进入函数内部，应使用（　）。A. stepi　B. nexti　C. next　D. step —— **答案：A**。
>
> **知识点：**
> - 源码级：`next`、`step`；汇编级：`nexti`、`stepi`。
> - 使用 `disassemble` 可查看当前函数的汇编代码。

**38. gcc编译参数 `-w` 的作用是(  )**
<table>
  <tr><td width="50%">A. 关闭所有警告</td><td>B. 输出所有警告信息</td></tr>
  <tr><td>C. 指定输出文件名</td><td>D. 产生调试信息</td></tr>
</table>


> **答案：A**
>
> **解析：**GCC 的 `-w` 选项用于关闭所有警告信息；`-Wall` 开启所有警告；`-o` 指定输出文件名；`-g` 生成调试信息。
>
> **类似题：**GCC 中开启所有警告信息的选项是（　）。A. -w　B. -Wall　C. -Werror　D. -g —— **答案：B**。
>
> **知识点：**
> - `-Werror` 把警告当作错误处理。
> - 建议编译时使用 `-Wall` 以发现潜在问题。

**39. MVC模式中，C指的是(  )**
<table>
  <tr><td width="50%">A. 视图</td><td>B. 模型</td></tr>
  <tr><td>C. 控制器</td><td>D. 客户端</td></tr>
</table>


> **答案：C**
>
> **解析：**MVC 中 M=Model（模型）、V=View（视图）、C=Controller（控制器），控制器负责接收用户请求、调度模型和视图。
>
> **类似题：**MVC 模式中负责数据与业务逻辑的是（　）。A. Model　B. View　C. Controller　D. Client —— **答案：A**。
>
> **知识点：**
> - MVC 降低耦合、便于维护和测试。
> - 常见实现：Spring MVC、Struts2。

**40. 如果要查看已经被安装的JDK的包信息，可以用该命令(  )**
<table>
  <tr><td width="50%">A. rpm -qa | grep java</td><td>B. rpm -e java</td></tr>
  <tr><td>C. java -version</td><td>D. java -path</td></tr>
</table>

> **答案：A**
>
> **解析：**查看已安装的 JDK 包信息可用 `rpm -qa | grep java`：`-qa` 列出所有已安装包，管道 `grep java` 筛选出 java 相关包；`rpm -e` 是卸载，`java -version` 显示版本号而非包信息。
>
> **类似题：**查询系统中所有已安装的 RPM 包，应使用（　）。A. rpm -qa　B. rpm -ql　C. rpm -e　D. rpm -V —— **答案：A**。
>
> **知识点：**
> - `rpm -qi 包名` 查看包详细信息，`rpm -ql 包名` 列出包内文件。
> - 用 `yum list installed | grep java` 也可查询。


## 二、填空题（共 10 题）

41. 现代操作系统中，\_\_\_\_\_\_\_\_\_\_通常是处理器调度运行的最小单位。

> **答案：线程**
>
> **解析：**现代操作系统中，进程是资源分配的基本单位，线程是处理器调度运行的最小单位。同一进程内的多个线程共享该进程的资源。
>
> **类似题：**线程与进程的关系是（　）。A. 线程是资源分配的基本单位　B. 进程是处理器调度的最小单位　C. 线程共享所属进程的资源　D. 一个进程只能有一个线程 —— **答案：C**。
>
> **知识点：**
> - 线程开销小、切换快、通信方便，但一个线程崩溃可能影响整个进程。
> - 进程拥有独立地址空间，线程共享地址空间。

42. Linux内核版本号由3组数字组成，如4.16.13，该内核次版本号是\_\_\_\_\_\_\_\_\_\_。

> **答案：16**
>
> **解析：**Linux 内核版本号格式为“主版本号.次版本号.修订号”，4.16.13 中主版本号 4、次版本号 16、修订号 13，因此次版本号是 16。
>
> **类似题：**内核版本 3.10.0 的主版本号是（　）。A. 3　B. 10　C. 0　D. 310 —— **答案：A**。
>
> **知识点：**
> - 次版本号为偶数表示稳定版，奇数表示开发版（传统规则）。
> - `uname -r` 查看内核版本，`uname -a` 查看全部系统信息。

43. 通常情况下，RPM软件包采用系统默认的安装路径，可执行的命令会安装在\_\_\_\_\_\_\_\_\_\_目录下。

> **答案：/usr/bin**
>
> **解析：**RPM 包安装时采用系统默认路径：用户可执行命令通常在 `/usr/bin`，管理员命令在 `/usr/sbin`，配置文件在 `/etc`，库文件在 `/usr/lib`，文档在 `/usr/share`。
>
> **类似题：**RPM 安装的软件包中，配置文件通常位于（　）目录。A. /usr/bin　B. /etc　C. /var　D. /opt —— **答案：B**。
>
> **知识点：**
> - 可通过 `rpm -ql 包名` 查看包内文件的具体安装路径。
> - 源码编译安装默认路径一般为 `/usr/local`。

44. 通过配置\_\_\_\_\_\_\_\_\_\_协议，服务器控制器一段IP地址范围，客户机登录服务器时就可以自动获得服务器分配的IP地址和子网掩码。

> **答案：DHCP**
>
> **解析：**DHCP（Dynamic Host Configuration Protocol，动态主机配置协议）使服务器管理一段 IP 地址范围，客户机登录时自动获取 IP 地址、子网掩码、网关、DNS 等网络参数。
>
> **类似题：**DHCP 服务使用的端口是（　）。A. 53　B. 67/68　C. 80　D. 22 —— **答案：B**。
>
> **知识点：**
> - DHCP 采用客户端/服务器模式，客户端 68 端口，服务端 67 端口。
> - 网卡配置 `BOOTPROTO=dhcp` 即通过 DHCP 获取地址。

45. 在 `/home` 目录下查找后缀为 `.dat` 的文件应使用命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`find /home -name "*.dat"`**
>
> **解析：**`find` 从指定路径递归查找文件，`-name "*.dat"` 按名称模式匹配以 .dat 结尾的文件，模式加引号可避免 Shell 提前展开通配符。
>
> **类似题：**在 /var 下查找大于 100MB 的文件，可使用（　）。A. find /var -size +100M　B. find /var -name 100M　C. ls /var -size 100M　D. du /var -size 100M —— **答案：A**。
>
> **知识点：**
> - find 常用条件：`-name`、`-type`、`-size`、`-mtime`、`-user`、`-perm`。
> - 组合条件：`-a`（与）、`-o`（或）、`!`（非）。

46. 改变当前目录下文件file1的访问权限为所属组用户都可执行，应使用命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`chmod g+x file1`**
>
> **解析：**`g` 表示同组用户（group），`+x` 表示增加执行权限，即 `chmod g+x file1`；也可用数字法先查看原权限再计算。
>
> **类似题：**给文件 file1 的所有者增加写权限，应使用（　）。A. chmod u+w file1　B. chmod g+w file1　C. chmod o+w file1　D. chmod a+w file1 —— **答案：A**。
>
> **知识点：**
> - 对象：u 所有者、g 同组、o 其他、a 全部。
> - 操作：+ 增加、- 去除、= 设置。

47. 一般使用\_\_\_\_\_\_\_\_\_\_命令查看当前机器的主机名。

> **答案：hostname**
>
> **解析：**`hostname` 命令不带参数时显示当前主机名；带参数可临时设置主机名。也可用 `hostnamectl status` 查看。
>
> **类似题：**查看当前主机名的文件是（　）。A. /etc/hostname　B. /etc/hosts　C. /etc/resolv.conf　D. /etc/sysconfig/network —— **答案：A**（RHEL 6 及以前为 `/etc/sysconfig/network`）。
>
> **知识点：**
> - 临时修改：`hostname 新名字`；永久修改：编辑配置文件或 `hostnamectl set-hostname`。
> - `/etc/hosts` 用于主机名与 IP 的映射。

48. 以MB为单位，显示当前内存的使用情况，应使用命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`free -m`**
>
> **解析：**`free` 显示内存和交换空间使用情况，`-m` 表示以 MB 为单位（默认 KB，`-g` 为 GB，`-h` 为易读格式）。
>
> **类似题：**以 GB 为单位显示内存使用情况的命令是（　）。A. free -m　B. free -g　C. df -g　D. du -g —— **答案：B**。
>
> **知识点：**
> - free 输出主要列：total、used、free、shared、buff/cache、available。
> - available 是应用程序真正可用的内存估计值。

49. 统计文件file1中包含字符串“ASDF”的行数，可使用命令\_\_\_\_\_\_\_\_\_\_。

> **答案：`grep "ASDF" file1 | wc -l`**
>
> **解析：**`grep "ASDF" file1` 筛选出包含 ASDF 的行，管道交给 `wc -l` 统计行数；也可用 `grep -c "ASDF" file1` 一步完成。
>
> **类似题：**统计文件中包含字符串 abc 的行数，最简洁的命令是（　）。A. grep -c abc file　B. grep abc file | wc -c　C. wc -l abc file　D. cat file | grep abc —— **答案：A**。
>
> **知识点：**
> - `grep -c` 直接输出匹配行数。
> - `grep -n` 显示行号，`grep -i` 忽略大小写，`grep -v` 反向匹配。

50. 在Linux系统中，表示进程优先级的有两个参数：Priority和Nice，这两个数值越\_\_\_\_\_\_\_\_\_\_代表该进程越被优先CPU处理。

> **答案：小**
>
> **解析：**进程优先级的两个参数 Priority（PRI）和 Nice（NI）都是数值越小表示优先级越高，越容易被调度获得 CPU；数值越大表示优先级越低。
>
> **类似题：**nice 值为 -10 的进程与 nice 值为 10 的进程相比，优先级（　）。A. 前者高　B. 后者高　C. 相同　D. 无法比较 —— **答案：A**。
>
> **知识点：**
> - nice 范围 -20（最高）～19（最低）。
> - 普通用户只能增大 nice 值；root 可减小 nice 值提高优先级。


## 三、综合应用题（共 2 题）

**1. 假设有两台服务器，主机名分别是server1和server2。如果想配置在server1上以用户名user2免密登录到server2上：**
(1) 首先，应该在服务器server1上运行【51】\_\_\_\_\_\_\_\_\_\_命令来生成密钥；
(2) 然后进入当前用户（user1）的 `/home/user1/`【52】\_\_\_\_\_\_\_\_\_\_目录；
(3) 执行如下命令把生成的公钥复制到server2上：
   【53】\_\_\_\_\_\_\_\_\_\_ `user2@server2:/home/user2/.ssh/id_rsa.pub.s1`
(4) 登录server2，进入相应目录后执行追加操作：
   `cat id_rsa.pub.s1 >>` 【54】\_\_\_\_\_\_\_\_\_\_
(5) 再调用sshd restart重新启动ssh服务之后，就能使用server1上的user1免密登录server2上的user2账号了。用户登录到server2上以后，要编译和调试 `/home/user2/tmp` 下的一个c语言程序 `tmp.c`。首先调用如下命令进行编译链接：
   `gcc tmp.c` 【55】\_\_\_\_\_\_\_\_\_\_ `../include -Wall` 【56】\_\_\_\_\_\_\_\_\_\_ `-O2 -o tmp`
(6) 如果有多个源文件且依赖关系比较复杂，应编写【57】\_\_\_\_\_\_\_\_\_\_文件描述编译规则。
(7) 输入【58】\_\_\_\_\_\_\_\_\_\_对编译生成的tmp程序启动gdb调试；
(8) 在调试器中输入【59】\_\_\_\_\_\_\_\_\_\_对函数 `func1` 位置设置断点。
(9) 然后输入run命令开始执行程序，程序停留在断点处。如需进入函数内部单步执行，应输入【60】\_\_\_\_\_\_\_\_\_\_命令。


> **参考答案与解析：**
>
> - 【51】`ssh-keygen` —— 生成公钥/私钥对。
> - 【52】`.ssh` —— 密钥默认存放在用户主目录下的 `.ssh` 隐藏目录中。
> - 【53】`scp` —— 把生成的公钥复制到 server2 上（如 `scp id_rsa.pub user2@server2:/home/user2/.ssh/id_rsa.pub.s1`）。
> - 【54】`authorized_keys` —— 将公钥追加到 `~/.ssh/authorized_keys` 中（完整路径为 `/home/user2/.ssh/authorized_keys`）。
> - 【55】`-I` —— 指定头文件搜索目录（`-I ../include`，大写 i）。
> - 【56】`-g` —— 生成调试信息，供 gdb 使用。
> - 【57】`Makefile`（或 `makefile`）—— 描述多文件编译依赖关系。
> - 【58】`gdb tmp` —— 启动 GDB 调试 tmp 程序。
> - 【59】`break func1`（或 `b func1`）—— 在 func1 函数入口处设置断点。
> - 【60】`step`（或 `s`）—— 单步执行并进入函数内部。
>
> **解析要点：**
> - 完整编译命令：`gcc tmp.c -I ../include -Wall -g -O2 -o tmp`。
> - 免密登录原理：服务端 authorized_keys 保存客户端公钥，登录时用私钥验证。
> - `-I` 头文件路径、`-L` 库文件路径、`-l` 链接库名，注意区分大小写。
>
> **变式练习：**GCC 中指定库文件搜索路径的选项是（　）。A. -I　B. -L　C. -l　D. -g —— **答案：B**。
>
> **知识点：**
> - 密钥生成与分发：`ssh-keygen -t rsa`、`ssh-copy-id user@host`。
> - 调试流程：`gcc -g` 编译 → `gdb 程序` → `break`/`run`/`step`/`print`/`continue`。

**2. (1) 编写脚本程序 `prune.sh`，功能为删除给定文件中的倒数第2和第3两行后形成新文件，用法如下：`prune.sh file[file…]`**
```bash
#!/bin/sh
for file 【61】__________ #对每个文件进行处理
do
    LINES=`wc 【62】__________ | awk '{print $1}'` #计算行数
    LINES=`expr 【63】__________ `
    head 【64】__________ $file > $file.new #输出前 LINES 行
    【65】__________ -n 1 $file >> $file.new #输出最后一行
done
```

**(2) 编写脚本，提示用户输入一个数字作为参数，然后打印出一个*号组成的方形区域。例如用户输入5，最终显示5x5的星号矩阵：**
`*****`
`*****`
`*****`
`*****`
`*****`
```bash
#!/bin/bash
while :
do
    【66】__________ -p "Please input a number: " n
    n1=`echo $n|sed 's/[0-9]//g'`
    if [ -n "$n1" ]
    then
        echo "$n is not a number."
        【67】__________
    else
        【68】__________
    fi
done

for i in `seq 1 $n`
do
    for j in `seq 1 $n`
    do
        echo 【69】__________ "*"
    done
    【70】__________
done
```

> **参考答案与解析：**
>
> **（1）prune.sh 脚本填空：**
> - 【61】`in $*`（更严谨可写 `in "$@"`）—— 遍历脚本传入的所有文件名参数。
> - 【62】`-l $file` —— `wc -l $file` 统计当前文件行数。
> - 【63】`$LINES - 3` —— `expr` 计算需要保留的前 N 行（删掉倒数第 2、3 行后，前 N-3 行保留）。
> - 【64】`-n $LINES` —— `head -n 前N行 $file` 输出前 N-3 行。
> - 【65】`tail` —— `tail -n 1 $file` 输出最后一行（第 N 行）。
>
> **（2）打印星号方阵脚本填空：**
> - 【66】`read` —— `read -p "..." n` 读取用户输入。
> - 【67】`continue` —— 输入不是数字时跳过本次循环，重新提示输入。
> - 【68】`break` —— 输入合法时跳出 while 循环，继续执行下面的打印逻辑。
> - 【69】`-n` —— `echo -n "*"` 不换行输出星号，使一行星号连续显示。
> - 【70】`echo` —— 内层循环结束后输出一个换行符，形成方阵的一行。
>
> **解析要点：**
> - 删除倒数第 2、3 行：保留前 N-3 行 + 最后 1 行，即 `head -n $((N-3))` 与 `tail -n 1`。
> - `sed 's/[0-9]//g'` 删除输入中的数字，若剩余内容非空说明含有非数字字符。
> - 双重循环：外层控制行数，内层控制每行星号个数。
>
> **变式练习：**Shell 中判断变量 n 是否全为数字，可以执行（　）。A. `[ -n "$(echo $n | sed 's/[0-9]//g')" ]` 判断非数字　B. `[ $n -eq 0 ]`　C. `[ ${#n} -gt 0 ]`　D. `[ -z "$n" ]` —— **答案：A**。
>
> **知识点：**
> - `expr` 用于整数运算和字符串处理，运算符两侧需有空格。
> - `for i in \`seq 1 $n\`` 或 `for ((i=1;i<=n;i++))` 实现计数循环。
> - `echo -n` 不换行输出。


---

